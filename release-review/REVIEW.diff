--- a/src/address_helpers.h
+++ b/src/address_helpers.h
@@ -19,11 +19,11 @@
 // Base58 Alphabet
 static const char* BASE58_ALPHABET = "123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz";
 
-// Patch 15C: decodeBase58Check() is defined below; declare it here so the
+// decodeBase58Check() is defined below; declare it here so the
 // canonical free helper can use the real Base58Check decoder.
 inline std::vector<uint8_t> decodeBase58Check(const std::string &base58Addr);
 
-// Patch 15C: canonical TRU mainnet P2PKH validation.
+// canonical TRU mainnet P2PKH validation.
 inline bool isValidAddress(const std::string &addr) {
     try {
         const auto decoded = decodeBase58Check(addr);
@@ -208,7 +208,7 @@
 
     // 4) Compare hash2[0..3] to cksum
     if (std::memcmp(hash2, cksum.data(), 4) != 0) {
-        // Log the mismatch so you can see the difference
+        // Log the address mismatch for diagnosis.
         std::ostringstream oss;
         oss << "[decodeBase58Check] Checksum mismatch.\n"
             << "    data hex   : " << hexEncode(data) << "\n"
@@ -394,7 +394,7 @@
     return encodeBase58Check(addressBytes);
 }
 
-// Patch 15C: removed unused duplicate script-to-address encoder.
+// removed unused duplicate script-to-address encoder.
 // Native P2PKH address generation uses the canonical Base58Check path.
 
 // Add new function specifically for contract addresses
--- a/src/ai_oracle_service.cpp
+++ b/src/ai_oracle_service.cpp
@@ -51,12 +51,12 @@
                                            const std::string& text)
 {
     // Prefix keeps it discoverable; keep payload small.
-    // If you want binary, you can compress before hex.
+    //
     const std::string prefix = "ai_response:";
     const std::string payload = prefix + requestID + "|" + providerName + "|" + text;
     // OP_RETURN = 0x6a <PUSHDATA>
-    // We'll store ASCII directly in script (your serializer already writes raw bytes)
-    // If you prefer strict-hex payloads, replace with hexEncode(payload) and decode on read.
+    // Store ASCII directly in the script; serialization preserves the bytes.
+    //
     std::string script;
     script.push_back(0x6a);                      // OP_RETURN
     if (payload.size() < 0x4c) {                 // PUSHDATA1 threshold
@@ -103,7 +103,7 @@
     return bytesToHex(v);
 }
 
-// FIX(patch15): normalize text extraction across OpenAI-compatible,
+// normalize text extraction across OpenAI-compatible,
 // Ollama/local, and Anthropic Messages API responses.
 static std::string extractAIProviderText(const nlohmann::json& response) {
     // OpenAI / Grok / Oobabooga Chat Completions.
@@ -202,7 +202,7 @@
 };
 
 // Try to convert the generic wallet UTXO model into OracleUTXO.
-// If your rpcListUnspent already returns the same shape, you can skip this.
+// Adapt wallet UTXOs to the RPC result shape.
 static std::vector<OracleUTXO> toOracleUtxos(const std::vector<UTXO>& in) {
     std::vector<OracleUTXO> out;
     out.reserve(in.size());
@@ -211,13 +211,13 @@
         o.txid            = u.txid;
         o.vout            = (uint32_t)u.vout;
         o.amount          = (uint64_t)u.amount;
-        o.scriptPubKeyHex = u.scriptPubKey; // adjust if your field name differs
+        o.scriptPubKeyHex = u.scriptPubKey; // UTXO script bytes.
         out.push_back(std::move(o));
     }
     return out;
 }
 
-// Pick the first UTXO >= minNeeded (simple strategy; improve if you want)
+// Select the first UTXO whose value is at least minNeeded.
 static bool selectUtxo(const std::vector<OracleUTXO>& utxos, uint64_t minNeeded, OracleUTXO& chosen) {
     for (const auto& u : utxos) {
         if (u.amount >= minNeeded) { chosen = u; return true; }
@@ -292,7 +292,7 @@
         }
 
         // 7) Build transaction
-        Transaction tx;        // non-witness version in your codebase
+        Transaction tx;        // Non-witness transaction.
         tx.version = 1;
 
         // a) Inputs
@@ -342,7 +342,7 @@
     }
 }
 //============================================================
-//      TOKEN EVOLUTION ON-CHAIN ANCHOR (PATCH 22)
+// TOKEN EVOLUTION ON-CHAIN ANCHOR
 //============================================================
 namespace {
 
@@ -1523,7 +1523,7 @@
 //============================================================
 //			HELPERS FOR AI ORACLE
 //============================================================
-// Patch 18: removed the unused createAIRequestTransaction() prototype path.
+// removed the unused createAIRequestTransaction() prototype path.
 // AI requests are queued through ContractStorage/RPC and are not encoded with
 // the consensus OP_DATAFEED opcode.
 
@@ -1601,7 +1601,7 @@
         {"timestamp", std::time(nullptr)}
     };
 
-    // FIX(patch15): NEVER persist cloud API keys into ContractStorage.
+    // NEVER persist cloud API keys into ContractStorage.
     // Persist only provider selection / model / endpoint and other non-secret
     // options. After restart, cloud credentials should come from environment
     // variables such as OPENAI_API_KEY, XAI_API_KEY, ANTHROPIC_API_KEY,
@@ -1790,12 +1790,12 @@
 //=================================================
 //		CREATE TX
 //=================================================
-// Patch 18: the legacy response-script builder that used OP_DATAFEED as a
+// the legacy response-script builder that used OP_DATAFEED as a
 // completion marker was removed. Signed AI commitments continue through the
 // ordinary OP_RETURN anchor path above and remain outside VM consensus feeds.
 
 //=================================================
-//      MANUAL LIVING TOKEN EVOLUTION PREVIEW (PATCH 19)
+// MANUAL LIVING TOKEN EVOLUTION PREVIEW
 //=================================================
 nlohmann::json ConfigurableAIOracle::evolveTokenManually(
     const std::string& tokenID,
@@ -1854,7 +1854,7 @@
 }
 
 //=================================================
-//      TOKEN EVOLUTION ANCHOR QUEUE (PATCH 22)
+// TOKEN EVOLUTION ANCHOR QUEUE
 //=================================================
 void ConfigurableAIOracle::processTokenEvolutionAnchorQueue()
 {
@@ -2743,7 +2743,7 @@
     // Store in contract storage
     storage->storeContractData("AI_ORACLE", "response:" + requestID, responseData.dump());
 
-    // Also store in global blockchain storage (as you had)
+    // Also persist in global blockchain storage.
     if (blockchain && blockchain->getStorage()) {
         const std::string respKey  = "ai_response:" + requestID;
         const std::string tokenKey = "ai_token:" + tokenID + ":last_response";
--- a/src/ai_oracle_service.h
+++ b/src/ai_oracle_service.h
@@ -13,7 +13,7 @@
 using nlohmann::json;
 class Blockchain;
 
-// FIX(patch16): declaration now matches the implementation in
+// declaration now matches the implementation in
 // ai_oracle_service.cpp. Oracle funding credentials are loaded internally
 // from tru.conf by writeAIResponseOnChain().
 bool writeAIResponseOnChain(
@@ -24,7 +24,7 @@
     std::string& outTxid                   // filled with txid if broadcast
 );
 
-// FIX(patch22): signed on-chain commitment for a living-token evolution.
+// signed on-chain commitment for a living-token evolution.
 // Full metadata remains in TOKEN_EVOLUTION storage; hashes/provenance are
 // committed to OP_RETURN.
 bool writeTokenEvolutionOnChain(
@@ -59,10 +59,10 @@
     std::shared_ptr<IAIProvider> getProviderForUser(const std::string& userAddress);
     void processAIRequest(const std::string& requestID);
 
-    // FIX(patch22): retryable queue -> signed evolution anchor processor.
+    // retryable queue -> signed evolution anchor processor.
     void processTokenEvolutionAnchorQueue();
 
-    // FIX(patch19): provider-agnostic manual SFT/NCFT evolution preview.
+    // provider-agnostic manual SFT/NCFT evolution preview.
     // Produces/persists a TRU_TOKEN_EVOLVE_V1 preview only.
     // It does NOT modify balances/ownership/supply and does NOT broadcast.
     nlohmann::json evolveTokenManually(
@@ -97,7 +97,7 @@
                                    const nlohmann::json& response,
                                    const std::string& tokenID,
                                    const std::string& providerUsed);
-    // Patch 18: legacy OP_DATAFEED-based response transaction builder removed.
+    // legacy OP_DATAFEED-based response transaction builder removed.
     std::vector<std::string> scanForRequests();
 };
 
--- a/src/ai_providers.cpp
+++ b/src/ai_providers.cpp
@@ -43,7 +43,7 @@
 }
 
 // ---------------------------------------------------------------------------
-// FIX(patch14): shared HTTPS JSON POST transport for cloud AI providers.
+// shared HTTPS JSON POST transport for cloud AI providers.
 // Keeps API keys out of logs and returns structured, parseable errors.
 // ---------------------------------------------------------------------------
 static size_t AIHttpWriteCallback(void* contents, size_t size, size_t nmemb, void* userp) {
@@ -207,7 +207,7 @@
         configured = true;
     }
     
-    // FIX(patch16b): oracle credentials are loaded internally by
+    // oracle credentials are loaded internally by
     // writeAIResponseOnChain() from tru.conf, so this helper no longer
     // carries oracleWIF/oracleAddress parameters.
     nlohmann::json sendRequestAndWrite(
@@ -253,7 +253,7 @@
     }
 
     nlohmann::json sendRequest(const nlohmann::json& request) override {
-        // FIX(patch18): optional environment authentication for local Oobabooga.
+        // optional environment authentication for local Oobabooga.
         if (apiKey.empty()) {
             if (const char* envKey = std::getenv("OOBABOOGA_API_KEY")) {
                 apiKey = envKey;
@@ -317,7 +317,7 @@
 private:
     std::string endpoint = "https://api.openai.com/v1/chat/completions";
     std::string apiKey;
-    // FIX(patch14): current non-reasoning model with Chat Completions support.
+    // current non-reasoning model with Chat Completions support.
     // Can still be overridden through provider configuration.
     std::string model = "gpt-4.1";
     bool configured = false;
@@ -372,7 +372,7 @@
     nlohmann::json performHttpRequest(const std::string& url, 
                                       const std::string& key, 
                                       const nlohmann::json& payload) {
-        // FIX(patch14): real HTTPS JSON transport.
+        // real HTTPS JSON transport.
         return performJsonPost(url, key, payload);
     }
 };
@@ -480,7 +480,7 @@
     }
 };
 
-// FIX(patch17): First-class Nemotron harness provider.
+// First-class Nemotron harness provider.
 //
 // The local Nemotron server exposes an OpenAI-compatible
 // /v1/chat/completions endpoint. Bearer authentication is optional for
@@ -502,7 +502,7 @@
     }
 
     void configure(const nlohmann::json& config) override {
-        // FIX(patch21): endpoint priority:
+        // endpoint priority:
         // explicit provider config > NEMOTRON_ENDPOINT > default 5051.
         if (config.contains("endpoint") &&
             config["endpoint"].is_string() &&
@@ -535,7 +535,7 @@
     }
 
     nlohmann::json sendRequest(const nlohmann::json& request) override {
-        // FIX(patch21): process-level endpoint override.
+        // process-level endpoint override.
         if (const char* envEndpoint = std::getenv("NEMOTRON_ENDPOINT")) {
             if (*envEndpoint != '\0') {
                 endpoint = envEndpoint;
@@ -623,7 +623,7 @@
     nlohmann::json performHttpRequest(const std::string& url, 
                                       const std::string& key, 
                                       const nlohmann::json& payload) {
-        // FIX(patch17): real local JSON transport.
+        // real local JSON transport.
         return performJsonPost(url, key, payload);
     }
 };
@@ -633,7 +633,7 @@
 private:
     std::string endpoint = "https://api.x.ai/v1/chat/completions";
     std::string apiKey;
-    // FIX(patch14): current xAI Chat Completions model.
+    // current xAI Chat Completions model.
     // Can still be overridden through provider configuration.
     std::string model = "grok-4.5";
     bool configured = false;
@@ -687,13 +687,13 @@
     nlohmann::json performHttpRequest(const std::string& url, 
                                       const std::string& key, 
                                       const nlohmann::json& payload) {
-        // FIX(patch14): xAI uses Bearer auth and an OpenAI-compatible
+        // xAI uses Bearer auth and an OpenAI-compatible
         // Chat Completions request/response shape.
         return performJsonPost(url, key, payload);
     }
 };
 
-// FIX(patch18): Google Gemini provider.
+// Google Gemini provider.
 //
 // Uses Gemini's native generateContent REST API. The API key is sent in the
 // x-goog-api-key header and is never placed in the request URL.
@@ -900,7 +900,7 @@
     nlohmann::json performHttpRequest(const std::string& url, 
                                       const std::string& key, 
                                       const nlohmann::json& payload) {
-        // FIX(patch17): real JSON transport with configurable authentication.
+        // real JSON transport with configurable authentication.
         std::vector<std::string> headers;
 
         if (!key.empty() && !authHeader.empty()) {
--- a/src/animated_background.h
+++ b/src/animated_background.h
@@ -1,4 +1,4 @@
-// Add this new file: animated_background.h
+// Animated terminal background.
 #ifndef ANIMATED_BACKGROUND_H
 #define ANIMATED_BACKGROUND_H
 
--- a/src/block.cpp
+++ b/src/block.cpp
@@ -16,9 +16,9 @@
 {
 #ifdef HAS_ZKPROOF
     if(tx.isCoinbase) {
-        // coinbase with a ZK? Just a stub, or real logic
-    }
-    // If you had "tx.isZKProof" => do real checks
+        // Placeholder: coinbase zero-knowledge verification is not implemented.
+    }
+    // Placeholder: this function does not verify a zero-knowledge proof.
     return true;
 #else
     return true; // default stub
@@ -140,7 +140,7 @@
         Logger::log("[Block::fromProto] Loaded token metadata");
     }
 
-    // TRU Security Patch 09 v4:
+    //
     // chainWork is not committed by the block hash and is NOT authoritative.
     // Cumulative work lives only in Blockchain::BlockIndexEntry::chainWork.
     // Never accept a peer-supplied chain_work value.
@@ -428,7 +428,7 @@
     block.header.bits = safeStoul(data.substr(delimPositions[3] + 1, delimPositions[4] - delimPositions[3] - 1), "bits");
     block.header.nonce = safeStoul(data.substr(delimPositions[4] + 1, delimPositions[5] - delimPositions[4] - 1), "nonce");
     block.blockHash = data.substr(delimPositions[5] + 1, delimPositions[6] - delimPositions[5] - 1);
-    // TRU Security Patch 09 v4:
+    //
     // Consume the legacy serialized field for format compatibility, but do not
     // trust/store it in memory. Authoritative cumulative work is recomputed into
     // BlockIndexEntry::chainWork by Blockchain.
@@ -574,7 +574,7 @@
     
     tokenMetadata.clear();
 
-    // FIX(patch26): Living-token evolution anchors are normal OP_RETURN
+    // Living-token evolution anchors are normal OP_RETURN
     // transactions, not token issuance/TRUScript payloads. Recognize the
     // first pushed chunk "TRU_EVOLVE_V1" so metadata extraction leaves the
     // anchor transaction untouched for normal mining/confirmation.
@@ -950,7 +950,7 @@
                 {
                     if (isTRUEvolutionAnchor(out.scriptPubKey))
                     {
-                        // FIX(patch26): evolution proof, not a TRUScript transfer.
+                        // evolution proof, not a TRUScript transfer.
                         continue;
                     }
 
--- a/src/block.h
+++ b/src/block.h
@@ -80,7 +80,7 @@
 public:
     BlockHeader header;
     std::string blockHash;               ///< final puzzle solution
-    // TRU Security Patch 09 v4: legacy wire/disk field only. It is not
+    // legacy wire/disk field only. It is not
     // committed by the block hash and MUST NOT be used for fork selection.
     // Authoritative cumulative work lives in BlockIndexEntry::chainWork.
     uint64_t    chainWork;
--- a/src/blockchain.cpp
+++ b/src/blockchain.cpp
@@ -1,5 +1,5 @@
 #include "blockchain.h"
-#include "tru_limits.h"  // FIX(patch47): shared block/network size limits
+#include "tru_limits.h"  // shared block/network size limits
 #include "block.h"
 #include "tx.h"
 #include "sha256_21e8.h"
@@ -32,9 +32,9 @@
 #include "address_helpers.h"
 #include "wallet.h"
 #include "logging.h"
-#include "rpc_utils.h"  // Patch 05: authenticated local RPC
+#include "rpc_utils.h"  // authenticated local RPC
 #include <filesystem>
-#include <unistd.h>  // Patch 08B.3b: PID-scoped validation sandbox names
+#include <unistd.h>  // PID-scoped validation sandbox names
 #include "mempool.h"
 #include <cctype>
 #include <mutex>
@@ -51,15 +51,15 @@
 #include "crypto_ecdsa.h"
 #include <optional>
 #include <memory>
-#include <limits>  // TRU Security Patch 08A v4: safe candidate-height narrowing
+#include <limits>  // safe candidate-height narrowing
 #include "message_handler.h"
 #include <stdexcept>
 #include <shared_mutex>
 #include "contract_storage.h"
 #include "tru_network_params.h"
-#include "script_context_builder.h"  // Patch 14B: shared execution context
+#include "script_context_builder.h"  // shared execution context
 #include "contract_call_policy.h"     // Patch 14D2/14D3B: state-anchor call policy
-#include "contract_state_runtime.h"    // Patch 14D3B: confirmed state snapshot + scratch execution
+#include "contract_state_runtime.h"    // confirmed state snapshot + scratch execution
 
 httplib::Server g_explorerServer;
 
@@ -77,27 +77,27 @@
 //==================================================================================
 //              BLOCK SIZE SETTINGS
 //==================================================================================
-// FIX(patch47): limits live in tru_limits.h so consensus validation, mining,
+// limits live in tru_limits.h so consensus validation, mining,
 // RPC block templates and P2P transport cannot silently drift apart.
 static constexpr size_t MAX_BLOCK_BYTES = tru_limits::MAX_BLOCK_BYTES;
-// TRU Security Patch 08A v4: hard bounds for attacker-supplied side-chain data.
+// hard bounds for attacker-supplied side-chain data.
 static constexpr int MAX_SIDE_FORK_DEPTH = 100;
 static constexpr int MAX_SIDE_BRANCH_LENGTH = 100;
 static constexpr size_t MAX_SIDE_CHILDREN_PER_PARENT = 4;
 static constexpr size_t MAX_INDEXED_SIDE_BLOCKS = 256;
 static constexpr size_t MAX_SIDE_POOL_BYTES = 256ULL * 1024ULL * 1024ULL;
 
-// Patch 08B.4D.1 v1: runtime stale-side pruning is deliberately bounded.
+// runtime stale-side pruning is deliberately bounded.
 // Objective aging cleanup only; work-aware eviction belongs to 08B.4D.2.
 static constexpr size_t MAX_RUNTIME_SIDE_PRUNES_PER_PASS = 16;
 
-// Patch 08B.4A v2: journal retention and maximum supported fork depth are a
+// journal retention and maximum supported fork depth are a
 // coupled invariant. No pruning exists yet, but the future pruning patch MUST
 // consume this derived constant rather than invent an independent depth.
 static constexpr int UNDO_JOURNAL_RETENTION_MARGIN = MAX_SIDE_FORK_DEPTH;
 static constexpr int UNDO_JOURNAL_RETENTION_DEPTH =
     MAX_SIDE_FORK_DEPTH + UNDO_JOURNAL_RETENTION_MARGIN;
-// Patch 08B.4D.3 v1b: hard bounded active-U4 maintenance budgets.
+// hard bounded active-U4 maintenance budgets.
 static constexpr size_t MAX_UNDO_PRUNE_HEIGHT_PROBES_PER_INVOCATION = 32;
 static constexpr size_t MAX_UNDO_PRUNE_DELETE_PER_INVOCATION = 8;
 static constexpr const char* UNDO_PRUNE_RULES_VERSION = "P08B4D3-U1B";
@@ -112,7 +112,7 @@
 static constexpr int CANDIDATE_VALIDATION_ANCESTRY_LOOKBACK =
     DIFFICULTY_RETARGET_INTERVAL + CANDIDATE_VALIDATION_ANCESTRY_MARGIN;
 
-// Patch 08B.3b: keep disposable candidate databases beside the configured
+// keep disposable candidate databases beside the configured
 // datadir (same filesystem capacity characteristics), not blindly under /tmp.
 static std::filesystem::path candidateSandboxRootForDb(const std::string& dbPath) {
     std::filesystem::path livePath = std::filesystem::path(dbPath).lexically_normal();
@@ -148,14 +148,14 @@
 static std::atomic<uint64_t> g_candidateSandboxSequence{0};
 
 #ifdef TRU_08B3T_TEST_HOOKS
-// Patch 08B.3c: these local runtime-test gates exist only in explicitly opted-in
+// these local runtime-test gates exist only in explicitly opted-in
 // development builds. No RPC path references these hooks.
 static constexpr const char* PATCH08B3T_ENABLE_ENV = "TRU_ENABLE_08B3T_TEST_HOOKS";
 static constexpr const char* PATCH08B3T_ENABLE_MAGIC = "I_UNDERSTAND_08B3T_IS_LOCAL_TEST_CODE";
 static constexpr const char* PATCH08B3T_MUTATE_ENV = "TRU_ENABLE_08B3T_MUTATING_TESTS";
 static constexpr const char* PATCH08B3T_MUTATE_MAGIC = "I_ACCEPT_SINGLE_TIP_DISCONNECT_REAPPLY";
 
-// Patch 08B.4D.2T v1: extra DEV-only mutation gate.
+// extra DEV-only mutation gate.
 // These values exist only in TRU_08B3T_TEST_HOOKS builds.
 static constexpr const char* PATCH08B4D2T_ENV =
     "TRU_ENABLE_08B4D2T_MUTATING_TESTS";
@@ -170,13 +170,13 @@
     return value != nullptr &&
            std::string(value) == PATCH08B4D2T_MAGIC;
 }
-// Patch 08B.4B.1T v1: deterministic local fail-stop injection. Compiled only
+// deterministic local fail-stop injection. Compiled only
 // into explicit TRU_08B3T_TEST_HOOKS development builds. The executor still
 // requires the normal runtime + mutation gates and zero peers/miners.
 static constexpr const char* PATCH08B4B_FAILSTOP_ENV = "TRU_08B4B_FAILSTOP_POINT";
 static constexpr const char* PATCH08B4B_FAIL_AFTER_DISCONNECT_1 = "AFTER_DISCONNECT_1";
 static constexpr const char* PATCH08B4B_FAIL_AFTER_CONNECT_1 = "AFTER_CONNECT_1";
-// Patch 08B.4B.1T v2: deterministic HARD-CRASH witness for the two-stage
+// deterministic HARD-CRASH witness for the two-stage
 // candidate connect gap. This point fires only after applyBlock() has durably
 // committed candidate state/U4 + side-marker deletion, but BEFORE active-tip
 // metadata + CONNECT_PUBLISHED are published by saveChainState().
@@ -211,7 +211,7 @@
 static constexpr const char* SIDE_RULES_VERSION = "P12C3B-I256";
 static constexpr const char* SIDE_LOCAL_SOURCE = "local";
 
-// Patch 08B.4D.9A: production activation gate definition ONLY. The exact
+// production activation gate definition ONLY. The exact
 // magic is intentionally awkward and OFF by default. 9A does not wire this
 // gate to submitBlockInternal(), side admission, PREPARED, or execution.
 static constexpr const char* PATCH08B4D9_AUTO_REORG_ENV =
@@ -225,7 +225,7 @@
            std::string(value) == PATCH08B4D9_AUTO_REORG_MAGIC;
 }
 
-// TRU Security Patch 08B.1 v4:
+//
 // Persist exact LevelDB pre-images in the same synchronous WriteBatch as each
 // accepted non-genesis block. Reorganization remains disabled until later 08B
 // stages validate and consume these journals.
@@ -305,7 +305,7 @@
 static constexpr uint32_t CR01B_ANCHOR_SPENT_VOUT = 0;
 static constexpr uint32_t CR01B_ANCHOR_CHANGE_VOUT = 1;
 
-// TRU Security Patch 08B.4A: durable reorganization PLAN/PREFLIGHT state.
+// durable reorganization PLAN/PREFLIGHT state.
 // IMPORTANT: this patch does not contain a multi-block executor and cannot
 // activate an alternate branch. The only durable phase it can write is
 // PREPARED, which by definition means active chain state has not been touched.
@@ -313,7 +313,7 @@
 static constexpr const char* REORG_PENDING_KEY = "reorg:pending";
 static constexpr const char* REORG_PHASE_PREPARED = "PREPARED";
 
-// Patch 08B.4D.4 v1b: positive candidate-validation cache.
+// positive candidate-validation cache.
 // This is strictly a performance cache. It is memory-only and never changes
 // consensus, durable state, or automatic reorg policy.
 static constexpr const char* CANDIDATE_VALIDATION_CACHE_RULES_VERSION =
@@ -323,7 +323,7 @@
     64ULL * 1024ULL;
 
 
-// Patch 08B.4D.5 v1b: immutable candidate-reject cache.
+// immutable candidate-reject cache.
 // IMPORTANT: generic 4A/08B.3 `false` results are NEVER cached.
 static constexpr const char* IMMUTABLE_NEGATIVE_CACHE_RULES_VERSION =
     "P08B4D5-N1B";
@@ -332,7 +332,7 @@
     64ULL * 1024ULL;
 
 
-// Patch 08B.4D.6 v1: per-source expensive candidate-sandbox budget.
+// per-source expensive candidate-sandbox budget.
 // This is PROCESS-LOCAL node policy, not consensus state.
 static constexpr const char* CANDIDATE_VALIDATION_BUDGET_RULES_VERSION =
     "P08B4D6-B1";
@@ -341,7 +341,7 @@
 static constexpr int64_t CANDIDATE_VALIDATION_BUDGET_IDLE_EXPIRY_SECONDS = 15 * 60;
 static constexpr size_t MAX_CANDIDATE_VALIDATION_BUDGET_SOURCES = 1024;
 
-// Patch 08B.4D.7 v1: aggregate expensive-sandbox budget across ALL untrusted
+// aggregate expensive-sandbox budget across ALL untrusted
 // candidate sources. 4D.6 remains the per-source limiter; this closes source
 // churn / Sybil-style aggregate bypass without changing consensus validity.
 static constexpr const char* CANDIDATE_VALIDATION_GLOBAL_BUDGET_RULES_VERSION =
@@ -350,7 +350,7 @@
 static constexpr int64_t CANDIDATE_VALIDATION_GLOBAL_BUDGET_REFILL_SECONDS = 15;
 
 
-// Patch 08B.4B.1 v2: execution markers are deliberately a different rules
+// execution markers are deliberately a different rules
 // version from the harmless PREPARED marker. 08B.4C will add deterministic
 // restart recovery for these phases. Until then, seeing one at startup is a
 // hard fail-closed condition. CONNECT_PUBLISHED means the candidate state batch
@@ -719,7 +719,7 @@
                 throw std::runtime_error("delete entry unexpectedly has postRawSha256");
             }
 
-            // Patch 08B.1 v4: a valid journal proves what was atomically written
+            // a valid journal proves what was atomically written
             // at commit time, but recovery may skip validation/apply ONLY if the
             // live DB still exactly matches that journaled POST-state. Also
             // recognize an exact PRE-state so a stale journal can be replaced
@@ -1112,7 +1112,7 @@
 }
 
 
-// TRU Security Patch 08B.2: authenticated U4 journal reader for a single
+// authenticated U4 journal reader for a single
 // active-tip disconnect. This reader never mutates state; it requires the
 // current database to match the journal's exact POST-state before exposing
 // PRE-images to rollbackBlock(). Multi-block reorg remains disabled.
@@ -1934,11 +1934,11 @@
 }
 static constexpr size_t BLOCK_ASSEMBLY_BYTES = tru_limits::BLOCK_ASSEMBLY_BYTES;
 // FIX(overflow): max spendable value guard. 21,000,000 * 1e8 TRU atoms = TRU monetary cap.
-static constexpr uint64_t MAX_MONEY = tru_limits::MAX_MONEY;  // TRU Security Patch 10: shared invariant
+static constexpr uint64_t MAX_MONEY = tru_limits::MAX_MONEY;  // shared invariant
 static const uint64_t MAX_TARGET_64BIT = 0x00000000FFFF0000ULL;  // legacy telemetry only
 static constexpr uint32_t TRU_POW_LIMIT_BITS = 0x1e00ffffU;
 
-// TRU Security Patch 09: strict compact-target + chainwork helpers.
+// strict compact-target + chainwork helpers.
 static bool compactToTarget256Strict(uint32_t bits, ChainWork256& targetOut);
 static uint32_t target256ToCompact(const ChainWork256& target);
 static const ChainWork256& truPowLimitTarget256();
@@ -1949,7 +1949,7 @@
     ChainWork256& out);
 static void target256ToLE32(const ChainWork256& target, unsigned char out[32]);
 
-// TRU Security Patch 11: coinbase height commitment.
+// coinbase height commitment.
 // Accepted current miner encodings:
 //   height:<H>|...
 //   GPUHeight:<H>
@@ -1996,7 +1996,7 @@
     return false;
 }
 
-// TRU Security Patch 12A: cheap structural transaction limits before BIP30
+// cheap structural transaction limits before BIP30
 // lookups, txid hashing, or script execution. `blockInputs`/`blockOutputs` are
 // cumulative for the candidate block.
 static bool validateTxResourceShape(
@@ -2059,7 +2059,7 @@
 
     }
 
-    // Patch 15A — transaction-level activated stateful creation validation.
+    // transaction-level activated stateful creation validation.
     // Canonical f751 anchors are classified by their strict TRUSTATE schema:
     // one-entry K/V V1 or canonical Voting V1. Other families remain closed.
     const auto stateCreation =
@@ -2228,7 +2228,7 @@
 
     Logger::log("[Blockchain] Starting shutdown sequence...");
 
-    // Patch 08B.4A.1a v2: shutdown is a lifetime barrier, not a best-effort
+    // shutdown is a lifetime barrier, not a best-effort
     // timeout. No Blockchain-owned worker may outlive stop().
     running_.store(false, std::memory_order_release);
     stopProcessing.store(true, std::memory_order_release);
@@ -2670,7 +2670,7 @@
 void Blockchain::rebuildChainState() {
     Logger::log("[rebuildChainState] Rebuilding chain state...");
 
-    // TRU Security Patch 06:
+    //
     // Serialize recovery with normal block submission. reloadBlocks() owns mtx
     // internally; pre-locking mtx here would self-deadlock shared_mutex.
     std::lock_guard<std::mutex> submissionLock(miningSubmitMutex);
@@ -2933,7 +2933,7 @@
     const std::string& parentHash,
     int candidateHeight) const {
 
-    // TRU Security Patch 09:
+    //
     // Candidate ancestry remains Patch-07 anchored, but the retarget itself is
     // integer-only over the full 256-bit target. No float enters consensus.
     static constexpr uint64_t DESIRED_TIMESPAN =
@@ -3104,7 +3104,7 @@
     const std::string& parentHash,
     int candidateHeight) const {
 
-    // TRU Security Patch 07:
+    //
     // MTP must follow the candidate's parent links, not chain[] by height.
     // Preserve the existing early-chain behavior (MTP disabled through a
     // parent height of 11), then use the immediate parent plus its 10 ancestors.
@@ -3190,7 +3190,7 @@
         return false;
     }
 
-    // TRU Security Patch 12A: enforce cheap byte/count limits before expensive
+    // enforce cheap byte/count limits before expensive
     // txid recomputation, BIP30 UTXO lookups, Merkle work, or script execution.
     size_t serializedBlockSize = 0;
     try {
@@ -3296,7 +3296,7 @@
         return false;
     }
 
-    // TRU Security Patch 03:
+    //
     // Never trust a transaction ID supplied by a peer or deserializer.  The
     // Merkle root below is built from tx.txid, so every claimed ID must first
     // be proven to match TRU's canonical computeTxId() result for the actual
@@ -3383,7 +3383,7 @@
         return false;
     }
 
-    // TRU Security Patch 11: every non-genesis coinbase must commit to the
+    // every non-genesis coinbase must commit to the
     // candidate block height in its scriptSig. Coinbase scriptSig IS included
     // in TRU's coinbase txid, so this commitment is merkle-covered today.
     if (block.height > 1) {
@@ -3398,7 +3398,7 @@
             return false;
         }
     }
-    // FIX(patch48): the coinbase amount is NOT validated here any more.
+    // the coinbase amount is NOT validated here any more.
     //
     // The old test was:
     //     if (block.transactions[0].vout[0].amount > calculateSubsidy(height))
@@ -3452,7 +3452,7 @@
         uint64_t txGasUsed = 0;
         bool sawStatefulCallInput = false;
 
-        // Patch 14D3A2 — retain creation classification while validating
+        // retain creation classification while validating
         // inputs so consensus can bind the new root to its sole P2PKH creator.
         const auto stateCreation =
             tru_contract_call::ValidateStatefulCreationShape(tx);
@@ -3503,7 +3503,7 @@
                 }
             }
 
-            // TRU Security Patch 02:
+            //
             // Consensus monetary arithmetic must never rely on uint64_t wraparound.
             // Bound every referenced UTXO and accumulate the input value with an
             // explicit MAX_MONEY check.
@@ -3519,7 +3519,7 @@
             }
             inSum += utxo.amount;
 
-            // Script verification (your existing code)
+            // Script verification.
             std::vector<unsigned char> scriptPubKeyBytes;
             try {
                 scriptPubKeyBytes = hexDecode(utxo.scriptPubKey);
@@ -3536,7 +3536,7 @@
                 return false;
             }
 
-            // TRU Smart Contract Patch 13A — leading OP_RETURN outputs are unspendable.
+            // leading OP_RETURN outputs are unspendable.
             // Data-carrier outputs must never be made spendable by attacker-controlled
             // scriptSig stack contents or by OP_RETURN's interpreter termination behavior.
             // This rule is based only on the locking-script prefix and therefore applies
@@ -3557,7 +3557,7 @@
                 return false;
             }
 
-            // TRU Smart Contract Patch 14D3B — owner-authorized Stateful K/V
+            // owner-authorized Stateful K/V
             // continuation execution. State is loaded from the stable confirmed
             // root and executed on a disposable scratch copy during validation.
             const auto statefulScan =
@@ -3725,7 +3725,7 @@
                 tru_limits::MAX_BLOCK_SCRIPT_GAS - blockGasUsed});
             if (inputGasLimit == 0) return false;
 
-            // TRU Smart Contract Patch 14B — consensus context is built from
+            // consensus context is built from
             // explicit block/input data. OP_BLOCKTIME now uses the candidate
             // block header timestamp, never the validator node's wall clock.
             const std::vector<unsigned char> inputSighash =
@@ -3740,7 +3740,7 @@
                 return false;
             }
 
-            // TRU Smart Contract Patch 14C — state is scoped by the locking UTXO
+            // state is scoped by the locking UTXO
             // identity, never by owner/caller. Validation works on a local copy;
             // Patch 14D will define atomic persistence/rollback of successful writes.
             std::unordered_map<std::string, std::vector<unsigned char>> scratchState;
@@ -3826,7 +3826,7 @@
             return false;
         }
 
-        // TRU Security Patch 02:
+        //
         // Fee arithmetic is consensus-critical. Keep the per-transaction fee
         // and the running block fee total bounded so neither can wrap.
         const uint64_t txFee = inSum - outSum;
@@ -3844,7 +3844,7 @@
     }
 
     // ---- Step 8b: Enforce coinbase value <= subsidy + totalFees ----
-    // TRU Security Patch 02:
+    //
     // The coinbase transaction was intentionally skipped by the regular
     // transaction-output loop above, so it needs the SAME MAX_MONEY bounds
     // and checked accumulation here. Without this, multiple huge uint64_t
@@ -3898,7 +3898,7 @@
     // any other height an attacker could set trivial `bits` and mine for free.
     if (block.height > 1) {
         try {
-            // TRU Security Patch 07: difficulty is anchored to THIS block's parent.
+            // difficulty is anchored to THIS block's parent.
             std::string expectedBits =
                 calculateExpectedBits(block.header.prevHash, block.height);
             uint32_t exp = std::stoul(expectedBits, nullptr, 16);
@@ -3984,7 +3984,7 @@
     std::shared_lock<std::shared_mutex> lock(mtx);
     uint64_t totalSize = 0;
     for (const auto& block : chain) {
-        totalSize += block.serialize().size(); // FIX(patch47): serialize() is already bytes
+        totalSize += block.serialize().size(); // serialize() is already bytes
     }
     Logger::log("[getBlockchainSize] Calculated blockchain size: " + std::to_string(totalSize) + " bytes");
     return totalSize;
@@ -3999,7 +3999,7 @@
         Logger::log("[getBlockSize] Block not found: " + blockHash);
         return 0;
     }
-    uint64_t size = it->second.block.serialize().size(); // FIX(patch47): serialize() is already bytes
+    uint64_t size = it->second.block.serialize().size(); // serialize() is already bytes
     Logger::log("[getBlockSize] Block " + blockHash + " size: " + std::to_string(size) + " bytes");
     return size;
 }
@@ -4144,7 +4144,7 @@
         return 0.0;
     }
 
-    // FIXED: Calculate reported hash rate from active miners with longer timeout
+    // Calculate reported hash rate from active miners with longer timeout
     double reportedHashrate = 0.0;
     auto now = std::chrono::steady_clock::now();
     
@@ -4154,7 +4154,7 @@
         auto it = minerLastActivity.find(address);
         if (it != minerLastActivity.end()) {
             auto timeSinceActivity = now - it->second;
-            // FIXED: Increased timeout from 1 to 3 minutes for more stable reporting
+            // Increased timeout from 1 to 3 minutes for more stable reporting
             if (timeSinceActivity < std::chrono::minutes(2)) {
                 reportedHashrate += rate;
                 Logger::log("[getNetworkHashrate] Active miner " + address + 
@@ -4195,7 +4195,7 @@
             {
                 double avgBlockTime = totalTime / totalBlocks;
 
-                // FIXED: More accurate hashrate calculation
+                // More accurate hashrate calculation
                 // Instead of using raw difficulty, calculate based on expected hashes
                 // For SHA-256, expected hashes = 2^256 / target
                 uint32_t bits = recentBlocks.back().header.bits;
@@ -4216,7 +4216,7 @@
         }
     }
 
-    // FIXED: Improved priority logic
+    // Improved priority logic
     double finalHashrate = 0.0;
     
     if (reportedHashrate > 0) {
@@ -4229,11 +4229,11 @@
         if (blockchainHashrate > 0) {
             double ratio = reportedHashrate / blockchainHashrate;
             
-            // FIXED: More reasonable ratio checking (factor of 5 instead of 10)
+            // More reasonable ratio checking (factor of 5 instead of 10)
             if (ratio > 5.0 || ratio < 0.2) {
                 Logger::log("[getNetworkHashrate] Large discrepancy detected - ratio: " + std::to_string(ratio));
                 
-                // FIXED: Weight reported hashrate more heavily since it's real-time
+                // Weight reported hashrate more heavily since it's real-time
                 finalHashrate = (reportedHashrate * 0.8) + (blockchainHashrate * 0.2);
                 
                 Logger::log("[getNetworkHashrate] Using weighted average: " + std::to_string(finalHashrate) + " H/s");
@@ -4249,7 +4249,7 @@
         Logger::log("[getNetworkHashrate] No hashrate data available");
     }
     
-    // FIXED: Add minimum threshold to avoid tiny values
+    // Add minimum threshold to avoid tiny values
     if (finalHashrate < 100.0) { // 1 MH/s minimum
         Logger::log("[getNetworkHashrate] Hashrate below minimum threshold, using blockchain calculation");
         finalHashrate = std::max(blockchainHashrate, 100.0);
@@ -4298,7 +4298,7 @@
     if (timeTaken > 0.001 && hashesTried > 0) {
         hashRate = static_cast<double>(hashesTried) / timeTaken;
         
-        // FIXED: Increase cap for modern GPU miners (10 TH/s)
+        // Increase cap for modern GPU miners (10 TH/s)
         const double MAX_REASONABLE_HASHRATE = 1e13; // 10 TH/s
         if (hashRate > MAX_REASONABLE_HASHRATE) {
             Logger::log("[updateMinerActivity] WARNING: Unreasonable hashrate " + std::to_string(hashRate) + 
@@ -4306,7 +4306,7 @@
             hashRate = MAX_REASONABLE_HASHRATE;
         }
         
-        // FIXED: More conservative moving average (85% new, 15% old)
+        // More conservative moving average (85% new, 15% old)
         if (minerHashRates.find(minerAddress) != minerHashRates.end()) {
             double previousRate = minerHashRates[minerAddress];
             // Only smooth if previous rate exists and is reasonable
@@ -4322,7 +4322,7 @@
                    ": " + std::to_string(hashRate) + " H/s (" + std::to_string(hashesTried) + 
                    " hashes in " + std::to_string(timeTaken) + "s)");
     } else {
-        // FIXED: Don't reset hash rate to 0 immediately, just update timestamp
+        // Don't reset hash rate to 0 immediately, just update timestamp
         minerLastActivity[minerAddress] = std::chrono::steady_clock::now();
         
         // Only reset hash rate if it's been several reports with zero hashes
@@ -4367,7 +4367,7 @@
     std::unique_lock<std::shared_mutex> lock(mtx);
     
     auto now = std::chrono::steady_clock::now();
-    // FIXED: Increased timeout to match getNetworkHashrate (3 minutes + 1 minute buffer)
+    // Increased timeout to match getNetworkHashrate (3 minutes + 1 minute buffer)
     auto inactiveThreshold = std::chrono::minutes(5);
     
     std::vector<std::string> minersToRemove;
@@ -4428,7 +4428,7 @@
     std::string& hashOut,
     int& heightOut) const {
 
-    // TRU Security Patch 07:
+    //
     // Template/candidate builders need hash+height from the SAME chain view.
     std::shared_lock<std::shared_mutex> lock(mtx);
     hashOut = bestTipHash;
@@ -4687,7 +4687,7 @@
     std::unordered_set<std::string> addresses;
     try {
         Logger::log("[getActiveAddresses] Starting iteration over address keys");
-        // FIX(patch49): same key-parsing bug as the explorer's startup cache.
+        // same key-parsing bug as the explorer's startup cache.
         // The old code assumed iteratePrefix leaves the "address:" prefix on
         // the key and hard-coded offset 8, then cut at the SECOND colon. In
         // practice iteratePrefix strips the prefix, so offset 8 removed eight
@@ -4704,7 +4704,7 @@
                 rest = rest.substr(strlen("address:"));
             }
 
-            // Patch 21: address indexes historically may contain contract
+            // address indexes historically may contain contract
             // outpoint identifiers. Detect the complete txid:vout prefix before
             // slicing at ':' so Base58 validation never sees contract identity.
             const size_t firstColon = rest.find(':');
@@ -4917,7 +4917,7 @@
     );
 }
 
-// Patch 21: exact display-only scaling without integer overflow.
+// exact display-only scaling without integer overflow.
 // exchangeRate is token units per TRU atom; one TRU is 100,000,000 atoms.
 static inline std::string tokenUnitsPerTRUText(uint64_t exchangeRate) {
     if (exchangeRate == 0) return "0";
@@ -4927,7 +4927,7 @@
 //================================================================================
 //                      GET CONTRACT
 //================================================================================
-// Patch 21: read-only registry view used by Contract Vault classification.
+// read-only registry view used by Contract Vault classification.
 // A historical lineage outpoint may no longer be the live anchor, so resolve
 // lineage -> root -> live first, then ask the already-hardened confirmed-state
 // loader to validate the CURRENT live anchor and owner/family/state records.
@@ -5007,7 +5007,7 @@
     Logger::log("[getContracts] Starting contract retrieval");
     nlohmann::json contracts = nlohmann::json::array();
 
-    // Patch 20 v2: Contract Vault maturity uses the same parent-chain MTP
+    // Contract Vault maturity uses the same parent-chain MTP
     // clock as CLTV, never local wall time. Snapshot hash+height atomically.
     uint32_t activeMedianTimePast = 0;
     try {
@@ -5280,7 +5280,7 @@
                     " source=" + classificationSource +
                     " for script: " + contractScript);
 
-                // Patch 21: bridge enrichment is classification-preserving.
+                // bridge enrichment is classification-preserving.
                 // The push-aware scanner above decides BRIDGE; payload bytes can
                 // no longer overwrite Time/Hash/Oracle/registry-backed identity.
                 if (contractType == "BRIDGE") {
@@ -5520,7 +5520,7 @@
         if (key.size() <= 9) return;
         std::string rest = key.substr(9);
 
-        // Patch 21: canonical ContractStorage keys are
+        // canonical ContractStorage keys are
         //   contract:<txid>:<vout>:<field>
         // so the identity itself contains one colon. Preserve a one-colon
         // fallback for older Base58-address keyed records.
@@ -5690,7 +5690,7 @@
             return false;
         }
 
-        // FIX(patch9): use the ONE canonical PoW verifier (rebuilds header,
+        // use the ONE canonical PoW verifier (rebuilds header,
         // double-SHA256, applies the 21E8 tweak, compares to target) - the same
         // test mining and block acceptance use. The previous hand-rolled check
         // compared the raw stored hash WITHOUT the 21E8 transform, so it wrongly
@@ -5792,7 +5792,7 @@
     Logger::log("[bitsToTargetHelper] exp=" + std::to_string(exp) + 
                " mant=" + std::to_string(mant));
     
-    // FIX(patch8): the old 0x2400ffff special-case forced a max (zero-diff)
+    // the old 0x2400ffff special-case forced a max (zero-diff)
     // target. The new starting difficulty 0x1e00ffff goes through the standard
     // calculation below, so no special-case is needed. Kept as a guard only for
     // any legacy 0x2400ffff blocks that might still be read from disk.
@@ -5833,7 +5833,7 @@
 //
 //================================================================================
 //================================================================================
-//        STRICT COMPACT TARGET / 256-BIT CHAINWORK (PATCH 09)
+// STRICT COMPACT TARGET / 256-BIT CHAINWORK
 //================================================================================
 static uint32_t target256ToCompact(const ChainWork256& target)
 {
@@ -6274,7 +6274,7 @@
       rpcPort(tru_network::MAINNET_RPC_PORT),
       //difficulty(0x1d00ffff),
       //difficulty(0x2400ffff),   // OLD: target > 2^256, zero difficulty (nonce 0)
-      difficulty(0x1e00ffff),     // FIX(patch8): ~16.8M hashes/block, real work; retarget climbs to 60s
+      difficulty(0x1e00ffff),     // ~16.8M hashes/block, real work; retarget climbs to 60s
       blockReward(50ULL * 100000000ULL),
       bestTipHeight(0),
       hashrateHistory(),
@@ -6331,7 +6331,7 @@
                   << " at height: " << bestTipHeight << "\n";
     }
 
-    // Patch 08B.4A: inspect any durable PREPARED reorg intent before worker
+    // inspect any durable PREPARED reorg intent before worker
     // threads begin accepting work. 08B.4A has no executor, so a valid
     // PREPARED record is always an abandoned non-mutating intent and is
     // synchronously cleared. Corrupt/unknown records fail startup closed.
@@ -6541,7 +6541,7 @@
 //                     IS CHAIN BETTER
 //================================================================================
 bool Blockchain::isChainBetter(const std::vector<Block>& newChain, uint64_t newChainWork) const {
-    // TRU Security Patch 09: authoritative work is now uint256 in blockIndex.
+    // authoritative work is now uint256 in blockIndex.
     // This legacy uint64 selector stays disabled until Patch 08B replaces it.
     (void)newChain;
     (void)newChainWork;
@@ -6554,7 +6554,7 @@
 //            SIDE-CHAIN INDEX VALIDATION (NO STATE MUTATION)
 //================================================================================
 //================================================================================
-// PATCH 08B.4D.5 v1b - BOUNDED IMMUTABLE NEGATIVE CANDIDATE CACHE
+// BOUNDED IMMUTABLE NEGATIVE CANDIDATE CACHE
 //================================================================================
 bool Blockchain::immutableNegativeCandidateCacheContains(
     const std::string& fingerprint,
@@ -6659,7 +6659,7 @@
 
 
 bool Blockchain::validateBlockForIndex(const Block& block, const std::string& serializedBlock) const {
-    // TRU Security Patch 08A v4:
+    //
     // Header hash + PoW are repeated here as defense in depth.
     // indexSideChainBlock() already ran them BEFORE side-pool policy/locking.
 
@@ -6707,7 +6707,7 @@
         }
     }
 
-    // Patch 08B.4D.5 v1b:
+    //
     // Build a cache identity from BOTH the exact serialized bytes and the
     // independent state-affecting payload fingerprint. Block::blockHash alone
     // is intentionally insufficient until Patch 17 commits every
@@ -6942,7 +6942,7 @@
 }
 
 //================================================================================
-//       PATCH 08B.4A - DURABLE REORG PLAN + PREFLIGHT (NO EXECUTOR)
+// DURABLE REORG PLAN + PREFLIGHT (NO EXECUTOR)
 //================================================================================
 bool Blockchain::buildReorgPlanLocked(
     const std::string& candidateTipHash,
@@ -7507,7 +7507,7 @@
 
 
 //================================================================================
-//  PATCH 08B.4C.1 - AUTHENTICATED RECOVERY-STATE CLASSIFIER
+// AUTHENTICATED RECOVERY-STATE CLASSIFIER
 //================================================================================
 // Classification only. This code authenticates and reconciles a durable E2
 // execution marker with the reconstructed live index and LevelDB witnesses.
@@ -7959,7 +7959,7 @@
         return true;
     };
 
-    // Patch 08B.4C.1a v1:
+    //
     // CONNECT_STATE_BEFORE_PUBLISH is the narrow two-batch crash window where
     // applyBlock() has already committed candidate state + exact U4 + deletion
     // of sideblock:<candidate>, but saveChainState() has NOT yet published the
@@ -8864,7 +8864,7 @@
 
 
 //================================================================================
-// PATCH 08B.4D.4 v1b - BOUNDED POSITIVE CANDIDATE VALIDATION CACHE
+// BOUNDED POSITIVE CANDIDATE VALIDATION CACHE
 //================================================================================
 bool Blockchain::buildCandidateValidationCacheWitness(
     const ReorgPlan& plan,
@@ -9191,7 +9191,7 @@
 
 
 //================================================================================
-// PATCH 08B.4D.6 v1 - PER-SOURCE EXPENSIVE CANDIDATE-SANDBOX BUDGET
+// PER-SOURCE EXPENSIVE CANDIDATE-SANDBOX BUDGET
 //================================================================================
 bool Blockchain::consumeCandidateValidationBudgetForSource(
     const std::string& sourceKey,
@@ -9523,7 +9523,7 @@
         return false;
     }
 
-    // Patch 08B.4D.4 v1b: build the positive-cache witness only AFTER
+    // build the positive-cache witness only AFTER
     // the normal durable execution-source preflight. A HIT skips only the
     // expensive 08B.3 sandbox. Plan rebuild/equivalence and final durability
     // recheck remain mandatory.
@@ -9566,7 +9566,7 @@
     // keys, so 4A verifies journal envelope/durability above and lets the
     // sandbox authenticate exact state in rewind order.
     //
-    // Patch 08B.4D.6 v1: charge ONLY a real positive-cache MISS that is about
+    // charge ONLY a real positive-cache MISS that is about
     // to execute the expensive sandbox. The candidate source is resolved from
     // authoritative indexed ownership while submission ordering is held; no
     // caller-supplied source override exists here.
@@ -9720,7 +9720,7 @@
 }
 
 //================================================================================
-//  PATCH 08B.4B.1 - CONTROLLED MULTI-BLOCK REORG EXECUTOR (MANUAL ONLY)
+// CONTROLLED MULTI-BLOCK REORG EXECUTOR (MANUAL ONLY)
 //================================================================================
 std::string Blockchain::buildReorgExecutionMarkerPayload(
     const ReorgPlan& plan,
@@ -10136,7 +10136,7 @@
                         durableReason);
         }
 #ifdef TRU_08B3T_TEST_HOOKS
-        // Patch 08B.4B.1T v1: deterministic fail-stop only AFTER the first
+        // deterministic fail-stop only AFTER the first
         // disconnect's state, tip metadata, side marker, and E2 execution marker
         // have all been durably committed and read back successfully.
         if (i == 0 && patch08B4BFailstopAt(PATCH08B4B_FAIL_AFTER_DISCONNECT_1)) {
@@ -10288,7 +10288,7 @@
                         durableReason);
         }
 #ifdef TRU_08B3T_TEST_HOOKS
-        // Patch 08B.4B.1T v1: second deterministic recovery witness. The fault
+        // second deterministic recovery witness. The fault
         // fires only after candidate state + side-marker deletion, tip metadata,
         // and CONNECT_PUBLISHED marker have all committed and read back exactly.
         if (i == 0 && patch08B4BFailstopAt(PATCH08B4B_FAIL_AFTER_CONNECT_1)) {
@@ -10421,7 +10421,7 @@
 
 
 //================================================================================
-//  PATCH 08B.4C.2 v2 - AUTHENTICATED FINISH-FORWARD CRASH RECOVERY
+// AUTHENTICATED FINISH-FORWARD CRASH RECOVERY
 //================================================================================
 bool Blockchain::recoverInterruptedReorgWithSubmissionLockHeld(
     std::string& reasonOut,
@@ -10616,7 +10616,7 @@
         captured.sourceKey = sourceIt->second;
         captured.serializedBytes = serialized.size();
 
-        // Patch 08B.4C.2 v2:
+        //
         // Preflight EVERY in-memory accounting witness before the first
         // decrement/erase. Once mutation begins below there are deliberately
         // no remaining validation branches that can return false.
@@ -11854,7 +11854,7 @@
         return;
     }
 
-    // Patch 08B.4C.1: classify authenticated E2 execution progress BEFORE the
+    // classify authenticated E2 execution progress BEFORE the
     // legacy PREPARED reader. Classification is intentionally read-only and
     // startup still stops after a recoverable state is identified.
     ReorgExecutionRecoveryState execution;
@@ -11989,8 +11989,8 @@
 
 #ifdef TRU_08B3T_TEST_HOOKS
 //================================================================================
-//       PATCH 08B.3T v2 - CONTROLLED LOCAL RUNTIME TEST HOOKS
-//       Patch 08B.3c: compile-time excluded from production builds by default.
+// CONTROLLED LOCAL RUNTIME TEST HOOKS
+// compile-time excluded from production builds by default.
 //================================================================================
 bool Blockchain::debug08B3TTestHooksEnabled() const {
     return !validationOnly_ &&
@@ -12138,7 +12138,7 @@
         std::shared_lock<std::shared_mutex> lock(mtx);
         std::string cursor = afterTip;
         int steps = 0;
-        // Patch 08B.3c: production-like validation only needs to walk the
+        // production-like validation only needs to walk the
         // amount the active chain could have advanced while sandbox replay ran.
         // If height unexpectedly regressed, the two-step bound fails closed.
         const int maxSteps = std::max(0, afterHeight - beforeHeight) + 2;
@@ -12213,7 +12213,7 @@
         originalTip = chain.back();
     }
 
-    // Patch 14D3B1: optional contract-specific witness layered around the
+    // optional contract-specific witness layered around the
     // existing authenticated 08B.3T single-tip disconnect/reapply primitive.
     // This code is already inside TRU_08B3T_TEST_HOOKS and is additionally
     // runtime-gated so production/default execution is unchanged.
@@ -12565,7 +12565,7 @@
 
     uint32_t finalMemoryDifficulty = 0;
     {
-        // Patch 08B.3c: synchronize the final runtime difficulty witness with
+        // synchronize the final runtime difficulty witness with
         // the same chain-state mutex used by active-tip writers.
         std::shared_lock<std::shared_mutex> lock(mtx);
         finalMemoryDifficulty = difficulty;
@@ -12823,7 +12823,7 @@
 
 
 //================================================================================
-//  PATCH 08B.4D.2T v1 - DEV-ONLY CONTROLLED SIDE-PRESSURE FIXTURE
+// DEV-ONLY CONTROLLED SIDE-PRESSURE FIXTURE
 //================================================================================
 bool Blockchain::debug08B4D2TRunFixture(
     const std::string& mode,
@@ -13397,7 +13397,7 @@
 
 
 //================================================================================
-// PATCH 08B.4D.4T v1a - DEV-ONLY POSITIVE-CACHE FIFO CHURN FIXTURE
+// DEV-ONLY POSITIVE-CACHE FIFO CHURN FIXTURE
 //================================================================================
 bool Blockchain::debug08B4D4TPositiveCacheFifo(std::string& report) {
 
@@ -13748,7 +13748,7 @@
 
 
 //================================================================================
-// PATCH 08B.4D.5T v1c - DEV-ONLY IMMUTABLE NEGATIVE-CACHE FIFO CHURN FIXTURE
+// DEV-ONLY IMMUTABLE NEGATIVE-CACHE FIFO CHURN FIXTURE
 //================================================================================
 bool Blockchain::debug08B4D5TNegativeCacheFifo(std::string& report) {
 
@@ -14081,7 +14081,7 @@
 #endif  // TRU_08B3T_TEST_HOOKS
 
 //================================================================================
-//       PATCH 08B.3 v2 - FULL STATEFUL CANDIDATE VALIDATION IN SANDBOX
+// FULL STATEFUL CANDIDATE VALIDATION IN SANDBOX
 //================================================================================
 bool Blockchain::validateCandidateBranchStateful(
     const std::string& candidateTipHash) {
@@ -14538,7 +14538,7 @@
 //================================================================================
 
 //================================================================================
-//  PATCH 08B.4D.2 v1 - WORK-AWARE SIDE-POOL SINGLE-LEAF REPLACEMENT
+// WORK-AWARE SIDE-POOL SINGLE-LEAF REPLACEMENT
 //================================================================================
 bool Blockchain::tryWorkAwareSideReplacementWithSubmissionLockHeld(
     const Block& block,
@@ -15304,7 +15304,7 @@
 bool Blockchain::indexSideChainBlock(
     const Block& block,
     const std::string& sourceKey) {
-    // TRU Security Patch 08A v4:
+    //
     // ATTACKER-COST GATE FIRST. Garbage known-parent packets fail hash/PoW
     // before taking mtx or walking fork policy.
 
@@ -15390,7 +15390,7 @@
             return false;
         }
 
-        // Patch 08B.4D.2 v1: capacity checks are intentionally deferred
+        // capacity checks are intentionally deferred
         // until AFTER validateBlockForIndex(). Invalid input must never
         // evict authenticated side state. Structural fork/depth/branch
         // policy above remains fail-closed here.
@@ -15411,7 +15411,7 @@
         return false;
     }
 
-    // Patch 08B.4D.2 v1: full side validation is complete. Only now may
+    // full side validation is complete. Only now may
     // capacity pressure trigger work-aware replacement. submitBlockInternal()
     // still owns miningSubmitMutex across this helper and the ordinary
     // no-pressure side-admission path.
@@ -15587,12 +15587,12 @@
 }
 
 //================================================================================
-// PATCH 08B.4D.1 v1a — BOUNDED RUNTIME STALE-SIDE PRUNING
+// BOUNDED RUNTIME STALE-SIDE PRUNING
 // Caller MUST already own miningSubmitMutex.
 //================================================================================
 
 // ============================================================================
-// PATCH 08B.4D.3 v1b - ACTIVE-CHAIN U4 RETENTION CORE
+// ACTIVE-CHAIN U4 RETENTION CORE
 // ============================================================================
 // Caller owns miningSubmitMutex.
 // Strict boundary: depth <= 200 RETAIN; depth 201 is first prune-eligible.
@@ -16412,7 +16412,7 @@
     std::vector<SideRecord> records;
     std::vector<std::string> purgeHashes;
 
-    // Patch 08B.4C.0 v1:
+    //
     // An interrupted E2 reorganization can leave durable candidate/old-branch
     // records that ordinary side-index reconstruction considers stale because
     // the published active tip intentionally lags the already-committed state.
@@ -16731,7 +16731,7 @@
     const std::string* reorgTipPublishMarkerPayload,
     const std::string* promotedSideBlockHash,
     bool recoveryPublishOnly) {
-    // TRU Security Patch 06: private confirmed-state connector.
+    // private confirmed-state connector.
     Logger::log("[connectTipBlock] Attempting to connect block: " + blk.blockHash);
 
     if (!validationOnly_ &&
@@ -16919,7 +16919,7 @@
         return false;
     }
 
-    // TRU Security Patch 09: calculate work BEFORE state mutation.
+    // calculate work BEFORE state mutation.
     ChainWork256 nextChainWork;
     try {
         ChainWork256 parentWork = 0;
@@ -17068,7 +17068,7 @@
             std::_Exit(87);
         }
 
-        // Patch 08B.4B.1T v2:
+        //
         // Deliberately simulate a process-killing crash in the exact two-stage
         // CONNECT gap. At this point applyBlock() returned success, therefore:
         //   - candidate state is durable,
@@ -17115,7 +17115,7 @@
     if (newHeight > currentHeight) {
         bestTipHeight.store(newHeight);
         bestTipHash = blk.blockHash;
-        // Patch 08B.3T v2: difficulty is active-tip metadata. Rollback restores
+        // difficulty is active-tip metadata. Rollback restores
         // the parent's bits, so a successful reconnect must restore the new tip's
         // bits before saveChainState() persists metadata. Consensus validation
         // continues to use calculateExpectedBits(parentHash, height).
@@ -17346,7 +17346,7 @@
             {
                 std::string tokenID = metadata["tokenID"].get<std::string>();
 
-                // FIX(patch51): reject a second issuance of the same tokenID
+                // reject a second issuance of the same tokenID
                 // WITHIN this block before consulting LevelDB.
                 //
                 // processedTokenIssuances was declared and inserted into but
@@ -17575,7 +17575,7 @@
     Logger::log("[applyBlock] Phase 2: Processing transactions");
 
     for (const auto& tx : blk.transactions) {
-        // TRU Smart Contract Patch 14B1 — keep even the current applyBlock
+        // keep even the current applyBlock
         // context on the shared construction path. UTXOSet::applyTransaction()
         // does not presently execute scripts or read ctx, but routing it through
         // the builder prevents silent context drift if that changes later.
@@ -17583,7 +17583,7 @@
         applyCtxSpec.gasLimit = 1000000;
         applyCtxSpec.tx = &tx;
         applyCtxSpec.blockTime = blk.header.timestamp;
-        // Patch 20 v2: applyTransaction() does not execute scripts. Leave
+        // applyTransaction() does not execute scripts. Leave
         // medianTimePast at its fail-closed default (0) rather than recomputing
         // MTP while connectTipBlock holds mtx. Any future script execution added
         // to applyTransaction must plumb the already-validated parent MTP first.
@@ -17705,7 +17705,7 @@
         for (const auto& spentKey : result.utxosToDelete) {
             mainBatch.Delete(spentKey);
 
-            // Patch 08B.1: parse canonical "utxo:<txid>:<vout>" once and
+            // parse canonical "utxo:<txid>:<vout>" once and
             // include all current derived indexes caused by this spend in the
             // same journaled state batch.
             static const std::string kUtxoPrefix = "utxo:";
@@ -17834,7 +17834,7 @@
             addressesInTx.insert(std::get<2>(utxo));
         }
         for (const auto& spentKey : result.utxosToDelete) {
-            // Patch 08B.1 v4: use the same canonical outpoint parser as the
+            // use the same canonical outpoint parser as the
             // journaled spent-index cleanup above. The legacy parser looked
             // for a second ':' after vout and therefore skipped normal
             // "utxo:<txid>:<vout>" keys.
@@ -17904,7 +17904,7 @@
             mainBatch.Put(txKey, "1");
         }
 
-        // Patch 08B.1: no pre-commit mempool mutation here.
+        // no pre-commit mempool mutation here.
         // Phase 8 cleans confirmed transactions after durable state success.
     }
 
@@ -17931,7 +17931,7 @@
         }
     }
 
-    // Patch 15A — activated state-root creation persistence for K/V V1 and
+    // activated state-root creation persistence for K/V V1 and
     // canonical Voting V1. These writes enter mainBatch BEFORE the
     // existing undo journal is generated, so crash recovery/disconnect captures
     // exact pre-images for state + lineage + live-anchor records.
@@ -18061,7 +18061,7 @@
             std::to_string(stateCreation.accountedStateBytes));
     }
 
-    // Patch 15A — persist validated K/V V1 or Voting V1 continuation calls.
+    // persist validated K/V V1 or Voting V1 continuation calls.
     // Re-execute against the same confirmed root snapshot used by validation,
     // then queue ONLY the resulting state delta + lineage rollover in mainBatch.
     // No persistent write occurs during VM execution, and every mutation lands
@@ -18671,7 +18671,7 @@
             " final=" + std::to_string(finalAmount));
     }
 
-    // Patch 08B.1 v4: address transaction counts are confirmed-state data.
+    // address transaction counts are confirmed-state data.
     // Fold them into this same batch so rollback can restore their exact
     // pre-block values instead of trying to infer them later.
     std::unordered_map<std::string, int> committedTxCounts;
@@ -18789,7 +18789,7 @@
         if (tru_contract_state::IsCanonicalContractOutpoint(addr)) continue;
         if (!isValidAddress(addr)) continue;
         
-        // FIX(patch49): these two writes to addressCache were unsynchronised
+        // these two writes to addressCache were unsynchronised
         // while the explorer's /api/addresses and /api/address read the same
         // unordered_map under a shared_lock on addressCacheMutex. Concurrent
         // write-during-read on an unordered_map is undefined behaviour and can
@@ -19127,7 +19127,7 @@
     }
 }
 //================================================================================
-//  PATCH 08B.4D.9A - PRODUCTION ACTIVATION GATE + FAIL-STOP PLUMBING ONLY
+// PRODUCTION ACTIVATION GATE + FAIL-STOP PLUMBING ONLY
 //================================================================================
 bool Blockchain::productionAutomaticReorgGateEnabled() const {
     // Validation sandboxes can never arm production activation semantics.
@@ -19183,7 +19183,7 @@
 }
 
 //================================================================================
-//  PATCH 08B.4D.9B - INCOMING STRICT-WINNING SIDE-TIP ACTIVATION ONLY
+// INCOMING STRICT-WINNING SIDE-TIP ACTIVATION ONLY
 //  Caller MUST already own miningSubmitMutex. 9C owns startup reconciliation.
 //================================================================================
 bool Blockchain::maybeActivateIncomingWinningSideTipWithSubmissionLockHeld(
@@ -19361,7 +19361,7 @@
 }
 
 //================================================================================
-//  PATCH 08B.4D.9C - STARTUP WINNING-SIDE RECONCILIATION
+// STARTUP WINNING-SIDE RECONCILIATION
 //  Called by main only after the 9A fail-stop bridge is installed and before
 //  node exposure to peers/listeners. Reuses the exact 9B -> 4A -> 4B path.
 //================================================================================
@@ -19730,7 +19730,7 @@
             return false;
         }
 
-        // Patch 08B.4D.1 v1a: submitBlockInternal still owns miningSubmitMutex.
+        // submitBlockInternal still owns miningSubmitMutex.
         // Do not hook connectTipBlock itself because 4B/4C call that primitive
         // while authenticated reorg:pending evidence may exist.
         size_t pruned = 0;
@@ -19744,7 +19744,7 @@
                 "pruning failed safely: " + pruneReason);
         }
 
-        // Patch 08B.4D.3 v1b: post-connect maintenance only.
+        // post-connect maintenance only.
         // A maintenance failure never invalidates the accepted tip.
         std::string undoPruneReason;
         if (!pruneActiveUndoJournalsWithSubmissionLockHeld(
@@ -19763,7 +19763,7 @@
             return false;
         }
 
-        // Patch 08B.4D.9B: only a freshly accepted/persisted side tip can enter
+        // only a freshly accepted/persisted side tip can enter
         // this production activation policy. The caller still owns
         // miningSubmitMutex. Gate-OFF/non-winning outcomes preserve the exact
         // side acceptance result; 9C separately owns startup reconciliation.
@@ -19793,7 +19793,7 @@
 //                      Handle Chain Reorg
 //=============================================================================
 bool Blockchain::handleChainReorganization(const std::vector<Block>& newChain) {
-    // TRU Security Patch 08A v4:
+    //
     // Alternate branches may be bounded/persisted/indexed, but activation stays
     // disabled until Patch 09 chainwork + Patch 08B undo/state journaling.
     (void)newChain;
@@ -20371,7 +20371,7 @@
         prevHash = bestTipHash;
     }
 
-    // TRU Security Patch 07:
+    //
     // Stamp the candidate with difficulty derived from the snapshotted parent.
     // calculateExpectedBits() takes mtx itself, so call it AFTER releasing the
     // snapshot lock above to avoid recursive shared_mutex acquisition.
@@ -20407,7 +20407,7 @@
     auto memTxs = mempool->getAllTransactions();
     std::unordered_set<std::string> spentUTXOs;
 
-    // FIX(patch47): size-aware block assembly.  Do not blindly copy the whole
+    // size-aware block assembly.  Do not blindly copy the whole
     // mempool into a candidate.  Keep 8 MiB below the hard consensus cap so
     // block-level token/TRUSCRIPT metadata has room to be attached afterward.
     size_t assembledBytes = newBlk.serialize().size();
@@ -20438,7 +20438,7 @@
 
     newBlk.extractTokenMetadata(&dbStorage);
 
-    // FIX(patch47): final hard stop after metadata expansion.  Never waste GPU
+    // final hard stop after metadata expansion.  Never waste GPU
     // work on a candidate consensus will reject for size.
     const size_t finalCandidateBytes = newBlk.serialize().size();
     if (finalCandidateBytes > MAX_BLOCK_BYTES) {
@@ -20743,7 +20743,7 @@
     Block newBlk(newHeight, prevHash, static_cast<uint32_t>(std::time(nullptr)), expectedBits);
 
     Transaction coinbase(true);
-    // FIX(patch35): roll a per-attempt extraNonce into the coinbase so each
+    // roll a per-attempt extraNonce into the coinbase so each
     // no-solution retry builds a DIFFERENT coinbase -> different merkle root
     // -> different 80-byte header -> a fresh 2^32 nonce search space. Without
     // this, every retry rebuilt an identical header and re-searched the same
@@ -20764,7 +20764,7 @@
     auto memTxs = mempool->getAllTransactions();
     std::unordered_set<std::string> spentUTXOs;
 
-    // FIX(patch47): size-aware block assembly.  Do not blindly copy the whole
+    // size-aware block assembly.  Do not blindly copy the whole
     // mempool into a candidate.  Keep 8 MiB below the hard consensus cap so
     // block-level token/TRUSCRIPT metadata has room to be attached afterward.
     size_t assembledBytes = newBlk.serialize().size();
@@ -20802,7 +20802,7 @@
         throw;
     }
 
-    // FIX(patch47): final hard stop after metadata expansion.  Never mine a
+    // final hard stop after metadata expansion.  Never mine a
     // candidate that validateBlock() will reject for exceeding 64 MiB.
     {
         const size_t finalCandidateBytes = newBlk.serialize().size();
@@ -21097,7 +21097,7 @@
 
         bestTipHash = "";
         bestTipHeight = 0;
-        difficulty = 0x1e00ffff;   // FIX(patch8): match new starting difficulty
+        difficulty = 0x1e00ffff;   // match new starting difficulty
         chain.clear();
         blockIndex.clear();
         contractStates.clear();
@@ -21118,7 +21118,7 @@
         blockIndex.clear();
         heightToHash.clear();
 
-        // TRU Security Patch 08A v4:
+        //
         // Reset side-pool accounting at the same point blockIndex is cleared so
         // every early-return/exception path starts from a consistent empty pool.
         sideBlockCount_ = 0;
@@ -21247,7 +21247,7 @@
             }
         }
         
-        // TRU Security Patch 09: rebuild cumulative uint256 work every restart.
+        // rebuild cumulative uint256 work every restart.
         if (blocksLoaded > 0) {
             ChainWork256 cumulativeWork = 0;
             std::string expectedParentForWork(64, '0');
@@ -21295,7 +21295,7 @@
             Logger::log("[reloadBlocks] Successfully loaded " + std::to_string(blocksLoaded) + " active blocks.");
 
             // Update bestTipHeight if we loaded fewer blocks than expected.
-            // TRU Security Patch 08A v4: repair the active tip BEFORE side reload.
+            // repair the active tip BEFORE side reload.
             if (blocksLoaded < bestTipHeight) {
                 Logger::log("[reloadBlocks] Warning: Only loaded " + std::to_string(blocksLoaded) + 
                            " blocks out of expected " + std::to_string(bestTipHeight));
@@ -21423,7 +21423,7 @@
         batch.Put("bestTipHeight", dbStorage.computeDataChecksum(bestTipHeightStr) + "|" + bestTipHeightStr);
         batch.Put("difficulty", dbStorage.computeDataChecksum(difficultyStr) + "|" + difficultyStr);
 
-        // Patch 08B.3T v2: persist the current active-tip height mapping in the
+        // persist the current active-tip height mapping in the
         // same metadata batch. This makes disconnect->reapply restore the durable
         // height:N key immediately instead of waiting for clean shutdown.
         if (bestTipHeight.load() > 0 && !bestTipHash.empty()) {
@@ -21520,7 +21520,7 @@
             return;
         }
 
-        // Patch 08B.4A.1: stage dirty block records, but DO NOT clear isDirty
+        // stage dirty block records, but DO NOT clear isDirty
         // until the synchronous durable batch has succeeded. A failed/throwing
         // commit therefore leaves every staged block eligible for retry.
         std::string cur = bestTipHash;
@@ -21549,7 +21549,7 @@
             cur = it->second.block.header.prevHash;
         }
 
-        // Patch 08B.4A.1: consensus/durability state must not outlive stack
+        // consensus/durability state must not outlive stack
         // objects through a detached worker. Commit synchronously while the
         // chain lock, WriteBatch, and dirty-set witnesses are all alive.
         if (!dbStorage.putBatch(batch, true)) {
@@ -21633,7 +21633,7 @@
     }
 
     try {
-        // Patch 08B.4A.1a v2: never read/save chain state without owning mtx.
+        // never read/save chain state without owning mtx.
         // External reader threads are joined by main before Blockchain::stop(),
         // and Blockchain-owned workers are joined before this call.
         std::unique_lock<std::shared_mutex> lock(mtx);
@@ -21902,7 +21902,7 @@
         // Persist block:<genesis>, height:1 and best-tip metadata.
         saveChainState();
 
-        // Patch 15A v1c: fail closed unless genesis is actually durable.
+        // fail closed unless genesis is actually durable.
         // saveChainState() is void and may return after a storage/lock failure,
         // so genesis initialization must verify its own durable postcondition.
         std::string persistedGenesisBlock;
@@ -21944,7 +21944,7 @@
 //-----------------------------------------
 
 bool Blockchain::addTransaction(const Transaction& tx) {
-    // TRU Security Patch 10: bound attacker-controlled work BEFORE a transaction
+    // bound attacker-controlled work BEFORE a transaction
     // can accumulate in the asynchronous queue.
     if (tx.txid.size() != 64 || !isValidHex(tx.txid)) {
         Logger::log("[Blockchain] Rejecting TX before queue: malformed txid");
@@ -22354,7 +22354,7 @@
 //			DECODE TIME LOCK
 //-----------------------------------------------------------------
 std::string decodeTimeLock(const std::string& scriptPubKey) {
-    // Patch 20: the canonical 4-byte Unix timestamp is little-endian.
+    // the canonical 4-byte Unix timestamp is little-endian.
     if (scriptPubKey.length() < 10) return "";
     const std::string h = scriptPubKey.substr(2, 8);
     uint32_t locktime = 0;
@@ -22413,7 +22413,7 @@
             if (meta.value("type", "") == "TRUSCRIPT") {
                 if (count >= skip) {
                     TRUScriptInfo ts;
-                    // FIX(patch51): iteratePrefix() already removed the
+                    // iteratePrefix() already removed the
                     // "tokenMetadata:" prefix. Stripping 14 more characters
                     // turned every 64-character TXID into a 50-character
                     // fragment, which is why the TRUscriptions page showed
@@ -22513,7 +22513,7 @@
         // exclusive lock on shared_mutex
         std::lock_guard<std::shared_mutex> cacheLock(addressCacheMutex);
 
-        // FIX(patch49): derive the ADDRESS from the index key instead of
+        // derive the ADDRESS from the index key instead of
         // keeping the whole remainder of the key.
         //
         // The old line was:
@@ -22568,12 +22568,12 @@
         Logger::log("[startExplorerServer] Address index yielded " +
                     std::to_string(cacheAddrs.size()) + " unique addresses");
 
-        // FIX(patch49): price each address ONCE. The old loop called
+        // price each address ONCE. The old loop called
         // calculate_balance() per index key, and calculate_balance() itself
         // rescans the whole address index, so startup was quadratic in the
         // number of UTXOs.
         for (const auto& addr : cacheAddrs) {
-            // FIX(patch49): read txCount with the SAME API applyBlock writes
+            // read txCount with the SAME API applyBlock writes
             // it with. applyBlock Phase 6 uses putWithDataChecksum, which
             // stores "<checksum>|<value>". A plain get() therefore returned a
             // checksummed string that std::stoi could not parse, so every
@@ -22590,7 +22590,7 @@
                 catch (...) { txc = 0; }
             }
 
-            // FIX(patch49): calculate_balance() returns TRU ATOMS. The old code
+            // calculate_balance() returns TRU ATOMS. The old code
             // assigned it straight into a double as though it were TRU, while
             // applyBlock's Phase 6 updater stores balance/1e8. The same cache
             // field therefore held two different units depending on which
@@ -22611,7 +22611,7 @@
 
 
 //========================================================================================
-//              PATCH 05 — PUBLIC WEB RPC GATEWAY
+// PUBLIC WEB RPC GATEWAY
 //========================================================================================
 // Browser code never receives the privileged Core RPC credential. These
 // same-origin Explorer routes enforce a strict allowlist and create a fresh
@@ -23029,7 +23029,7 @@
                 try {
                     nlohmann::json tokenUtxoJson = nlohmann::json::parse(value);
                     if (tokenUtxoJson["owner"].get<std::string>() == address) {
-                        // FIX(patch49): normalise the tokenUTXO key the same
+                        // normalise the tokenUTXO key the same
                         // way /api/tokens does. That handler names its callback
                         // parameter keySansPrefix and expects "<txid>:<vout>",
                         // while this one assumed the "tokenUTXO:" prefix was
@@ -23050,7 +23050,7 @@
                             {"owner", address},
                             {"txid", txid}
                         };
-                        // FIX(patch49): two bugs here.
+                        // two bugs here.
                         //
                         // 1. dbStorage.get() on a key written with
                         //    putWithDataChecksum returns "<checksum>|<json>".
@@ -23100,7 +23100,7 @@
                 auto entry = nlohmann::json::parse(rawValue);
                 if (entry.value("type", "") == "TRUSCRIPT" && entry.value("owner", "") == address) {
                     scriptsJson.push_back({
-                        // FIX(patch51): iteratePrefix() already stripped the
+                        // iteratePrefix() already stripped the
                         // prefix; the second strip truncated the TXID.
                         {"txid", key},
                         {"data", entry.value("data", "")}
@@ -23148,7 +23148,7 @@
         }
 
         // Use a shared lock to safely read the blockchain data
-        // FIX(patch49): honour ?address=. script.js calls
+        // honour ?address=. script.js calls
         // /api/transactions?address=<addr> to fill the Recent Transactions
         // panel on the address page, but this handler only ever read start
         // and limit, so every address was shown the newest transactions on
@@ -23307,7 +23307,7 @@
         }
 
         // ========================== ADDED: confirmations, vin enrichment, fee ==========================
-        // Compute confirmations if you have best tip height handy
+        // Compute confirmations from the best-tip height.
         // NOTE: getBestTipHeight() should already exist wherever you compute tip height elsewhere.
         int bestTip = getBestTipHeight();            // <- use your existing tip getter
         int confirmations = 0;
@@ -23426,7 +23426,7 @@
                 entry.txid = txid;
                 entry.vout = vout;
 
-                // FIX(patch49): resolve metadata current-first, issuance-second.
+                // resolve metadata current-first, issuance-second.
                 //
                 // The old code looked up ONLY "tokenMetadata:" + <this utxo's
                 // txid>. Wallet::… writes a fresh tokenMetadata record under a
@@ -23480,7 +23480,7 @@
         // Log total tokens found
         Logger::log("[Explorer /api/tokens] Total tokens found: " + std::to_string(tokens.size()));
 
-        // FIX(patch49): honour ?address=. script.js calls
+        // honour ?address=. script.js calls
         // /api/tokens?address=<addr> and then filters client side, but this
         // handler ignored the parameter and returned only the first `limit`
         // (default 10) tokenUTXO entries on the entire chain. Any address
@@ -23870,7 +23870,7 @@
                 if (entry.value("type", "") == "TRUSCRIPT") {
                     TRUScriptInfo ts;
                     
-                    // FIX(patch51): iteratePrefix() already stripped the
+                    // iteratePrefix() already stripped the
                     // prefix. The extra strip here meant this lookup could
                     // never match a TXID returned by /api/truscripts.
                     ts.txid = key;
@@ -24951,7 +24951,7 @@
         std::string errorMessage;
         
         try {
-            // TRU Security Patch 12C.3b v2:
+            //
             // Explorer HTTP block submission is untrusted network ingress.
             // Route side blocks through a bounded source bucket rather than
             // the trusted local/miner bucket.
@@ -24959,7 +24959,7 @@
             if (success) {
                 Logger::log("[Explorer /api/submitblock] Block submitted successfully: " + block.blockHash);
                 
-                // Optional: Broadcast to peers if you have P2P enabled
+                // Broadcast to connected peers when P2P is enabled.
                 try {
                     node.broadcastBlock(block);
                     Logger::log("[Explorer /api/submitblock] Block broadcasted to peers");
--- a/src/blockchain.h
+++ b/src/blockchain.h
@@ -6,7 +6,7 @@
 #include "tx_relay_queue_v1.h"
 #include <chrono>
 #include <unordered_map>
-#include <unordered_set>  // TRU Security Patch 10: queued-tx deduplication
+#include <unordered_set>  // queued-tx deduplication
 #include <shared_mutex>  
 #include <vector>
 #include <cstdint>
@@ -27,8 +27,8 @@
 #include <optional>
 #include <condition_variable>
 #include <future>
-#include <functional>  // Patch 08B.4D.9A: fail-stop shutdown bridge
-#include <boost/multiprecision/cpp_int.hpp>  // TRU Security Patch 09: 256-bit chainwork
+#include <functional>  // fail-stop shutdown bridge
+#include <boost/multiprecision/cpp_int.hpp>  // 256-bit chainwork
 
 class P2PNode;
 class Transaction;
@@ -110,7 +110,7 @@
     Block mineBlockCPU(P2PNode* p2pNode, const std::string& minerAddress);
     bool mineBlock(Block& block, uint64_t& foundNonce, std::vector<unsigned char>& finalHash, uint64_t maxNonce = UINT64_MAX);
     std::string mineBlockGPUChain(P2PNode* p2pNode, const std::string& minerAddress);
-    bool addTransaction(const Transaction& tx);  // TRU Security Patch 10: bounded ingress queue
+    bool addTransaction(const Transaction& tx);  // bounded ingress queue
 
     void removeMempoolTransaction(const std::string& txid);
     std::vector<Transaction> getMempoolTransactions() const;
@@ -131,7 +131,7 @@
     uint64_t getBlockReward() const;
 
     bool loadChainState();
-    // Patch 08B.4B.1 v2: optional marker is published atomically with active-tip
+    // optional marker is published atomically with active-tip
     // metadata. Normal callers use nullptr.
     void saveChainState(const std::string* reorgTipPublishMarkerPayload = nullptr);
     void reloadBlocks();
@@ -266,7 +266,7 @@
     int getConnectedPeerCount() const ;
 
 #ifdef TRU_08B3T_TEST_HOOKS
-    // Patch 08B.3c: local 08B.3T test hooks are compile-time excluded by default.
+    // local 08B.3T test hooks are compile-time excluded by default.
     // Even development builds still require the runtime environment gates.
     bool debug08B3TTestHooksEnabled() const;
     bool debug08B3TMutationEnabled() const;
@@ -278,16 +278,16 @@
                                      std::string& report);
     bool debug08B3TDisconnectReapplyTip(std::string& report);
 
-    // Patch 08B.4A: development-only inspection of the production reorg
+    // development-only inspection of the production reorg
     // planning/preflight machinery. No command can activate a reorganization.
     std::string debug08B4AStatus() const;
-    // Patch 08B.4D.2T v1: DEV-only controlled side-pressure fixture.
+    // DEV-only controlled side-pressure fixture.
     bool debug08B4D2TRunFixture(const std::string& mode,
                                std::string& report);
-    // Patch 08B.4D.4T v1a: DEV-only bounded positive-cache FIFO fixture.
+    // DEV-only bounded positive-cache FIFO fixture.
     // Process-local cache only; no durable state and no consensus mutation.
     bool debug08B4D4TPositiveCacheFifo(std::string& report);
-    // Patch 08B.4D.5T v1c: DEV-only immutable negative-cache FIFO fixture.
+    // DEV-only immutable negative-cache FIFO fixture.
     // Process-local cache only; no durable state and no consensus mutation.
     bool debug08B4D5TNegativeCacheFifo(std::string& report);
     bool debug08B4APreflightCandidate(const std::string& candidateTipHash,
@@ -295,7 +295,7 @@
     bool debug08B4APrepareCandidateForRestartTest(
         const std::string& candidateTipHash,
         std::string& report);
-    // Patch 08B.4B.1 v2: development-only MANUAL executor. It can run only
+    // development-only MANUAL executor. It can run only
     // against an authenticated PREPARED 08B.4A plan and never activates
     // automatic network fork promotion.
     bool debug08B4BExecutePreparedCandidate(
@@ -309,14 +309,14 @@
     bool submitBlock(const Block& block);
     bool submitBlockFromNetwork(const Block& block, const std::string& sourceKey);
 
-    // Patch 08B.4D.9A: production activation plumbing only. The gate is
+    // production activation plumbing only. The gate is
     // OFF unless the exact environment magic is present, and 9A does not
     // connect this gate to side-branch prepare/execute.
     bool productionAutomaticReorgGateEnabled() const;
     void setReorgFailStopShutdownCallback(
         std::function<void(const std::string&)> callback);
 
-    // Patch 08B.4D.9C: startup-only reconciliation. Main calls this after the
+    // startup-only reconciliation. Main calls this after the
     // 9A fail-stop bridge is installed and before any peer connection/listener.
     // Gate-OFF is a no-op. Gate-ARMED scans only the bounded persisted side set.
     bool reconcileWinningSideTipAtStartup(std::string& reasonOut);
@@ -343,7 +343,7 @@
                bool noSeeds,
                bool validationOnly);
 
-    // TRU Security Patch 06: submitBlock() is the sole PUBLIC block
+    // submitBlock() is the sole PUBLIC block
     // acceptance entry point. Confirmed-state mutators stay private.
     bool connectTipBlock(
         const Block& block,
@@ -351,14 +351,14 @@
         const std::string* promotedSideBlockHash = nullptr,
         bool recoveryPublishOnly = false);
 
-    // TRU Security Patch 08A v4: bounded side-chain/fork scaffolding.
+    // bounded side-chain/fork scaffolding.
     // Alternate branches may be indexed, but MUST NOT mutate active
     // UTXO/contract/wallet state. Reorg stays fail-closed.
     bool validateBlockForIndex(const Block& block, const std::string& serializedBlock) const;
     bool submitBlockInternal(const Block& block, const std::string& sourceKey);
     bool indexSideChainBlock(const Block& block, const std::string& sourceKey);
 
-    // Patch 08B.4D.2 v1: caller MUST already own miningSubmitMutex.
+    // caller MUST already own miningSubmitMutex.
     // If a fully validated incoming SIDE block is capacity-blocked, perform at
     // most one deterministic lower-work SIDE-leaf replacement. No active-state
     // mutation, no automatic reorg activation, and no undo:block:* pruning.
@@ -368,10 +368,10 @@
         const std::string& sourceKey,
         bool& handledOut);
 
-    // Patch 08B.4D.1 v1a: caller MUST already own miningSubmitMutex.
+    // caller MUST already own miningSubmitMutex.
     // Removes only objectively stale SIDE leaves whose active fork depth now
     // exceeds MAX_SIDE_FORK_DEPTH. No work-based eviction and no U4 pruning.
-    // Patch 08B.4D.3 v1b: active-chain U4 retention core.
+    // active-chain U4 retention core.
     bool pruneActiveUndoJournalsWithSubmissionLockHeld(
         std::string& reasonOut);
 
@@ -383,7 +383,7 @@
     void reloadSideChainIndexLocked();
     std::string findForkPointHashLocked(const std::string& tipHash) const;
 
-    // Patch 08B.3 v2: reconstruct the fork-point state inside an isolated
+    // reconstruct the fork-point state inside an isolated
     // validation-only LevelDB sandbox, then run normal full block semantics.
     // The standalone wrapper owns miningSubmitMutex. 08B.4 MUST use the
     // explicit lock-held form when its caller already owns miningSubmitMutex.
@@ -394,7 +394,7 @@
         const std::string& candidateTipHash,
         bool submissionLockAlreadyHeld);
 
-    // Patch 08B.4A v2: optional reorgMarkerPayload plumbing remains available
+    // optional reorgMarkerPayload plumbing remains available
     // for atomic state-batch markers, but 08B.4B.1 v2 deliberately publishes
     // CONNECT progress with tip metadata instead. Candidate side ownership is
     // still removed atomically in this state batch via promotedSideBlockHash.
@@ -414,7 +414,7 @@
         const std::string* demoteSideSourceKey = nullptr,
         const std::vector<std::string>* recoveryAllowedSideChildren = nullptr);
 
-    // TRU Security Patch 08B.4A v2: durable reorganization planning/preflight.
+    // durable reorganization planning/preflight.
     // PREPARED remains an authenticated, non-mutating durable intent record.
     // Patch 08B.4B.1 v2 adds only a MANUAL/GATED executor that consumes this
     // exact plan; automatic network fork promotion remains disabled.
@@ -461,7 +461,7 @@
     bool clearPreparedReorgMarker(std::string& reasonOut);
     bool verifyReorgDurabilityPreflight(const ReorgPlan& plan,
                                         std::string& reasonOut);
-    // Patch 08B.4D.4 v1b: bounded in-memory POSITIVE
+    // bounded in-memory POSITIVE
     // candidate-validation cache. Correctness never relies on candidateTipHash
     // alone. The witness commits the exact reorg plan, state-affecting block
     // payload fingerprints, full contract-state snapshot, authenticated durable
@@ -480,7 +480,7 @@
         const std::string& candidateTipHash,
         const std::string& planSha256);
 
-    // Patch 08B.4D.5 v1b: bounded in-memory IMMUTABLE-REJECT cache.
+    // bounded in-memory IMMUTABLE-REJECT cache.
     // This cache is deliberately NOT used for 4A/08B.3 stateful candidate
     // failures. Only explicitly classified byte/content-intrinsic
     // validateBlockForIndex() failures may be stored.
@@ -491,7 +491,7 @@
         const std::string& fingerprint,
         const std::string& reasonCode) const;
 
-    // Patch 08B.4D.6 v1: per-authoritative-side-source budget for the
+    // per-authoritative-side-source budget for the
     // expensive 08B.3 candidate sandbox. This is node-local DoS policy only;
     // it does not change block validity, fork work, or durable chain state.
     bool consumeCandidateValidationBudgetForSource(
@@ -507,7 +507,7 @@
         ReorgPlan& planOut,
         std::string& reasonOut);
 
-    // Patch 08B.4B.1 v2: controlled/manual executor core. Crash recovery of an
+    // controlled/manual executor core. Crash recovery of an
     // interrupted execution marker is intentionally deferred to 08B.4C.
     std::string buildReorgExecutionMarkerPayload(
         const ReorgPlan& plan,
@@ -529,7 +529,7 @@
         std::string& reasonOut,
         bool& mutationStartedOut);
 
-    // Patch 08B.4C.1 v2: authenticated restart classification only.
+    // authenticated restart classification only.
     // This structure is a witness parsed from the checksum-authenticated E2
     // execution marker. 08B.4C.1 never mutates chain state from this record.
     struct ReorgExecutionRecoveryState {
@@ -569,20 +569,20 @@
     void recoverPreparedReorgMarkerAtStartup();
     bool handleChainReorganization(const std::vector<Block>& newChain);
 
-    // Patch 08B.4D.9A: normal-context bridge for a future 9B partial-mutation
+    // normal-context bridge for a future 9B partial-mutation
     // fail-stop. This helper never performs reorganization itself. Future live
     // activation must require reorgFailStopShutdownReady() before mutation.
     bool reorgFailStopShutdownReady() const;
     void requestReorgFailStopShutdown(const std::string& reason);
 
-    // Patch 08B.4D.9B: called only after a side block has been fully accepted
+    // called only after a side block has been fully accepted
     // and persisted while submitBlockInternal() still owns miningSubmitMutex.
     // Gate-OFF and non-winning candidates are no-ops. 9C will own startup scan.
     bool maybeActivateIncomingWinningSideTipWithSubmissionLockHeld(
         const std::string& candidateTipHash,
         std::string& reasonOut);
 
-    // TRU Security Patch 10: bounded pre-validation transaction queue.
+    // bounded pre-validation transaction queue.
     std::queue<Transaction> txQueue_;
     std::queue<std::size_t> txQueueSizes_;
     std::queue<std::uint8_t> txQueueRetries_;
@@ -601,13 +601,13 @@
     std::vector<Block> chain;
     std::unordered_map<std::string, BlockIndexEntry> blockIndex;
 
-    // TRU Security Patch 08A v4:
+    //
     // O(1) resource accounting for SIDE blocks only.
     size_t sideBlockCount_{0};
     size_t sidePoolBytes_{0};
     std::unordered_map<std::string, size_t> sideChildCount_;
 
-    // TRU Security Patch 12C.3b: bounded resource ownership for untrusted
+    // bounded resource ownership for untrusted
     // network sources. Keys are "peer:<IPv4>", "rpc", "explorer",
     // "p2p-legacy", or "local".
     //
@@ -622,12 +622,12 @@
     // In-memory only; restart resets to height 2 without correctness risk.
     int undoActivePruneCursorHeight_{2};
 
-    // Patch 08B.4B.1 v2: if a manually-gated execution fails after the first
+    // if a manually-gated execution fails after the first
     // durable mutation, block ALL live confirmed-state mutation and prevent
     // shutdown from republishing chain state before 08B.4C recovery.
     std::atomic<bool> reorgExecutionHalted_{false};
 
-    // Patch 08B.4D.9A: installed by main before network activity. The callback
+    // installed by main before network activity. The callback
     // only requests the program's existing orderly shutdown path; it does not
     // own or mutate blockchain state.
     mutable std::mutex reorgFailStopShutdownCallbackMutex_;
@@ -664,7 +664,7 @@
     std::chrono::steady_clock::time_point startTime;
     bool noSeeds_;
     bool validationOnly_{false}; // Patch 08B.3 isolated candidate sandbox mode
-    // Patch 08B.4D.4 v1b: memory-only positive validation cache.
+    // memory-only positive validation cache.
     // No durable state, no negative entries, deterministic FIFO eviction.
     struct CandidateValidationCacheEntry {
         std::string witnessSha256;
@@ -677,7 +677,7 @@
     size_t candidateValidationCacheBytes_{0};
 
 
-    // Patch 08B.4D.5 v1b: process-local immutable negative cache.
+    // process-local immutable negative cache.
     // No state/time-dependent failure is eligible.
     struct ImmutableNegativeCandidateCacheEntry {
         std::string fingerprintSha256;
@@ -690,7 +690,7 @@
     mutable size_t immutableNegativeCandidateCacheBytes_{0};
 
 
-    // Patch 08B.4D.6 v1: bounded process-local token buckets keyed by the
+    // bounded process-local token buckets keyed by the
     // authoritative sideBlockSource_ owner of the candidate TIP. Entries are
     // never persisted and therefore reset on process restart.
     struct CandidateValidationBudgetEntry {
@@ -702,7 +702,7 @@
     std::unordered_map<std::string, CandidateValidationBudgetEntry>
         candidateValidationBudgetBySource_;
 
-    // Patch 08B.4D.7 v1: one aggregate untrusted-source token bucket shares
+    // one aggregate untrusted-source token bucket shares
     // the 4D.6 mutex so per-source + global admission is committed atomically.
     // Memory only; process restart deliberately resets this node-policy state.
     size_t globalCandidateValidationBudgetTokens_{0};
@@ -713,7 +713,7 @@
     std::unordered_map<int, std::string> heightToHash;
     void rebuildHeightToHashIfNeeded();
     std::mutex applyBlockMutex;
-    // Patch 08B.3b: token/application serialization is per-chain instance so an
+    // token/application serialization is per-chain instance so an
     // isolated validation sandbox cannot stall the live chain on a process-global mutex.
     std::mutex tokenCreationMutex_;
     void debugTxCountChecksum(LevelDBStorage* storage, const std::string& addr);
--- a/src/contract_call_envelope.h
+++ b/src/contract_call_envelope.h
@@ -8,7 +8,7 @@
 #include "contract_state_init_envelope.h"
 #include "opcodes.h"
 
-// TRU Smart Contract Patch 14D2D — canonical state-call commitment envelope.
+// canonical state-call commitment envelope.
 //
 // The state-anchor input's scriptSig will carry PUSH-ONLY call arguments.
 // A caller-controlled P2PKH vin[1] signs the transaction with SIGHASH_ALL.
--- a/src/contract_call_policy.h
+++ b/src/contract_call_policy.h
@@ -13,10 +13,10 @@
 #include <openssl/sha.h>
 
 #include "opcodes.h"
-#include "contract_state_init_envelope.h"  // Patch 14D2C: creation init envelope primitives
-#include "contract_call_envelope.h"  // Patch 14D2D: signed call-argument commitment
-
-// TRU Smart Contract Patch 14D2 — canonical state-anchor call policy.
+#include "contract_state_init_envelope.h"  // creation init envelope primitives
+#include "contract_call_envelope.h"  // signed call-argument commitment
+
+// canonical state-anchor call policy.
 //
 // This file defines the pre-genesis transaction SHAPE for any locking script
 // that touches the future persistent contract-state domain.
@@ -46,7 +46,7 @@
     bool usesStoreOrLoad{false};
     bool usesMintToken{false};
 
-    // Patch 21: Contract Vault classification uses the same push-aware walk.
+    // Contract Vault classification uses the same push-aware walk.
     // Bridge opcode bytes inside pushed data are data, not executable bridge ops.
     bool usesBridgeOpcode{false};
 
@@ -319,7 +319,7 @@
 }
 
 
-// Patch 14D3A — first activated stateful family: Stateful K/V V1.
+// first activated stateful family: Stateful K/V V1.
 //
 // The locking script contains CALL logic only. Initial state is supplied by a
 // TRUSTATE envelope and committed once at root creation. Patch 14D3A2 also
@@ -631,7 +631,7 @@
     return true;
 }
 
-// Patch 21 — shared structural classifiers for read-side/explorer identity.
+// shared structural classifiers for read-side/explorer identity.
 // These helpers are deliberately byte-exact and never search inside pushed data.
 // They do not alter VM, mempool, or consensus policy.
 struct CanonicalTimeLockV1Info {
@@ -911,7 +911,7 @@
     return nullptr;
 }
 
-// Patch 15A — canonical Voting V1 initialization schema.
+// canonical Voting V1 initialization schema.
 //
 // proposal     1..200 bytes
 // numoptions   exactly 1 byte, value 2..10
@@ -980,7 +980,7 @@
     return true;
 }
 
-// Patch 15B — canonical Token Issuer V1 initialization schema.
+// canonical Token Issuer V1 initialization schema.
 //
 // token_name      1..10 canonical ASCII bytes [A-Za-z0-9_-]
 // exchange_rate   uint64 LE, > 0 tokens per TRU atom
@@ -1000,7 +1000,7 @@
         return false;
     }
 
-    // Patch 16 — pin the family-level serialization order in addition to the
+    // pin the family-level serialization order in addition to the
     // generic TRUSTATE sorted/unique requirement. This keeps Token Issuer V1
     // initialization single-encoding even if the generic envelope evolves.
     static constexpr std::array<const char*, 4> EXPECTED_INIT_KEYS = {
@@ -1172,7 +1172,7 @@
         return result;
     }
 
-    // Patch 14D3B: a continuation call also creates one canonical f751 output,
+    // a continuation call also creates one canonical f751 output,
     // but it is NOT a new state root. Presence of exactly one TRUCALL envelope
     // distinguishes that transaction-level shape. The input loop must still
     // prove that vin[0] spends the currently registered live state anchor.
@@ -1186,7 +1186,7 @@
         return result;
     }
 
-    // Patch 14D3A2: owner identity is bound to the sole funding input.
+    // owner identity is bound to the sole funding input.
     // Multi-input creation is deliberately deferred so there is no ambiguity
     // about which signer owns the state root.
     if (tx.vin.size() != 1) {
@@ -1285,7 +1285,7 @@
     return true;
 }
 
-// Patch 14D3A2 — canonical owner identity extracted from a standard P2PKH
+// canonical owner identity extracted from a standard P2PKH
 // locking script. Persisting only the 20-byte hash160 (as lowercase hex) avoids
 // Base58/network-display ambiguity while preserving exact ownership semantics.
 inline bool ExtractCanonicalP2PKHHash160Hex(
@@ -1401,7 +1401,7 @@
         return result;
     }
 
-    // Patch 15B — structural economic-call-value rule. No activated V1
+    // structural economic-call-value rule. No activated V1
     // continuation may reduce its state anchor. Family execution decides whether
     // the exact delta must be zero (K/V, Voting) or positive (Token Issuer).
     if (continuationAmount < lockedAmount) {
--- a/src/contract_state_init_envelope.h
+++ b/src/contract_state_init_envelope.h
@@ -10,7 +10,7 @@
 #include "contract_state_limits.h"
 #include "opcodes.h"
 
-// TRU Smart Contract Patch 14D2C — canonical state-init envelope primitives.
+// canonical state-init envelope primitives.
 //
 // Stateful locking scripts must contain CALL logic only. Creation-time state is
 // carried separately in a zero-value OP_RETURN output so initialization is
--- a/src/contract_state_limits.h
+++ b/src/contract_state_limits.h
@@ -8,7 +8,7 @@
 #include <unordered_map>
 #include <vector>
 
-// TRU Smart Contract Patch 14D2B — genesis contract-state resource policy.
+// genesis contract-state resource policy.
 //
 // These are CONSENSUS limits pinned before persistent VM state is activated.
 // The VM remains persistence-disabled until Patch 14D3.
--- a/src/contract_state_lineage.h
+++ b/src/contract_state_lineage.h
@@ -4,9 +4,9 @@
 #include <limits>
 #include <string>
 #include <string_view>
-#include "contract_state_limits.h"  // Patch 14D2B: shared state-key limits
+#include "contract_state_limits.h"  // shared state-key limits
 
-// TRU Smart Contract Patch 14D1 — stable state-domain / lineage key primitives.
+// stable state-domain / lineage key primitives.
 //
 // IMPORTANT: this header defines canonical identities and durable key shapes only.
 // It does NOT activate persistence, create a lineage registry, select continuation
@@ -123,7 +123,7 @@
     return "contractlive:" + std::string(stateDomain);
 }
 
-// Patch 14D3A2 — durable owner binding for owner-authorized V1 calls.
+// durable owner binding for owner-authorized V1 calls.
 // The value stored at this key is exactly 40 lowercase ASCII hex characters
 // representing the creator funding input's standard P2PKH hash160.
 inline std::string BuildContractOwnerKey(std::string_view stateDomain)
@@ -132,7 +132,7 @@
     return "contractowner:" + std::string(stateDomain);
 }
 
-// Patch 15A — immutable activated contract-family registry. Existing
+// immutable activated contract-family registry. Existing
 // Stateful K/V V1 roots intentionally have no family key; Voting V1 roots are
 // born with exactly "voting_v1". Future families must add explicit values
 // rather than inferring consensus semantics from mutable user state.
--- a/src/contract_state_runtime.h
+++ b/src/contract_state_runtime.h
@@ -17,7 +17,7 @@
 #include "script_interpreter.h"
 #include "tx.h"
 
-// TRU Smart Contract Patch 14D3B — confirmed state-domain loader +
+// confirmed state-domain loader +
 // transaction-local Stateful K/V V1 execution.
 //
 // Consensus/mempool callers load only the CONFIRMED LevelDB snapshot keyed by
@@ -147,7 +147,7 @@
         return false;
     }
 
-    // Patch 15B: existing K/V roots predate the immutable family registry and
+    // existing K/V roots predate the immutable family registry and
     // intentionally have no contractfamily key. Activated typed roots MUST carry
     // an exact known family id. Unknown future values fail closed.
     std::string family;
@@ -614,7 +614,7 @@
     }
     resultOut.state[voterKey] = voterValue;
 
-    // Patch 17 — bind countN, totalvotes and voted:<hash160> to the same
+    // bind countN, totalvotes and voted:<hash160> to the same
     // completed tally before mempool/block validation can accept the transition.
     std::size_t postNumOptions = 0;
     std::uint64_t postEndTime = 0;
@@ -669,7 +669,7 @@
     return true;
 }
 
-// Patch 15B — validate the complete authoritative Token Issuer V1 state.
+// validate the complete authoritative Token Issuer V1 state.
 // Until transfer semantics are activated, total_issued MUST equal the checked
 // sum of all persisted buyer balances.
 inline bool ValidateTokenIssuerV1State(
@@ -752,7 +752,7 @@
     return true;
 }
 
-// Patch 16 — exact persistent issuance delta. A purchase may create a new
+// exact persistent issuance delta. A purchase may create a new
 // buyer balance or accumulate an existing one, but it must mutate exactly two
 // logical keys and may not delete state:
 //   total_issued
--- a/src/crypto_ecdsa.cpp
+++ b/src/crypto_ecdsa.cpp
@@ -726,6 +726,6 @@
     EC_POINT_free(point);
     BN_CTX_free(ctx);
     
-    // TRU Security Patch 11: fixed-K/MagicLock signatures are normalized too.
+    // fixed-K/MagicLock signatures are normalized too.
     return normalizeLowS(signature);
 }
--- a/src/crypto_ecdsa.h
+++ b/src/crypto_ecdsa.h
@@ -52,7 +52,7 @@
                        const std::string& message,
                        const std::vector<unsigned char>& signature);
 
-    // TRU Security Patch 11: transaction-signature canonicality.
+    // transaction-signature canonicality.
     // signature is DER only; the trailing sighash byte is checked by script code.
     static bool isStrictDERLowS(const std::vector<unsigned char>& signature);
     static bool verifyCanonicalTransactionSignature(
--- a/src/leveldb_storage.cpp
+++ b/src/leveldb_storage.cpp
@@ -18,7 +18,7 @@
 std::mutex LevelDBStorage::s_lifecycleMtx;
 
 // ============================================================================
-// Patch 08B.4D.4 v1b - per-LevelDB-path logical mutation generation
+// per-LevelDB-path logical mutation generation
 //
 // Cache invalidation metadata only. It is intentionally process-local:
 // candidate validation cache entries are process-local and begin empty on
@@ -148,7 +148,7 @@
         }
     }
 
-    // Patch 08B.3b: serialize only slow open/close lifecycle work. The normal
+    // serialize only slow open/close lifecycle work. The normal
     // DB registry lock remains available to unrelated live DB I/O while Open()
     // performs filesystem recovery/LOCK-file work.
     std::lock_guard<std::mutex> lifecycleLock(s_lifecycleMtx);
@@ -391,7 +391,7 @@
             if (skipBlockRecords && key.rfind("block:", 0) == 0) {
                 continue;
             }
-            // Patch 08B.3 v2: historical undo journals dominate sandbox copy
+            // historical undo journals dominate sandbox copy
             // cost but only the active suffix being rewound is required. Keep
             // exactly that allow-list when one is supplied; candidate blocks
             // create their own temporary journals as they are applied.
@@ -469,7 +469,7 @@
         Logger::log("[LevelDBStorage] Put key " + key + " failed: " + status.ToString());
         throw std::runtime_error("[LevelDBStorage] Failed to put key: " + key + ", error: " + status.ToString());
     }
-    // Patch 08B.4D.4 v1b: successful logical DB mutation.
+    // successful logical DB mutation.
     patch08B4D4BumpMutationGeneration(dbPath);
     return true;
 }
@@ -487,7 +487,7 @@
         Logger::log("[LevelDBStorage] Delete key " + key + " failed: " + status.ToString());
         throw std::runtime_error("[LevelDBStorage] Failed to delete key: " + key + ", error: " + status.ToString());
     }
-    // Patch 08B.4D.4 v1b: successful logical DB mutation.
+    // successful logical DB mutation.
     patch08B4D4BumpMutationGeneration(dbPath);
     return true;
 }
@@ -552,7 +552,7 @@
         throw std::runtime_error("[LevelDBStorage] clear() failed: " + status.ToString());
     }
     Logger::log("[LevelDBStorage] Database cleared");
-    // Patch 08B.4D.4 v1b: successful logical DB mutation.
+    // successful logical DB mutation.
     patch08B4D4BumpMutationGeneration(dbPath);
 }
 
@@ -592,7 +592,7 @@
 bool LevelDBStorage::repairDatabase() {
     auto dbMutex = getDbMutexHandle();
     if (!dbMutex) return false;
-    // Patch 08B.4D.4 v1b: repair can rewrite logical state outside
+    // repair can rewrite logical state outside
     // ordinary mutation primitives; disable candidate-cache
     // generation for this path before repair proceeds.
     patch08B4D4DisableMutationGeneration(dbPath);
@@ -619,7 +619,7 @@
 }
 
 void LevelDBStorage::iteratePrefix(const std::string &prefix, std::function<void(const std::string &, const std::string &)> callback) const {
-    // Patch 08B.3a: snapshot matching entries while holding the lifetime-safe
+    // snapshot matching entries while holding the lifetime-safe
     // per-DB mutex, then release it before invoking callbacks. This prevents
     // registry erase/rehash races without deadlocking callbacks that read DB state.
     std::vector<std::pair<std::string, std::string>> entries;
@@ -693,7 +693,7 @@
         Logger::log("[LevelDBStorage] Batch write failed: " + status.ToString());
         return false;
     }
-    // Patch 08B.4D.4 v1b: successful logical DB mutation.
+    // successful logical DB mutation.
     patch08B4D4BumpMutationGeneration(dbPath);
     return true;
 }
@@ -725,7 +725,7 @@
         Logger::log("[LevelDBStorage] PutWithDataChecksum failed: " + status.ToString());
         return false;
     }
-    // Patch 08B.4D.4 v1b: successful logical DB mutation.
+    // successful logical DB mutation.
     patch08B4D4BumpMutationGeneration(dbPath);
     return true;
 }
@@ -824,7 +824,7 @@
         Logger::log("[LevelDBStorage] PutContract failed: " + status.ToString());
         return false;
     }
-    // Patch 08B.4D.4 v1b: successful logical DB mutation.
+    // successful logical DB mutation.
     patch08B4D4BumpMutationGeneration(dbPath);
     return true;
 }
@@ -959,7 +959,7 @@
         outValue = data;
     }
 
-    // Patch 08B.3a: migrate only after releasing the per-DB mutex; putContract()
+    // migrate only after releasing the per-DB mutex; putContract()
     // acquires the same mutex and the previous inline migration self-deadlocked.
     if (migrateOldChecksum) {
         if (!const_cast<LevelDBStorage*>(this)->putContract(key, data)) {
--- a/src/leveldb_storage.h
+++ b/src/leveldb_storage.h
@@ -19,11 +19,11 @@
     ~LevelDBStorage();
 
     bool get(const std::string &key, std::string &value) const;
-    // Patch 08B.1: exact on-disk read for durable undo pre-images.
+    // exact on-disk read for durable undo pre-images.
     // Returns true for Found/NotFound; `found` distinguishes absence
     // from a storage/IO failure.
     bool getRaw(const std::string &key, std::string &rawValue, bool &found) const;
-    // Patch 08B.3: create a consistent raw-key snapshot in another LevelDB.
+    // create a consistent raw-key snapshot in another LevelDB.
     // Used only for isolated candidate-branch validation; values are copied
     // byte-for-byte so checksum wrappers and undo journals remain exact.
     bool copyRawSnapshotTo(
@@ -42,11 +42,11 @@
     size_t countKeys() const;
 
     bool repairDatabase();
-    // Patch 08B.3b: live-state callers keep durable sync=true by default.
+    // live-state callers keep durable sync=true by default.
     // Disposable validation sandboxes may explicitly use sync=false.
     bool putBatch(leveldb::WriteBatch& batch, bool sync = true);
     const std::string& path() const noexcept { return dbPath; }
-    // Patch 08B.4D.4 v1b: process-local, per-database-path
+    // process-local, per-database-path
     // logical mutation generation. This is NOT durable consensus state.
     // Candidate-validation cache entries bind to this generation so any
     // successful same-path LevelDB mutation forces a cache miss/fail-closed
@@ -71,10 +71,10 @@
 
     leveldb::DB* db;
     const std::string dbPath;
-    // Patch 08B.3b: the shared registry owns the custom LevelDB block cache so
+    // the shared registry owns the custom LevelDB block cache so
     // repeated validation-sandbox open/close cycles cannot leak 100 MiB each.
     static std::unordered_map<std::string, SharedDbEntry> s_dbInstances;
-    // Patch 08B.3a: shared ownership prevents erase from destroying a mutex
+    // shared ownership prevents erase from destroying a mutex
     // while an in-flight operation still holds/waits on it. The map itself is
     // protected by s_globalMtx.
     static std::unordered_map<std::string, std::shared_ptr<std::mutex>> s_dbMutexes;
@@ -82,7 +82,7 @@
     // Slow DB::Open/close operations are serialized here, outside s_globalMtx,
     // so unrelated live DB operations are not frozen by sandbox lifecycle I/O.
     static std::mutex s_lifecycleMtx;
-    // Patch 08B.3a: copy a lifetime-safe per-path mutex handle while holding
+    // copy a lifetime-safe per-path mutex handle while holding
     // s_globalMtx. Callers then lock the shared mutex object without retaining
     // an unordered_map iterator/reference across rehash/erase.
     std::shared_ptr<std::mutex> getDbMutexHandle() const;
--- a/src/logging.h
+++ b/src/logging.h
@@ -11,10 +11,10 @@
 
 class Logger {
 public:
-    // Call this once, near the start of main(), with your chosen filename.
+    // Initialize logging once near the start of main().
     static void init(const std::string &filename);
 
-    // Close the log on exit if you want:
+    // Close the log during shutdown.
     static void shutdown();
 
     // Write a message to the log file (thread-safe).
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -11,7 +11,7 @@
 #include "mempool.h"
 #include "utils.h"
 #include "rpc_server.h"
-#include "rpc_utils.h"  // Patch 05: RPC transport authentication
+#include "rpc_utils.h"  // RPC transport authentication
 #include "ai_oracle_service.h"
 #include "ai_providers.h"          // TOKEN-AI-03A wallet evolution provider factories
 #include "token_evolution.h"       // TOKEN-AI-03A wallet evolution engine
@@ -34,7 +34,7 @@
 #include <ctime>    
 #include "gpu_miner.h"
 
-// TRU CLI UX PATCH 30B5D — vendor-neutral GPU discovery uses the same
+// vendor-neutral GPU discovery uses the same
 // OpenCL runtime family as the TRU GPU miner. CPU-only builds remain valid.
 #ifdef USE_OPENCL
 #ifndef CL_TARGET_OPENCL_VERSION
@@ -68,15 +68,15 @@
 #include <cstring>
 #include <cstdint>
 #include <poll.h>
-#include <sys/wait.h>   // FIX(patch36): waitpid for spawned miner processes
-#include <fcntl.h>   // FIX(patch36): open() for miner log redirect
+#include <sys/wait.h>   // waitpid for spawned miner processes
+#include <fcntl.h>   // open() for miner log redirect
 #include <future>
 #include <termios.h>
 #include <unistd.h>
 #include "script_interpreter.h"
-#include "contract_call_policy.h"  // Patch 14D3A: Stateful K/V V1 creation
-#include "contract_state_lineage.h"  // Patch 14D3B: stable root/live/owner call resolution
-#include "contract_state_runtime.h"  // Patch 15A1: shared confirmed-state/Voting runtime helpers
+#include "contract_call_policy.h"  // Stateful K/V V1 creation
+#include "contract_state_lineage.h"  // stable root/live/owner call resolution
+#include "contract_state_runtime.h"  // shared confirmed-state/Voting runtime helpers
 #include <curl/curl.h>
 #include <fstream>
 #include <stdexcept>
@@ -93,7 +93,7 @@
 #include "network_identity.h"  // MULTINODE-01D local-endpoint self-peer guard
 
 // -------------------------------------------------------------------
-//     TRU CLI UX PATCH 30B5D — OPENCL GPU DISCOVERY
+// OPENCL GPU DISCOVERY
 // -------------------------------------------------------------------
 // This is intentionally read-only hardware discovery. It enumerates the same
 // vendor-neutral OpenCL GPU class used by TRU's GPU mining backends, but it
@@ -263,7 +263,7 @@
 // -------------------------------------------------------------------
 //              HELPERS FOR DISPLAY
 // -------------------------------------------------------------------
-// TRU CLI UX PATCH 30A v3 — explicit terminal ownership.
+// explicit terminal ownership.
 //
 // Normal deck mode:
 //   rows 1..15  = persistent TRU / miner header
@@ -284,7 +284,7 @@
 static std::atomic<unsigned int> g_cliInputDepth{0};
 
 // -------------------------------------------------------------------
-//   TRU CLI UX PATCH 30B4 — RESULT / APPEND / PROMPT OWNERSHIP
+// RESULT / APPEND / PROMPT OWNERSHIP
 // -------------------------------------------------------------------
 // `displayOutput()` used to clear row 16 through the bottom on *every* call.
 // That made multi-line command reports erase themselves one line at a time.
@@ -300,13 +300,13 @@
 static std::vector<TruCliResultLine> g_cliResultLines;
 static bool                          g_cliCommandOutputStarted{false};
 
-// TRU CLI UX PATCH 30B5A — background sync owns a dedicated status lane.
+// background sync owns a dedicated status lane.
 // Sync state is stored independently of result text so it can remain visible
 // without stealing the active input cursor or erasing command output.
 static std::mutex g_cliSyncStatusMutex;
 static std::string g_cliSyncStatus{"Starting blockchain synchronization..."};
 
-// TRU CLI UX PATCH 30B5B — fixed three-line footer stack.
+// fixed three-line footer stack.
 // The command entry is deliberately ABOVE both asynchronous status lanes so
 // neither the heartbeat nor background sync can ever replace typed commands.
 static int truMenuPromptRow(int rows) {
@@ -326,7 +326,7 @@
     return 16;
 }
 
-// TRU CLI UX PATCH 30B5 — nested questions must remain in the user's visual
+// nested questions must remain in the user's visual
 // focus instead of sharing the bottom command/sync lanes. The prompt is placed
 // midway between the result origin and the heartbeat, with a dedicated banner
 // row immediately above it.
@@ -587,7 +587,7 @@
     g_cliContentFocus.store(true, std::memory_order_release);
     clearCliResultState();
 
-    // TRU CLI UX PATCH 30B5E — progressive workflows already own visible
+    // progressive workflows already own visible
     // result rows before a nested displayPrompt() is invoked. Mark the command
     // generation active here so displayPrompt() does not initialize an empty
     // owned-result buffer and erase those progressive rows.
@@ -609,7 +609,7 @@
 
 
 // -------------------------------------------------------------------
-//      TRU CLI UX PATCH 30B3 — SHARED EXACT TRU AMOUNTS
+// SHARED EXACT TRU AMOUNTS
 // -------------------------------------------------------------------
 // Human decimal parsing and atom formatting now live in tru_amount.h so CLI,
 // wallet, smart-contract and RPC transaction builders share one exact parser.
@@ -720,7 +720,7 @@
 
 std::atomic<bool> g_shuttingDown{false};
 
-// Patch 08B.4A.1b v2: POSIX signal handlers remain async-signal-safe.
+// POSIX signal handlers remain async-signal-safe.
 // Handlers only update sig_atomic_t state and write one byte to a non-blocking
 // self-pipe. Logging, atomics, mutexes and terminal resizing happen later in
 // normal C++ execution context.
@@ -744,7 +744,7 @@
     errno = savedErrno;
 }
 
-// Patch 08B.4D.9A: normal C++ execution-context bridge for a future
+// normal C++ execution-context bridge for a future
 // post-mutation reorg fail-stop. This does not impersonate a POSIX signal; it
 // sets the existing shutdown atomics and writes one wake byte so a blocked CLI
 // poll exits through the already-reviewed graceful shutdown path.
@@ -930,7 +930,7 @@
     }
 }
 
-// TRU CLI UX PATCH 30A v3: every signal-aware input path owns the cursor
+// every signal-aware input path owns the cursor
 // while blocked. The top-level command prompt gets a second guard so the
 // heartbeat may continue there while remaining paused for nested input.
 class TruCliInputPaintGuard {
@@ -996,7 +996,7 @@
     g_statusNoticeColor = colorCode;
 }
 
-// TRU CLI UX PATCH 30A v2: row 16 is the normal-mode status lane.
+// row 16 is the normal-mode status lane.
 // Output Focus owns row 16+, so a notice is preserved until the deck returns.
 void paintStatusNotice() {
     if (g_cliContentFocus.load(std::memory_order_acquire) ||
@@ -1103,7 +1103,7 @@
 //============================================================================================
 //                      Display Voting V1 Confirmed Results
 //============================================================================================
-// Patch 17 — canonical reader only. Voting results are derived from the
+// canonical reader only. Voting results are derived from the
 // confirmed stable-root/live-anchor state domain. Legacy "contract:<addr>:*"
 // mirrors are intentionally ignored and are no longer an authority.
 void displayVotingResults(const Blockchain& chain, const std::string& contractInput,
@@ -1229,7 +1229,7 @@
 //===========================================================================================
 //				SMART CONTRACT TOKENS
 //===========================================================================================
-// Patch 16 — Token Issuer V1 reads use the canonical root-scoped LevelDB
+// Token Issuer V1 reads use the canonical root-scoped LevelDB
 // namespace. Legacy ContractStorage is not authoritative.
 uint64_t getTokenBalance(
     const Blockchain& chain,
@@ -1424,7 +1424,7 @@
 //            		Spinner at row=60
 //============================================================================================
 void runBlockchainSpinner(std::atomic<bool> &running, std::mutex &coutMutex) {
-    // TRU CLI UX PATCH 30A6 — restore the original TRU spinner identity while
+    // restore the original TRU spinner identity while
     // retaining the new fixed heartbeat lane and cursor-safety architecture.
     // Original pre-UI sequence:
     //   ⣾ ⣽ ⣻ ⢿ ⡿ ⣟ ⣯ ⣷  Blockchain Running Smoothly...
@@ -1498,7 +1498,7 @@
 }
 
 //============================================================================================
-//                    TRU CLI UX PATCH 30C — TRANSACTION RECEIPTS
+// TRANSACTION RECEIPTS
 //============================================================================================
 // Receipts are presentation/read-only. They never classify an arbitrary
 // script as a contract. Canonical contract family/root fields are emitted only
@@ -1918,7 +1918,7 @@
             if (choice != 1 && choice != 2) choice = 1;
         }
 
-        // --- Oobabooga config (defaults to your local flags) ---
+        // Oobabooga configuration defaults.
         OobaConfig cfg;
         {
             std::string baseUrl = readLineTrimmed("Oobabooga base URL (enter to use " + cfg.baseUrl + "):", rows, coutMutex);
@@ -3199,7 +3199,7 @@
                 return;
             }
 
-            // FIX(patch56-confirmed-token-index): tokenUTXO is a derived
+            // tokenUTXO is a derived
             // current-ownership index.  Never display a record whose
             // controlling confirmed UTXO has already been spent.
             const std::string liveUtxoKey =
@@ -3457,7 +3457,7 @@
 //========================================================================
 //               TRU CONTRACT VAULT — PAGED CONTRACT LIST
 //========================================================================
-// Patch 19B: contract listing follows the same alternate-buffer ownership
+// contract listing follows the same alternate-buffer ownership
 // model as the Token Vault, but packs complete contract cards onto pages so
 // one contract is never split merely because the wallet has many contracts.
 static void displayPagedContractVault(const Blockchain& chain,
@@ -3860,7 +3860,7 @@
                 return;
             }
 
-            // FIX(patch56-confirmed-token-index): debug view must also
+            // debug view must also
             // distinguish current holdings from historical/stale indexes.
             const std::string liveUtxoKey =
                 "utxo:" + txid + ":" + std::to_string(vout);
@@ -4204,7 +4204,7 @@
     fmt::print(colorText("      T O K E N I Z E D  R E A L  U T I L I T Y   A N E X T - G E N   C O N S O L E   E X P E R I E N C E         \n\n", 90, false));
 }
 
-// TRU CLI UX PATCH 30A v3 — no-flicker header repaint.
+// no-flicker header repaint.
 //
 // The legacy printBanner() remains the startup/full-reset painter. This helper
 // restores the same TRU identity into the shared rows 9..15 without clearing
@@ -4223,7 +4223,7 @@
     std::lock_guard<std::mutex> lock(coutMutex);
     fmt::print("\033[s");
 
-    // TRU CLI UX PATCH 30A4: clean stale full-screen instructions above the
+    // clean stale full-screen instructions above the
     // persistent logo without a destructive whole-screen clear.
     for (int row = 1; row <= 8; ++row) {
         fmt::print("\033[{};1H\033[K", row);
@@ -4246,7 +4246,7 @@
 // -------------------------------------------------------------------
 void printChainInfo(const Blockchain &chain) {
     try {
-        // FIX(patch41): build ALL lines as one atomic string and clear each
+        // build ALL lines as one atomic string and clear each
         // target row explicitly, drawn at rows 9-14 (above where the menu sits
         // on a normal terminal). Previously this drew at row 12 -- the same row
         // the menu starts at -- so a menu repaint truncated the lower lines.
@@ -4275,7 +4275,7 @@
 // -------------------------------------------------------------------
 
 // ---------------------------------------------------------------------------
-// FIX(patch36): external miner process manager.
+// external miner process manager.
 //
 // The node must NOT mine on its own threads (that starves its RPC). Instead we
 // spawn the standalone miner executables as child processes and just track them.
@@ -4335,7 +4335,7 @@
 } // namespace tru_miner_proc
 
 // ---------------------------------------------------------------------------
-// FIX(patch37): live miner-stats banner rendered in the node's own TUI.
+// live miner-stats banner rendered in the node's own TUI.
 // Reads the stats the node already tracks (updated by the miners' periodic
 // reportmineractivity RPC), so it works with the decoupled child-process
 // miners from patch36 without sharing their terminal.
@@ -4372,11 +4372,11 @@
         }
     }
 
-    // FIX(patch39): only show the banner while mining is actually running.
+    // only show the banner while mining is actually running.
     // If not active, clear the banner rows (2..6) and draw nothing, so the
     // stats disappear when mining stops and reappear when it resumes.
     if (!active) {
-        // FIX(patch46-fixed): do NOT erase rows 9..19 on every CLI loop.
+        // do NOT erase rows 9..19 on every CLI loop.
         // Those rows are also used by the TRU logo, Chain Info and command output.
         return;
     }
@@ -4393,7 +4393,7 @@
 
     std::ostringstream bx; bx << std::hex << bits;
 
-    // TRU CLI UX PATCH 30A v2: rich neon dashboard owns rows 9..15 only.
+    // rich neon dashboard owns rows 9..15 only.
     // Row 16 is reserved for status/output, so the dashboard never clears it.
     static const char* NEON_PINK  = "\033[38;2;255;20;147m";
     static const char* NEON_BLUE  = "\033[38;2;0;255;255m";
@@ -4472,7 +4472,7 @@
     std::cout << ss.str() << std::flush;
 }
 
-// FIX(patch46-fixed): stable responsive menu.
+// stable responsive menu.
 //
 // Important design rules:
 //   1. Draw menu lines with ABSOLUTE cursor positions -- never a long stream
@@ -4509,7 +4509,7 @@
     return static_cast<int>(text.size()) + 1;
 }
 
-// TRU Smart Contract Patch 20B — Time Lock command discoverability.
+// Time Lock command discoverability.
 // Both canonical redemption commands are first-class deck tokens.
 static bool truDeckHighlightToken(const std::string& token)
 {
@@ -4562,7 +4562,7 @@
 
     if (text.empty()) return;
 
-    // TRU CLI UX PATCH 30A4a: box-drawing glyphs are UTF-8 strings, not
+    // box-drawing glyphs are UTF-8 strings, not
     // single-byte C++ char literals. Compare complete byte sequences and slice
     // by their encoded width so borders are never split mid-codepoint.
     const std::string boxTopLeft = u8"╔";
@@ -4570,7 +4570,7 @@
     const std::string boxBotLeft = u8"╚";
     const std::string boxVertical = u8"║";
 
-    // TRU CLI UX PATCH 30A4b: keep the frame rails purple but promote the
+    // keep the frame rails purple but promote the
     // title and section-header names to green for the requested TRU CORE look.
     const std::string boxTopRight = u8"╗";
     const std::string boxMidRight = u8"╣";
@@ -4601,7 +4601,7 @@
         text.size() >= (boxMidLeft.size() + boxMidRight.size()) &&
         text.compare(text.size() - boxMidRight.size(),
                      boxMidRight.size(), boxMidRight) == 0) {
-        // TRU CLI UX PATCH 30A4c: section names may be centered anywhere in
+        // section names may be centered anywhere in
         // the rail. Only the semantic label is green; every ═/╠/╣ stays purple.
         static const std::vector<std::string> sectionLabels = {
             "WALLET // IDENTITY & VALUE",
@@ -4669,9 +4669,9 @@
         return;
     }
 
-    // TRU CLI UX PATCH 30A4 — neon hierarchy + aligned Command Deck.
+    // neon hierarchy + aligned Command Deck.
     // Option codes are presentation-only; dispatcher branches are unchanged.
-    // TRU CLI UX PATCH 30A5 — sequential public CLI navigation.
+    // sequential public CLI navigation.
     const std::vector<std::string> fullLines = {
         "╔═════════════════════════════ T R U   C O R E ══════════════════════════════╗",
         "║  MENU NAVIGATION // command code + ENTER // M switches deck view           ║",
@@ -4948,7 +4948,7 @@
     // Display the newly minted banner
     printBanner(coutMutex);
 
-    // TRU CLI UX PATCH 30A v2:
+    //
     // The CLI heartbeat worker is started exactly once by main(). startCLI()
     // does not spawn a second terminal painter.
 
@@ -5036,8 +5036,8 @@
         resetCliCommandOutputGeneration();
 
 #ifdef TRU_08B3T_TEST_HOOKS
-        // Patch 08B.3T v2: hidden LOCAL-CLI-only runtime test commands.
-        // Patch 08B.3c: this parser is absent from production builds by default.
+        // hidden LOCAL-CLI-only runtime test commands.
+        // this parser is absent from production builds by default.
         // Deliberately absent from displayMenu() and never routed through RPC.
         if (choice == "08b3t" || choice.rfind("08b3t ", 0) == 0) {
             std::istringstream testInput(choice);
@@ -5086,10 +5086,10 @@
             continue;
         }
 
-        // Patch 08B.4D.4T v1a: hidden LOCAL-CLI-only positive-cache FIFO fixture.
+        // hidden LOCAL-CLI-only positive-cache FIFO fixture.
         // Entire branch is compiled out when TRU_08B3T_TEST_HOOKS=OFF.
         // No RPC registration exists.
-        // Patch 08B.4D.5T v1c: hidden LOCAL-CLI-only immutable negative-cache FIFO fixture.
+        // hidden LOCAL-CLI-only immutable negative-cache FIFO fixture.
         // Entire branch is compiled out when TRU_08B3T_TEST_HOOKS=OFF.
         // No RPC registration exists.
         if (choice == "08b4d5t" || choice.rfind("08b4d5t ", 0) == 0) {
@@ -5150,7 +5150,7 @@
             continue;
         }
 
-        // Patch 08B.4D.2T v1b: hidden LOCAL-CLI-only pressure fixture.
+        // hidden LOCAL-CLI-only pressure fixture.
         // Compiled out when TRU_08B3T_TEST_HOOKS=OFF; never routed through RPC.
         if (choice == "08b4d2t" || choice.rfind("08b4d2t ", 0) == 0) {
             std::istringstream testInput(choice);
@@ -5241,7 +5241,7 @@
         }
 #endif  // TRU_08B3T_TEST_HOOKS
 
-        // TRU CLI UX PATCH 30A5 — public sequential navigation adapter.
+        // public sequential navigation adapter.
         // The visible CLI is 1..36 in screen order. Internally, route those
         // numbers to the established handler codes so command logic remains
         // untouched. Lettered legacy aliases remain accepted but unadvertised.
@@ -5505,12 +5505,12 @@
             displayOutput(fmt::format("[CLI] Balance after manual refresh: {:.8f} TRU", refreshedBalance), rows, coutMutex);
         }
         else if (choice == "7") {
-            // FIX(patch36): CPU mining submenu -> spawn standalone tru_miner_cpu.
+            // CPU mining submenu -> spawn standalone tru_miner_cpu.
             // The node no longer mines on its own threads; it stays responsive.
             using namespace tru_miner_proc;
             std::string minerAddr = wallet.getCurrentAddress();
             unsigned int hw = std::thread::hardware_concurrency();
-            // TRU CLI UX PATCH 30B5C — mining submenu is owned result content.
+            // mining submenu is owned result content.
             // Raw std::cout here was erased when displayPrompt() began a new
             // command-output generation, leaving only the final "4. Back" line.
             std::ostringstream cpuMenu;
@@ -5564,10 +5564,10 @@
             // sub == "4" or anything else: back to menu.
         }
         else if (choice == "7a") {
-            // FIX(patch36): GPU mining submenu -> spawn standalone tru_miner.
+            // GPU mining submenu -> spawn standalone tru_miner.
             using namespace tru_miner_proc;
             std::string minerAddr = wallet.getCurrentAddress();
-            // TRU CLI UX PATCH 30B5C — keep all GPU submenu choices inside
+            // keep all GPU submenu choices inside
             // the owned result buffer so the mid-screen input card cannot erase them.
             const TruCliGpuInventory gpuInventory = detectTruOpenCLGpus();
             std::ostringstream gpuMenu;
@@ -6163,7 +6163,7 @@
                 }
             } catch (const std::exception& e) {
                 displayOutputProgressive(outputBuffer, "[CLI] Failed to issue token: " + std::string(e.what()), currentRow, rows, coutMutex, 31, true); // Red
-                // patch 7: also pin the failure as a sticky notice so it isn't
+                // also pin the failure as a sticky notice so it isn't
                 // covered by the menu redraw (e.g. immature-coinbase rejection).
                 setStatusNotice("Token FAILED: " + std::string(e.what()) +
                                 "  (see node log for details)", 31);
@@ -6432,7 +6432,7 @@
 
             ::displayOutput("\nSending to AI Oracle...", rows, coutMutex);
 
-            // Direct call to your handlers (defined in your codebase)
+            // Invoke the registered handlers.
             json response;
             try
             {
@@ -7379,7 +7379,7 @@
 
                 if (contractType == "TIME LOCK")
                 {
-                    // Patch 20 v2: show the consensus maturity clock before the
+                    // show the consensus maturity clock before the
                     // user chooses a timestamp. CLTV uses parent-chain MTP, not
                     // wall clock and not the candidate header timestamp.
                     try {
@@ -7551,7 +7551,7 @@
                         outputBuffer, currentRow, rows, coutMutex);
                     uint64_t threshold = std::stoull(thStr);
 
-                    // TRU Smart Contract Patch 07 — Oracle threshold numeric encoding.
+                    // Oracle threshold numeric encoding.
                     // Consensus numeric values are encoded as canonical 8-byte little-endian uint64.
                     std::vector<unsigned char> thresholdBytes(8);
                     for (size_t i = 0; i < thresholdBytes.size(); ++i)
@@ -7578,7 +7578,7 @@
 
                 else if (contractType == "TOKEN ISSUER")
                 {
-                    // Patch 15B — Token Issuer V1 creation uses the same proven
+                    // Token Issuer V1 creation uses the same proven
                     // f751 stable-root anchor as K/V and Voting. All authoritative
                     // initialization is carried by one canonical TRUSTATE envelope.
                     std::string tokenName = getValidatedInput(
@@ -7895,7 +7895,7 @@
                     }
                     lastVotingContract = now;
 
-                    // Patch 15A — Voting V1 uses the proven canonical f751
+                    // Voting V1 uses the proven canonical f751
                     // state-anchor. Initialization lives ONLY in TRUSTATE and
                     // becomes authoritative after the creation transaction mines.
                     scriptText = "OP_STORE OP_1";
@@ -8093,7 +8093,7 @@
                         outputBuffer, currentRow, rows, coutMutex);
 
                     // Exchange rate: how many TRU atoms are credited per 1 foreign atomic unit
-                    // (Pick your convention; below we mirror TOKEN ISSUER style but for foreign->TRU)
+                    // Use token-issuer conventions for foreign-chain to TRU transfers.
                     std::string rateStr = getValidatedInput(
                         "Enter exchange rate (TRU atoms credited per 1 foreign atomic unit): ",
                         [](const std::string &s)
@@ -8139,7 +8139,7 @@
                     // And include a small dispatcher to generate a deposit address on first call:
                     //   asset OP_LOAD "NOVO" OP_EQUAL OP_IF OP_NOVO_GENADDR OP_ELSE OP_BSTY_GENADDR OP_ENDIF
                     //
-                    // confirm/redeem/verify opcodes can run on later calls (your opcode impls handle state).
+                    // Confirm, redeem, and verify opcodes handle state on subsequent calls.
                     //
                     std::string scriptText;
                     // metadata
@@ -8147,7 +8147,7 @@
                     scriptText += asciiToHex("rate_le") + " " + bytesToHex(std::vector<unsigned char>(u64le(rate).begin(), u64le(rate).end())) + " OP_STORE ";
                     scriptText += asciiToHex("min_conf") + " " + bytesToHex(std::vector<unsigned char>(u32le(minConf).begin(), u32le(minConf).end())) + " OP_STORE ";
 
-                    // Address generation gate (idempotent in your opcode impl):
+                    // Idempotent address-generation gate.
                     // if asset == "NOVO" then OP_NOVO_GENADDR else OP_BSTY_GENADDR
                     scriptText += asciiToHex("asset") + " OP_LOAD ";
                     scriptText += asciiToHex("NOVO") + " OP_EQUAL OP_IF ";
@@ -8173,7 +8173,7 @@
                         throw std::runtime_error("Failed to fetch UTXO " + utxoTxid + ":" + std::to_string(utxoVout));
                     uint64_t coinValue = utxo.amount;
 
-                    // You already have contractAmount from earlier prompt in the 18 menu
+                    // Use contractAmount from the earlier menu prompt.
                     uint64_t fee = 10000;
                     if (coinValue < contractAmount + fee)
                         throw std::runtime_error(fmt::format(
@@ -8278,7 +8278,7 @@
                         { return s.size() <= 50; },
                         "[CLI] Too long.",
                         outputBuffer, currentRow, rows, coutMutex);
-                    // Patch 14D3A: creation state is NOT executable locking code.
+                    // creation state is NOT executable locking code.
                     // The anchor is call-only; the initial key/value is carried in
                     // the TRUSTATE envelope added to this creation transaction.
                     statefulInitKey = kvKey;
@@ -8327,7 +8327,7 @@
                 // contract output - USE THE CONTRACT AMOUNT!
                 tx.vout.emplace_back(contractAmount, scriptHex);
 
-                // Patch 14D3A — canonical one-time Stateful K/V initialization.
+                // canonical one-time Stateful K/V initialization.
                 // Keep the state anchor at vout 1 for stable contract-address
                 // semantics; append the zero-value TRUSTATE envelope afterward.
                 if (contractType == "STATEFUL CONTRACT")
@@ -9639,7 +9639,7 @@
 
         else if (choice == "23")
         {
-            // Patch 15B — canonical Token Issuer V1 payable continuation.
+            // canonical Token Issuer V1 payable continuation.
             // Legacy wallet.mintTokens()/ContractStorage is intentionally not
             // authoritative for this path.
             try
@@ -10019,7 +10019,7 @@
                 displayOutput("Total Issued: " + std::to_string(info.totalIssued), rows, coutMutex);
             }
 
-            // Patch 16 — UI arithmetic mirrors checked V1 multiplication.
+            // UI arithmetic mirrors checked V1 multiplication.
             displayOutput("\n=== EXCHANGE CALCULATOR ===", rows, coutMutex);
             std::vector<uint64_t> amounts = {1000, 10000, 100000, 1000000};
             for (uint64_t amt : amounts)
@@ -10042,7 +10042,7 @@
         }
         else if (choice == "12a")
         {
-            // Patch 16C v2 — displayOutput() clears the output region on each
+            // displayOutput() clears the output region on each
             // call, so compose this multi-line result and render it once.
             std::ostringstream tokenBalanceReport;
             tokenBalanceReport
@@ -10059,7 +10059,7 @@
                 continue;
             }
 
-            // Patch 16B — Blockchain::getContracts() returns a wrapper object
+            // Blockchain::getContracts() returns a wrapper object
             // containing a "contracts" array. Iterate the actual array rather
             // than the wrapper values so canonical Token Issuer V1 roots are
             // discoverable by the persistent-balance UI.
@@ -10371,7 +10371,7 @@
             }
         }
         else if (choice == "M" || choice == "m") {
-            // TRU CLI UX PATCH 30A v3: M/C restore the logo with an absolute
+            // M/C restore the logo with an absolute
             // header repaint instead of a full-screen clear. If mining is
             // active, displayMinerStats() will immediately reclaim rows 9..15.
             if (g_cliContentFocus.load(std::memory_order_acquire)) {
@@ -10385,7 +10385,7 @@
             continue;
         }
         else if (choice == "C" || choice == "c") {
-            // TRU CLI UX PATCH 30A6: restore the original Matrix-rain clear.
+            // restore the original Matrix-rain clear.
             // clearOutput() also releases Output Focus before the animation.
             clearOutput(rows, coutMutex);
             printBanner3(coutMutex);
@@ -10819,7 +10819,7 @@
             }
         }
 
-        // Patch 05: non-loopback privileged RPC requires explicit operator opt-in.
+        // non-loopback privileged RPC requires explicit operator opt-in.
         {
             auto it = networkCfg.find("rpcAllowRemote");
             if (it != networkCfg.end() && !it->second.empty()) {
@@ -10915,7 +10915,7 @@
         }
         Logger::log("[main] Using data directory: " + dbPath);
 
-        // Patch 05: authenticated privileged RPC boundary. Authentication is
+        // authenticated privileged RPC boundary. Authentication is
         // mandatory. Loopback is the default; non-loopback binds require an
         // explicit rpcAllowRemote=1 acknowledgement. The token is never logged.
         if (!tru_rpc::isLoopbackBind(rpcBind) && !rpcAllowRemote) {
@@ -10955,7 +10955,7 @@
         node = std::make_unique<P2PNode>();
         chain = std::make_unique<Blockchain>(dbPath, std::string(tru_network::GENESIS_ADDRESS), *node, noSeeds);
 
-        // Patch 08B.4D.9A: install fail-stop plumbing before the node is exposed
+        // install fail-stop plumbing before the node is exposed
         // to P2P. 9B may activate only freshly accepted strict-winning side tips.
         chain->setReorgFailStopShutdownCallback(
             [](const std::string& reason) {
@@ -10966,7 +10966,7 @@
             (chain->productionAutomaticReorgGateEnabled() ? "ARMED" : "OFF") +
             " activation=INCOMING_SIDE_ONLY");
 
-        // Patch 08B.4D.9C: reconcile a persisted strict-winning side tip before
+        // reconcile a persisted strict-winning side tip before
         // node exposure, outbound seed/saved-peer connections, or P2P listener.
         std::string startupReorgReason;
         if (!chain->reconcileWinningSideTipAtStartup(startupReorgReason)) {
@@ -11331,7 +11331,7 @@
 
             Logger::log("[main] Starting blockchain sync with peers...");
             std::atomic<bool> syncRunning{true};
-            // TRU CLI UX PATCH 30B5: background synchronization must never
+            // background synchronization must never
             // paint over interactive CLI questions. syncWithPeers() treats a
             // null cout mutex as "log only / no terminal progress output".
             // Users can still request explicit sync status from the CLI.
@@ -11617,7 +11617,7 @@
 
             Logger::log("[main] Blockchain processing shutdown phase completed");
 
-            // Patch 08B.4A.1a v2: synchronous shutdown saves with bounded retries.
+            // synchronous shutdown saves with bounded retries.
             Logger::log("[main] >>> Phase 4: Saving application state...");
             
             auto saveWithRetry =
--- a/src/main_ai_integration.cpp
+++ b/src/main_ai_integration.cpp
@@ -81,7 +81,7 @@
         script.push_back(OP_LOAD);
         
         script.push_back(OP_IF);
-            // Patch 18: AI routing is off-chain and must never invoke the
+            // AI routing is off-chain and must never invoke the
             // deterministic consensus OP_DATAFEED namespace. Legacy router
             // execution therefore fails closed instead of fabricating a feed.
             pushData("ROUTE_AI_REQUEST_OFFCHAIN");
--- a/src/master_bridge.h
+++ b/src/master_bridge.h
@@ -11,7 +11,7 @@
                             std::vector<std::vector<unsigned char>>& stack);
     bool redeemNOVO(ScriptExecutionContext& ctx,
                     std::vector<std::vector<unsigned char>>& stack);
-    // Optional if you use it in your switch:
+    // Optional switch integration.
     bool verifyNOVO(ScriptExecutionContext& ctx,
                     std::vector<std::vector<unsigned char>>& stack);
 }
@@ -23,7 +23,7 @@
                             std::vector<std::vector<unsigned char>>& stack);
     bool redeemBSTY(ScriptExecutionContext& ctx,
                     std::vector<std::vector<unsigned char>>& stack);
-    // Optional if you use it in your switch:
+    // Optional switch integration.
     bool verifyBSTY(ScriptExecutionContext& ctx,
                     std::vector<std::vector<unsigned char>>& stack);
 }
--- a/src/mempool.cpp
+++ b/src/mempool.cpp
@@ -1,5 +1,5 @@
 #include "mempool.h"
-#include "tru_limits.h"  // TRU Security Patch 10: shared money/mempool limits
+#include "tru_limits.h"  // shared money/mempool limits
 #include "tokens.h"    
 #include "tx.h"
 #include "logging.h"  
@@ -10,11 +10,11 @@
 #include "utils.h"
 #include "address_helpers.h"
 #include "script_interpreter.h"
-#include "script_context_builder.h"  // Patch 14B: shared execution context
+#include "script_context_builder.h"  // shared execution context
 #include "contract_call_policy.h"     // Patch 14D2/14D3B: state-anchor call policy
-#include "contract_state_runtime.h"    // Patch 14D3B: confirmed state snapshot + scratch execution
+#include "contract_state_runtime.h"    // confirmed state snapshot + scratch execution
 #include "tru_swap_prepared_funding_guard.h"  // TRU SWAP GROUP-01 input reservation
-#include "opcodes.h"  // Patch 13A2: explicit OP_RETURN consensus parity
+#include "opcodes.h"  // explicit OP_RETURN consensus parity
 #include "utils.h"
 #include <algorithm>  // TRU Security Patch 12A bounded gas min
 #include <ctime>      // TRU Security Patch 12A mempool block-time context
@@ -34,7 +34,7 @@
 //			HELPERS for the AI ORACLE
 //=========================================================================
 std::vector<std::string> Mempool::scanForAIRequests() const {
-    // Patch 18: OP_DATAFEED is a deterministic consensus opcode, not an AI
+    // OP_DATAFEED is a deterministic consensus opcode, not an AI
     // request marker. The AI service's persisted ContractStorage/RPC queue is
     // authoritative for off-chain inference work. Keep this API for caller
     // compatibility but deliberately return no script-derived requests.
@@ -409,7 +409,7 @@
         }
     }
 
-    // Patch 12B v3: reserve one shared validation-work slot BEFORE fee
+    // reserve one shared validation-work slot BEFORE fee
     // computation. This budgets both input/UTXO fee work and script execution.
     if (!tryAcquireValidationSlot()) {
         Logger::log(
@@ -792,7 +792,7 @@
         return true;
     }
 
-    // Patch 10: do NOT perform the old O(spent-set * pool * inputs) repair scan
+    // do NOT perform the old O(spent-set * pool * inputs) repair scan
     // for every not-found txid (applyBlock calls this for coinbase, too).
     // All marker/accounting mutations now happen atomically under mtx_.
     Logger::log("[Mempool] removeTransaction => TX not found => " + txid);
@@ -949,7 +949,7 @@
         }
     }
 
-    // Patch 14D3A — relay parity for first stateful creation family.
+    // relay parity for first stateful creation family.
     const auto stateCreation =
         tru_contract_call::ValidateStatefulCreationShape(tx);
     if (!stateCreation.valid) {
@@ -982,7 +982,7 @@
     std::unordered_set<std::string> seenInputs;
     bool sawStatefulCallInput = false;
 
-    // TRU Smart Contract Patch 20 v2 — snapshot relay execution context once
+    // snapshot relay execution context once
     // per transaction from ONE active-tip view. OP_BLOCKTIME/stateful runtime
     // keeps the admission wall clock, while CLTV receives candidate-parent MTP
     // so relay and block consensus use the same maturity clock.
@@ -1055,7 +1055,7 @@
             return false;
         }
 
-        // Patch 14D3A2 — the sole funding input of a new Stateful K/V
+        // the sole funding input of a new Stateful K/V
         // root is the durable owner identity. Require standard P2PKH now so
         // relay and confirmed application cannot disagree about ownership.
         if (stateCreation.hasCreation && i == 0) {
@@ -1110,7 +1110,7 @@
             return false;
         }
 
-        // TRU Smart Contract Patch 13A2 — mempool parity for leading OP_RETURN.
+        // mempool parity for leading OP_RETURN.
         // Patch 13A makes a confirmed UTXO whose locking script begins with
         // OP_RETURN consensus-unspendable in validateBlock(). Admission must
         // enforce the identical rule so an invalid spend cannot enter the
@@ -1128,7 +1128,7 @@
             Logger::log("[Mempool] malformed/over-budget script => TX=" + tx.txid);
             return false;
         }
-        // TRU Smart Contract Patch 14D3B — first mutable state activation.
+        // first mutable state activation.
         // Only the exact owner-bound Stateful K/V V1 anchor is executable.
         // State is loaded from the confirmed LevelDB root and copied into a
         // disposable scratch map; mempool admission never writes persistence.
@@ -1529,7 +1529,7 @@
     size_t inputIndex,
     const std::vector<unsigned char> &scriptPubKey
 ) const {
-    // TRU Security Patch 11: one sighash implementation only.
+    // one sighash implementation only.
     return tx.getSigHash(inputIndex, scriptPubKey);
 }
 
--- a/src/mempool.h
+++ b/src/mempool.h
@@ -50,13 +50,13 @@
     void cleanAfterBlock(const Block& block);
     bool isMagicLockScript(const std::string& scriptHex) const;
     std::vector<std::string> scanForAIRequests() const;
-    // Patch 18: OP_DATAFEED is no longer overloaded as an AI-request marker.
+    // OP_DATAFEED is no longer overloaded as an AI-request marker.
 
 private:
     std::unordered_map<std::string, Transaction> pool_;
     Blockchain& chain_;
 
-    // TRU Security Patch 10:
+    //
     // One mutex owns the transaction map, spent-outpoint set, and all resource
     // accounting so callers cannot observe torn mempool state.
     mutable std::mutex mtx_;
--- a/src/message_handler.cpp
+++ b/src/message_handler.cpp
@@ -729,7 +729,7 @@
                         Block block;
                         block.fromProto(blockProto);
 
-                        // TRU Security Patch 06:
+                        //
                         // Every P2P block enters the authoritative submitBlock()
                         // pipeline. No direct confirmed-state mutation here.
                         if (chain->submitBlockFromNetwork(
@@ -788,7 +788,7 @@
                             }
                         }
 
-                        // TRU Security Patch 10: queue admission is bounded and
+                        // queue admission is bounded and
                         // mempool validation is asynchronous behind that bounded queue.
                         if (chain->addTransaction(tx)) {
                             Logger::log(
--- a/src/message_handler.h
+++ b/src/message_handler.h
@@ -2,7 +2,7 @@
 #define MESSAGE_HANDLER_H
 
 #include <string>
-#include "tru_limits.h"  // FIX(patch47): shared P2P payload ceiling
+#include "tru_limits.h"  // shared P2P payload ceiling
 #include "message.pb.h"
 #include "blockchain.h"
 #include "logging.h"
@@ -15,7 +15,7 @@
 
 class MessageHandler {
 public:
-    // TRU Security Patch 12C.1: receive/send ceilings are per message type.
+    // receive/send ceilings are per message type.
     // BLOCK retains the large relay envelope; TX/control messages use smaller
     // limits from tru_limits.h. There is intentionally no single catch-all
     // MAX_MESSAGE_SIZE public constant anymore.
@@ -37,7 +37,7 @@
     static blockchain::BaseMessage createGetHeightRequest();
     static blockchain::BaseMessage createBlockNotFoundMessage(uint64_t height, const std::string& errorMsg = "");
 
-    // Patch 15B.1: per-connection fail-closed network handshake validation.
+    // per-connection fail-closed network handshake validation.
     static bool validateVersionMessage(const blockchain::Version& version);
     // Handle received messages
     static void handleMessage(const blockchain::BaseMessage& msg, Blockchain* chain, P2PNode* node);
--- a/src/my_gpu_miner.cpp
+++ b/src/my_gpu_miner.cpp
@@ -31,14 +31,14 @@
 #include <algorithm>
 #include <memory>
 
-// Include your blockchain headers - same as CPU miner
+// Shared blockchain headers used by CPU and GPU miners.
 #include "block.h"
 #include "tx.h"
 #include "sha256_21e8.h"
 #include "utils.h"
 #include "address_helpers.h"
 #include "logging.h"
-#include "rpc_utils.h"  // Patch 05: authenticated node RPC
+#include "rpc_utils.h"  // authenticated node RPC
 
 // Enhanced 2025 Blockchain Color Palette ⛓️
 #define C_RESET     "\033[0m"
@@ -711,7 +711,7 @@
     std::cout << C_INFO << "      🌡️ Thermal Safety: " << C_GOLD << (int)(profile.thermalSafetyFactor * 100) << "%" << C_RESET << "\n";
 }
 
-// FIXED: Ultra-Enhanced Modern GPU mining interface with AUTO-TUNING INFO! ⛓️✨
+// GPU mining interface with automatic tuning information.
 void printModernGPUInterface(int blockHeight, const std::vector<std::unique_ptr<GPUDevice>>& devices,
                             double totalHashRate, double elapsed, uint64_t totalHashes, 
                             uint64_t currentChunk, uint64_t totalChunks, bool firstCall,
@@ -805,7 +805,7 @@
     std::cout << C_TEXT << "│ " << SYMBOL_CROWN << " Blocks Mined  " << C_GOLD << C_BOLD << blockCount << " TOTAL VICTORIES" << std::string(30, ' ') << "│\n";
     std::cout << C_TEXT << "│ " << SYMBOL_SHIELD << " Thermal State " << C_SUCCESS << "SAFELY OPTIMIZED" << std::string(30, ' ') << "│\n";
 
-    // Update Performance Metrics with AI ENHANCEMENTS!
+    // Update performance metrics.
     std::cout << "\033[19;1H";
     std::cout << C_TEXT << "│ " << SYMBOL_SPEED << " Total Rate    " << formatHashRate(totalHashRate) << std::string(35, ' ') << "│\n";
     auto now = std::chrono::steady_clock::now();
@@ -1186,7 +1186,7 @@
     auto startTime = std::chrono::steady_clock::now();
     bool firstCall = true;
     
-    // FIX(patch34): lets the progress thread exit when a full nonce sweep
+    // lets the progress thread exit when a full nonce sweep
     // finishes with NO solution (g_found stays false, g_shutdown stays false).
     std::atomic<bool> miningDone{false};
     
@@ -1413,7 +1413,7 @@
                                     err = clGetEventInfo(*it, CL_EVENT_COMMAND_EXECUTION_STATUS, sizeof(cl_int), &eventStatus, nullptr);
                                     
                                     if (err == CL_SUCCESS && eventStatus == CL_COMPLETE) {
-                                        // FIX(patch32): capture the index ONCE, before any erase,
+                                        // capture the index ONCE, before any erase,
                                         // so `events` and `kernelWorkSizes` stay in lockstep.
                                         size_t idx = static_cast<size_t>(it - events.begin());
                                         size_t workCompleted = kernelWorkSizes[idx];
@@ -1472,7 +1472,7 @@
 
                                         clReleaseEvent(*it);
                                         it = events.erase(it);
-                                        // FIX(patch32): erase the SAME index captured above; using
+                                        // erase the SAME index captured above; using
                                         // (it - events.begin()) here points at the NEXT element after
                                         // erase, which desynced the vectors and hung the monitor loop.
                                         kernelWorkSizes.erase(kernelWorkSizes.begin() + idx);
@@ -1573,7 +1573,7 @@
         thread.join();
     }
     
-    // FIX(patch34): all device workers finished; release the progress thread
+    // all device workers finished; release the progress thread
     // even when no solution was found, so join() below cannot hang.
     miningDone.store(true, std::memory_order_release);
     
@@ -1887,7 +1887,7 @@
                 sbReq["params"] = paramsObj;
                 auto sbRes = cli.Post("/rpc", sbReq.dump(), "application/json");
 
-                // FIX(patch48): HTTP 200 does not mean the node accepted the block.
+                // HTTP 200 does not mean the node accepted the block.
                 // TRU returns {"result":{"status":"accepted"|"rejected"|"duplicate",...}}.
                 // See the matching change in my_miner.cpp.
                 bool accepted = false;
@@ -1986,7 +1986,7 @@
     std::cout << C_WARNING << "╰─" << dashLine << "─╯" << C_RESET << "\n";
 }
 
-// Enhanced main function with spectacular AI startup sequence! 🚀
+// GPU miner entry point.
 int main(int argc, char* argv[]) {
     // Setup signal handlers first
     signal(SIGINT, signalHandler);
@@ -2041,7 +2041,7 @@
         return 1;
     }
     
-    // SPECTACULAR AI STARTUP SEQUENCE! ⛓️
+    // Startup display.
     if (!quiet) {
         std::cout << "\033[2J\033[1;1H"; // Clear screen
         std::cout << C_BLOCKCHAIN << C_BOLD;
@@ -2083,7 +2083,7 @@
     // Cleanup GPU resources
     cleanupGPU();
     
-    // SPECTACULAR AI SHUTDOWN SEQUENCE! ⛓️
+    // Shutdown display.
     if (!quiet) {
         std::cout << "\n" << C_BLOCKCHAIN << C_BOLD;
         std::cout << "╔═══════════════════════════════════════════════════════════════════╗\n";
--- a/src/my_miner.cpp
+++ b/src/my_miner.cpp
@@ -26,7 +26,7 @@
 #include "utils.h"
 #include "address_helpers.h"
 #include "logging.h"
-#include "rpc_utils.h"  // Patch 05: authenticated node RPC
+#include "rpc_utils.h"  // authenticated node RPC
 #include "blockchain.h"
 
 // Modern 2025 Color Palette - Enhanced
@@ -269,7 +269,7 @@
     return oss.str();
 }
 
-// Format time duration - FIXED to show milliseconds
+// Format time duration — show milliseconds
 std::string formatDuration(double seconds) {
     int hours = static_cast<int>(seconds) / 3600;
     int minutes = (static_cast<int>(seconds) % 3600) / 60;
@@ -316,7 +316,7 @@
     // Calculate dynamic values for animations
     double cpuTemp = std::min(90.0, 30.0 + (hashRate / 1000000.0) * 60.0); // Simulated CPU temp
     int heatLevel = std::min(6, (int)(cpuTemp / 15.0));
-    double efficiency = nThreads > 0 ? (hashRate / nThreads) : 0.0; // H/s per thread - FIXED
+    double efficiency = nThreads > 0 ? (hashRate / nThreads) : 0.0; // H/s per thread
     
     if (firstCall) {
         Logger::log("[UI] Initializing ultra modern CPU miner interface");
@@ -377,7 +377,7 @@
     std::cout << "║ " << pulseColor << "HASH POWER " << ULTRA_PULSE_FRAMES[animFrame % 10] 
               << C_TEXT << " " << C_FIRE << std::setw(12) << formatHashRate(hashRate) 
               << C_MUTED << " │ " << C_NEON_BLUE << "Per Core: " << C_ACCENT 
-              << std::fixed << std::setprecision(1) << efficiency << " H/s"  // FIXED: Changed from KH/s to H/s
+              << std::fixed << std::setprecision(1) << efficiency << " H/s"  // Changed from KH/s to H/s
               << std::string(20, ' ') << C_NEON_GREEN << "║\n";
 
     // CPU Temperature with dynamic heat indicator
@@ -434,7 +434,7 @@
     std::cout << "║ " << statusColor << statusText << std::string(73 - statusText.length(), ' ') << C_NEON_PURPLE << "║\n";
     std::cout << "╚═══════════════════════════════════════════════════════════════════════════╝" << C_RESET << "\n\n";
 
-    // Statistics Dashboard - FIXED
+    // Statistics Dashboard
     std::cout << C_NEON_ORANGE << "╔══ 📊 MINING STATISTICS ═══════════════════════════════════════════════════╗\n";
     std::cout << "║ " << C_TEXT << "Runtime      " << C_NEON_BLUE << formatDuration(elapsed) 
               << C_MUTED << " │ " << C_TEXT << "Total Hashes " << C_ACCENT << std::setw(15) << totalHashes 
@@ -489,12 +489,12 @@
     }
 }
 
-// Enhanced progress thread - FIXED to accept blockCount by reference
+// Enhanced progress thread — accept blockCount by reference
 void progressThread(std::atomic<int>& miningStatus, std::atomic<uint64_t>& totalHashes,
                     int blockHeight, int nThreads, int maxThreads, uint64_t maxNonce,
                     std::vector<uint64_t>& threadNonces, int updateInterval, bool quiet,
                     httplib::Client& cli, const std::string& minerAddr, uint64_t& blockCount, 
-                    const CpuInfo& cpuInfo, uint32_t difficultyBits) {  // FIX(patch44): real bits
+                    const CpuInfo& cpuInfo, uint32_t difficultyBits) {  // real bits
     Logger::log("[PROGRESS] Starting progress thread for block " + std::to_string(blockHeight));
     
     auto startTime = std::chrono::steady_clock::now();
@@ -576,7 +576,7 @@
 }
 
 // Double SHA-256 with 21E8 tweak
-// FIX(patch48): allocation-free variant used by the mining hot loop.
+// allocation-free variant used by the mining hot loop.
 // The original returned a std::vector by value, which meant one heap
 // allocation and one free for EVERY nonce tried.  Writing into a caller
 // supplied 32-byte buffer removes that entirely.
@@ -621,7 +621,7 @@
     return std::vector<unsigned char>(hash2, hash2 + 32);
 }
 
-// Enhanced CPU mining function with accurate hash counting - FIXED to accept blockCount by reference
+// Enhanced CPU mining function with accurate hash counting — accept blockCount by reference
 static bool mineBlockCPU_21E8(Block& candidate, uint64_t maxNonce, int nThreads, httplib::Client& cli,
                               const std::string& minerAddr, int& extraNonce, const CpuInfo& cpuInfo, uint64_t& blockCount) {
     Logger::log("[MINING] Starting CPU mining for block " + std::to_string(candidate.height) + 
@@ -639,7 +639,7 @@
     std::atomic<int> miningStatus(0);
     // REMOVED: uint64_t blockCount = 0; - Now using the passed reference
 
-    // FIX(patch12): reset only per-attempt solution state.
+    // reset only per-attempt solution state.
     //
     // g_totalHashes is intentionally NOT reset here. It is a session-wide
     // cumulative counter used by the keepalive thread to calculate:
@@ -663,7 +663,7 @@
         
         std::vector<unsigned char> localHdr(header80);
         uint64_t localHashCount = 0;
-        // FIX(patch48): reused stack buffer, see doubleSha256_21E8 overload.
+        // reused stack buffer, see doubleSha256_21E8 overload.
         unsigned char finalHash[32];
         
         for (uint64_t nonce = tid; nonce < maxNonce && !found.load() && !g_shutdown.load(); nonce += nThreads) {
@@ -672,14 +672,14 @@
             localHdr[78] = (unsigned char)((nonce >> 16) & 0xff);
             localHdr[79] = (unsigned char)((nonce >> 24) & 0xff);
 
-            doubleSha256_21E8(localHdr.data(), finalHash);   // FIX(patch48): no per-hash allocation
+            doubleSha256_21E8(localHdr.data(), finalHash);   // no per-hash allocation
             
             // CRITICAL: Increment both local and global hash counters for accurate stats
             totalHashes.fetch_add(1, std::memory_order_relaxed);
             g_totalHashes.fetch_add(1, std::memory_order_relaxed);
             localHashCount++;
             
-            // FIX(patch11): use the canonical PoW comparison. The old loop
+            // use the canonical PoW comparison. The old loop
             // treated byte 0 as most-significant (big-endian), but the target
             // from bitsToTargetArrayFree() is little-endian, so it never matched
             // valid hashes. compare256LE() (utils.h) iterates byte 31 -> 0, the
@@ -723,7 +723,7 @@
     std::thread progThread(progressThread, std::ref(miningStatus), std::ref(totalHashes),
                            candidate.height, nThreads, cpuInfo.threads, maxNonce, std::ref(threadNonces),
                            200, false, std::ref(cli), minerAddr, std::ref(blockCount), std::ref(cpuInfo),
-                           candidate.header.bits);  // FIX(patch44): pass real difficulty bits
+                           candidate.header.bits);  // pass real difficulty bits
 
     for (auto& t : threads) t.join();
     if (found.load()) {
@@ -819,7 +819,7 @@
     return candidate;
 }
 
-// Enhanced mining loop with improved keepalive thread - FIXED to accept blockCount by reference
+// Enhanced mining loop with improved keepalive thread — accept blockCount by reference
 static void startMiningLoop(const std::string& nodeIP, int nodePort, const std::string& minerAddr,
                             int nThreads, uint64_t maxNonce, const CpuInfo& cpuInfo, uint64_t& blockCount) {
     Logger::log("[MAIN] Starting mining loop - Node: " + nodeIP + ":" + std::to_string(nodePort) + 
@@ -832,7 +832,7 @@
     cli.set_read_timeout(20, 0);
     int extraNonce = 0;
 
-    // FIX(patch12): remember which chain tip the current extraNonce belongs to.
+    // remember which chain tip the current extraNonce belongs to.
     // If getblocktemplate returns a new height or previousblockhash, start the
     // extraNonce sequence over at zero. If the same tip is returned, advance
     // extraNonce so we search a different coinbase/merkle-root/header space.
@@ -980,7 +980,7 @@
                     nlohmann::json()
                 );
 
-            // FIX(patch12): determine whether the node gave us a new chain tip.
+            // determine whether the node gave us a new chain tip.
             const int32_t templateHeight =
                 tplJson.value("height", 1);
 
@@ -1064,7 +1064,7 @@
                 sbReq["params"] = paramsObj;
                 auto sbRes = cli.Post("/rpc", sbReq.dump(), "application/json");
 
-                // FIX(patch48): HTTP 200 does NOT mean the node accepted the block.
+                // HTTP 200 does NOT mean the node accepted the block.
                 // TRU's handleSubmitBlockOptimized() always answers 200 and puts
                 // the verdict in an OBJECT result:
                 //     {"result":{"status":"accepted"|"rejected"|"duplicate",
@@ -1124,7 +1124,7 @@
                 }
             }
 
-            // FIX(patch12): do not unconditionally reset extraNonce here.
+            // do not unconditionally reset extraNonce here.
             // Template identity handling above decides whether the next
             // iteration should reset it or advance it.
             std::this_thread::sleep_for(std::chrono::seconds(1));
@@ -1234,10 +1234,10 @@
 
     std::cout << C_FIRE << C_BOLD << "🔥 Starting TRU CPU Miner v4.0..." << C_RESET << "\n";
     
-    // Initialize block count - FIXED
+    // Initialize block count
     uint64_t blockCount = 0;
     
-    // Start mining - FIXED to pass blockCount by reference
+    // Start mining — pass blockCount by reference
     startMiningLoop(nodeIP, nodePort, minerAddr, nThreads, maxNonce, cpuInfo, blockCount);
 
     // Enhanced shutdown message
--- a/src/oobalooga_ai_.h
+++ b/src/oobalooga_ai_.h
@@ -6,13 +6,13 @@
 #include <curl/curl.h>
 #include <nlohmann/json.hpp>
 
-// Adjust this include path to wherever your Wallet class lives:
+// Wallet interface.
 #include "wallet.h"   // must provide Wallet with issueExtendedSFT / issueExtendedNCFT
 
 // ---------- types ----------
 struct OobaConfig {
     std::string baseUrl = "http://127.0.0.1:3002";
-    std::string apiKey  = "878_878";   // from your --api-key
+    std::string apiKey  = "878_878";   // Local provider API key.
     int maxNewTokens    = 256;         // maps to "max_tokens"
     double temperature  = 0.7;
     int top_k           = 40;
@@ -128,7 +128,7 @@
     return meta;
 }
 
-// ---------- top-level issuers using your Wallet ----------
+// Wallet-backed token issuers.
 inline std::string createSFTWithOoba(
         Wallet& wallet,
         const OobaConfig& ooba,
--- a/src/opcodes.h
+++ b/src/opcodes.h
@@ -133,7 +133,7 @@
     OP_NOP10           = 0xb9,
 
     // =====================
-    // Custom "New Age" opcodes for your chain
+    // TRU extended opcodes.
     // =====================
 
     // This pushes the current block time (or block height) to the stack
@@ -190,7 +190,7 @@
     OP_BSTY_VERIFY     = 0xba,  // Verify BSTY transaction proof (new slot)
 
     // =====================
-    // TRU Smart Contract Patch 11C — reserved upgrade NOP space
+    // reserved upgrade NOP space
     // =====================
     // These bytes are consensus-valid no-ops at genesis so they can later
     // be tightened into new functionality by a compatible soft-fork rule.
--- a/src/p2p.cpp
+++ b/src/p2p.cpp
@@ -4,7 +4,7 @@
 #include "network_identity.h"  // MULTINODE-01D local-endpoint self-peer guard
 #include <arpa/inet.h>
 #include <netinet/in.h>
-#include <netinet/tcp.h>   // FIX(patch26): TCP_KEEPIDLE / TCP_KEEPINTVL / TCP_KEEPCNT
+#include <netinet/tcp.h>   // TCP_KEEPIDLE / TCP_KEEPINTVL / TCP_KEEPCNT
 #include <unistd.h>
 #include <fcntl.h>
 #include <stdexcept>
@@ -268,7 +268,7 @@
     int opt = 1;
     setsockopt(sock, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));
 
-    // FIX(patch26): TCP keepalive so a silently-dropped NAT/CGNAT connection
+    // TCP keepalive so a silently-dropped NAT/CGNAT connection
     // (Starlink) is detected in ~30-45s instead of ~2h. Without this, sends to a
     // dead socket black-hole and the chain appears stuck at the last propagated
     // height.
--- a/src/peer_connection.cpp
+++ b/src/peer_connection.cpp
@@ -277,7 +277,7 @@
         return;
     }
 
-    // FIX(patch47): send() is allowed to write only part of a buffer.  That was
+    // send() is allowed to write only part of a buffer.  That was
     // easy to miss with small messages but becomes a real block-relay failure
     // mode once TRU permits tens of MiB per block.  Keep sending until the full
     // framed message is on the socket or a real error occurs.
@@ -601,7 +601,7 @@
                 Logger::log("[PeerConnection::readLoop] Processing message type " + std::to_string(msg.type()) + " from " + ip_ + ":" + std::to_string(port_));
 
                 try {
-                    // Patch 15B.1: magic is checked by deserializeMessage().
+                    // magic is checked by deserializeMessage().
                     // At the application layer, no peer may send chain traffic
                     // until it proves the exact tru-mainnet VERSION identity.
                     if (!networkHandshakeComplete_.load() &&
--- a/src/peer_connection.h
+++ b/src/peer_connection.h
@@ -71,7 +71,7 @@
     std::map<uint64_t, std::promise<Block>> blockPromises_;
     std::string buffer_;
     std::atomic<bool> running_{false};
-    // Patch 15B.1: application messages are forbidden until this peer
+    // application messages are forbidden until this peer
     // proves the exact TRU mainnet VERSION identity.
     std::atomic<bool> networkHandshakeComplete_{false};
     uint64_t peerHeight_;
--- a/src/peer_manager.h
+++ b/src/peer_manager.h
@@ -33,7 +33,7 @@
     std::vector<PeerInfo> getPeers() const;
     void updatePeerActivity(const std::string& ip, int port);
 
-    // Patch 12C.2: connection admission/abuse controls.
+    // connection admission/abuse controls.
     // Acquire before PeerConnection/thread creation and release exactly once.
     bool tryAcquireConnectionSlot(
         const std::string& ip,
--- a/src/rpc_server.cpp
+++ b/src/rpc_server.cpp
@@ -1,7 +1,7 @@
 #include "rpc_server.h"
-#include "rpc_utils.h"  // Patch 05: authenticated RPC transport
+#include "rpc_utils.h"  // authenticated RPC transport
 #include "blockchain.h"
-#include "tru_limits.h"  // FIX(patch47): shared block/template limits
+#include "tru_limits.h"  // shared block/template limits
 #include "block.h"
 #include "tx.h"
 #include "tokens.h"
@@ -18,7 +18,7 @@
 #include "hdwallet.h"
 #include <openssl/rand.h>
 #include "script_interpreter.h"
-#include "contract_call_policy.h"  // Patch 14D2A: stateful creation activation gate
+#include "contract_call_policy.h"  // stateful creation activation gate
 #include "p2p.h"
 #include "smart_contract.h"
 #include "mempool.h"
@@ -32,7 +32,7 @@
 #include <future>
 #include <limits>
 #include <unordered_map>
-#include <unordered_set>   // FIX(patch48): template double-spend guard
+#include <unordered_set>   // template double-spend guard
 #include <chrono>
 #include "ai_provider_interface.h"
 #include "ai_oracle_service.h"
@@ -81,7 +81,7 @@
         hash, hash + SHA256_DIGEST_LENGTH)).substr(0, 16);
 }
 
-// TRU CLI UX PATCH 30B3 — exact human-TRU JSON amount boundary.
+// exact human-TRU JSON amount boundary.
 static bool parseJsonTRUAmount(const json& value, uint64_t& atomsOut, std::string& reason) {
     std::string text;
     if (value.is_string()) {
@@ -120,7 +120,7 @@
     return true;
 }
 
-// TRU Smart Contract Patch 12B — RPC / VM parity gate.
+// RPC / VM parity gate.
 // Browser and external RPC callers submit already-compiled script hex, bypassing
 // compileTextScript(). Run the same consensus preflight here before any contract
 // transaction is constructed so unsupported/malformed bytecode cannot be funded.
@@ -149,7 +149,7 @@
         return false;
     }
 
-    // TRU Smart Contract Patch 14D2A — browser/RPC parity with the native
+    // browser/RPC parity with the native
     // compiler. Stateful locking scripts must not be funded while Patch 14D2
     // keeps every stateful spend deliberately fail-closed.
     const auto statefulScan =
@@ -166,7 +166,7 @@
     return true;
 }
 
-// TRU Smart Contract Patch 13B — creation-time OP_RETURN value policy.
+// creation-time OP_RETURN value policy.
 // Official contract creation derives data-carrier semantics from the script
 // bytes, never from a client-supplied type string. Leading OP_RETURN outputs
 // are unspendable under Patch 13A, so the official creation APIs require zero
@@ -225,7 +225,7 @@
     return base58Encode(payload);
 }
 
-// Patch 15C: removed unused duplicate extractP2PKHAddressFromHexCorrect().
+// removed unused duplicate extractP2PKHAddressFromHexCorrect().
 // RPC script-to-address conversion remains extractP2PKHAddressCorrect().
 struct TokenMetadataSpec {
     std::unordered_set<std::string> required;
@@ -2256,14 +2256,14 @@
             return makeError(-32602, "Invalid sender address: " + err);
         }
         
-        // TRU Smart Contract Patch 12B — browser/RPC creation must pass the
+        // browser/RPC creation must pass the
         // same VM preflight as compileTextScript before funds can be committed.
         std::string scriptPreflightError;
         if (!validateContractScriptHexForVM(scriptHex, scriptPreflightError)) {
             return makeError(-32602, "Invalid scriptHex: " + scriptPreflightError);
         }
 
-        // TRU Smart Contract Patch 13B — determine OP_RETURN from bytecode,
+        // determine OP_RETURN from bytecode,
         // not from the untrusted client-supplied contract type.
         const bool leadingOpReturn = isLeadingOpReturnScriptHex(scriptHex);
         const bool typeClaimsOpReturn =
@@ -2272,7 +2272,7 @@
             contractType == "time-lock" || contractType == "TIME LOCK" ||
             contractType == "TIME_LOCK";
 
-        // Patch 20 v2: the public RPC Time Lock family is timestamp-only.
+        // the public RPC Time Lock family is timestamp-only.
         // Reject malformed or height-domain scripts at creation instead of
         // letting users create outputs that canonical CLTV will later reject.
         if (typeClaimsTimeLock) {
@@ -2347,7 +2347,7 @@
         std::string opReturnHex = "6a" + fmt::format("{:02x}", metaBytes.size()) + bytesToHex(metaBytes);
         tx.vout.emplace_back(0, opReturnHex);
         
-        // TRU Smart Contract Patch 13B — Output 1 is always the requested
+        // Output 1 is always the requested
         // contract/data script. Leading OP_RETURN scripts are zero-value by
         // policy; non-OP_RETURN contracts retain the requested contractAmount.
         // This also fixes the old OP_RETURN path that skipped scriptHex entirely.
@@ -2450,7 +2450,7 @@
             json result = json::object();
             
             for (const auto& tx : mempoolTxs) {
-                // TRU Security Patch 12B: isolate verbose failures per row.
+                // isolate verbose failures per row.
                 try {
                     const auto raw = tx.serializeBinary();
                     const uint64_t fee = tx.computeFee(chain);
@@ -3566,7 +3566,7 @@
                 
                 Logger::log("[handleIssueTokenSigned] Processed metadata: " + metadata.dump());
                 
-                // Patch 16A.0: metadata may travel with the unconfirmed
+                // metadata may travel with the unconfirmed
                 // transaction, but confirmed token state must not be persisted
                 // from RPC/mempool context. applyBlock() owns all durable
                 // tokenMetadata/tokenUTXO/tokenOwnerUTXO/tokenOwnership/
@@ -3582,7 +3582,7 @@
             }
         }
         
-        // Patch 16A.0: never delete confirmed token indexes from RPC context.
+        // never delete confirmed token indexes from RPC context.
         // Spending cleanup is performed by normal confirmed block application
         // and captured by the same mainBatch/U4 transition.
 
@@ -3946,7 +3946,7 @@
     }
 }
 
-// TRU Smart Contract Patch 13B2 — legacy createsmartcontract RPC removed.
+// legacy createsmartcontract RPC removed.
 // Contract creation is centralized in createcontracttransaction, which performs
 // funded UTXO construction, VM preflight, OP_RETURN value policy, and family allow-list checks.
 
@@ -4176,7 +4176,7 @@
 // Get block template
 //========================
 static json handleGetBlockTemplate(Blockchain &chain) {
-    // TRU Security Patch 07:
+    //
     // Hash + height must come from one chain snapshot so the template cannot
     // mix an old parent hash with a new height (or vice versa).
     std::string parentHash;
@@ -4198,7 +4198,7 @@
     // exact previousblockhash advertised above rather than active-chain height.
     gbt["bits"] = chain.calculateExpectedBits(parentHash, nextHeight);
 
-    // FIX(patch47): size-aware template construction.  Previously the RPC put
+    // size-aware template construction.  Previously the RPC put
     // the entire mempool into every template, so a busy mempool could create a
     // candidate larger than consensus allowed.  Size against the same textual
     // transaction representation Block::serialize() ultimately measures and
@@ -4206,7 +4206,7 @@
     auto mempoolTxs = chain.getMempoolTransactions();
     uint64_t totalFees = 0;
 
-    // TRU Security Patch 10: getblocktemplate is a second line of defense.
+    // getblocktemplate is a second line of defense.
     // Even if a malformed/stale entry somehow reaches the pool, fee arithmetic
     // must never wrap and poison every template.
     if (subsidy > tru_limits::MAX_MONEY) {
@@ -4217,7 +4217,7 @@
     size_t assembledBytes = 4096; // conservative header + coinbase + delimiters reserve
     size_t skippedForSize = 0;
 
-    // FIX(patch48): test-validate every mempool transaction against the
+    // test-validate every mempool transaction against the
     // prospective height BEFORE it goes into the template.
     //
     // Previously the whole mempool was copied in unconditionally. One
@@ -4819,7 +4819,7 @@
         std::string h, s, scr;
         std::getline(iss, h, '|');
         std::getline(iss, s, '|');
-        // FIX(patch23b): stop at the script field delimiter so trailing
+        // stop at the script field delimiter so trailing
         // UTXO metadata (for example cb=1) is not appended to scriptPubKey.
         std::getline(iss, scr, '|');
 
@@ -4841,7 +4841,7 @@
         int conf = tip >= bh ? tip - bh + 1 : 0;
         double amt = amount / 1e8;
 
-        // FIX(patch23b): Wallet::rpcListUnspent() consumes this RPC but does
+        // Wallet::rpcListUnspent() consumes this RPC but does
         // not currently inspect the JSON 'spendable' flag. Filter immature
         // coinbase UTXOs here so callers cannot accidentally select one.
         //
@@ -5405,7 +5405,7 @@
         Logger::log("[handleIssueToken] Token details - Type: " + type + ", ID: " + tokenID + 
                    ", Supply: " + std::to_string(supply) + ", Owner: " + address);
         
-        // Patch 16A.0: confirmed token metadata/index state is created only
+        // confirmed token metadata/index state is created only
         // when the transaction is accepted in a block through applyBlock() ->
         // transaction batch -> mainBatch -> authenticated U4 journal.
         //
@@ -6575,7 +6575,7 @@
                                     hasSecret = true;
                                     secretType = metadata["dataType"];
                                     
-                                    // FIXED: Retrieve the stored encryption key from LevelDB
+                                    // Retrieve the stored encryption key from LevelDB
                                     std::string encKey;
                                     LevelDBStorage* storage = chain.getStorage();
                                     if (storage) {
@@ -7918,7 +7918,7 @@
 // Frozen HTLC V1 wallet operations + durable coordinator record RPC.
 //=======================================================================
 static bool truSwapConstantTimeEqual(const std::string& a, const std::string& b) {
-    // Patch 05: use the transport helper's length-oblivious comparison so the
+    // use the transport helper's length-oblivious comparison so the
     // inner swap-token layer does not reintroduce an early length mismatch.
     return tru_rpc::constantTimeEqual(a, b);
 }
--- a/src/script_context_builder.h
+++ b/src/script_context_builder.h
@@ -1,7 +1,7 @@
 #pragma once
 
 #include "script_interpreter.h"
-#include "contract_state_lineage.h"  // Patch 14D1: stable lineage/state key primitives
+#include "contract_state_lineage.h"  // stable lineage/state key primitives
 
 #include <cstddef>
 #include <cstdint>
@@ -9,7 +9,7 @@
 #include <unordered_map>
 #include <vector>
 
-// TRU Smart Contract Patch 14B — shared base execution-context construction.
+// shared base execution-context construction.
 //
 // Every live script execution path must start from the ScriptExecutionContext
 // constructor (which installs all callback defaults) and then populate the same
@@ -22,7 +22,7 @@
     uint64_t gasLimit{1000000};
     const Transaction* tx{nullptr};
     uint32_t blockTime{0};
-    uint32_t medianTimePast{0}; // Patch 20 v2: parent-chain MTP for CLTV only
+    uint32_t medianTimePast{0}; // parent-chain MTP for CLTV only
     size_t inputIndex{0};
     const std::vector<unsigned char>* scriptPubKey{nullptr};
     std::vector<unsigned char> sighash;
--- a/src/script_interpreter.cpp
+++ b/src/script_interpreter.cpp
@@ -27,9 +27,9 @@
 #include <openssl/rand.h>
 #include <openssl/ripemd.h>
 #include "contract_storage.h"
-#include "contract_call_policy.h"  // Patch 14D2A: stateful creation activation gate
-#include "contract_state_limits.h" // Patch 14D2B: consensus state resource bounds
-#include "contract_state_lineage.h" // Patch 14D2B1: canonical state-domain identity
+#include "contract_call_policy.h"  // stateful creation activation gate
+#include "contract_state_limits.h" // consensus state resource bounds
+#include "contract_state_lineage.h" // canonical state-domain identity
 #include <mutex>
 //==================================================================
 // Helper function for SHA3-256 hashing
@@ -61,7 +61,7 @@
     return std::vector<unsigned char>(hash, hash + 32);
 }
 //==================================================================
-// Patch 18 — external/off-chain data is never a consensus input.
+// external/off-chain data is never a consensus input.
 // OP_EXTERNALDATA therefore fails closed unless a future protocol defines a
 // deterministic, authenticated representation. AI inference remains outside
 // Proof-of-Work consensus and is anchored separately by ordinary transactions.
@@ -96,7 +96,7 @@
         // No chain context (simulation / mempool) -> caller fails closed.
         return {};
     }
-    // Patch 14B: execHeight is an explicit execution-context field. Height 0
+    // execHeight is an explicit execution-context field. Height 0
     // is genesis and must never be mistaken for "unset" and replaced by the
     // node's live tip. Every live chain-backed execution path sets execHeight.
     const uint64_t h = static_cast<uint64_t>(ctx.execHeight);
@@ -133,7 +133,7 @@
 }
 
 //============================================================
-// TRU Smart Contract Patch 18 — canonical deterministic Oracle Feed V1.
+// canonical deterministic Oracle Feed V1.
 //
 // Consensus MUST NOT fetch HTTP/API/AI/local-config data during script
 // execution. Oracle Feed V1 therefore exposes only values derived from the
@@ -247,7 +247,7 @@
         case OP_CHECKMULTISIGVERIFY:
 
         // TRU contract opcodes.
-        // Patch 16: OP_MINT_TOKEN remains byte-reserved but unsupported.
+        // OP_MINT_TOKEN remains byte-reserved but unsupported.
         // Token Issuer V1 uses the checked family transition and economic
         // continuation delta instead of the legacy output-value opcode path.
         case OP_BLOCKTIME:
@@ -266,7 +266,7 @@
         case OP_REVERT:
         case OP_OUTPUTAMOUNT:
 
-        // TRU Smart Contract Patch 14C1 — legacy bridge containment.
+        // legacy bridge containment.
         //
         // OP_NOVO_* / OP_BSTY_* are intentionally NOT supported at genesis.
         // Their legacy implementations generate fresh random key material during
@@ -375,7 +375,7 @@
     const std::vector<std::vector<unsigned char>>& stack,
     size_t& totalBytes)
 {
-    // TRU Security Patch 12A v2: scan inherited stack exactly once.
+    // scan inherited stack exactly once.
     // Every later mutation is accounted incrementally in O(1).
     if (stack.size() > tru_limits::MAX_STACK_ITEMS) {
         Logger::log("[EvaluateScript] stack item-count limit exceeded");
@@ -397,7 +397,7 @@
     return true;
 }
 
-// TRU Smart Contract Patch 14C — canonical per-contract state namespace.
+// canonical per-contract state namespace.
 // ctx.state always points at the state map for ctx.contractAddress, whose
 // canonical identity is the locking outpoint "<txid>:<vout>". Therefore the
 // inner map stores raw logical keys exactly once; never prefix them again.
@@ -888,7 +888,7 @@
                 return false;
             }
 
-            // TRU Security Patch 11: TRU currently supports SIGHASH_ALL only.
+            // TRU currently supports SIGHASH_ALL only.
             if (signature.back() != 0x01)
             {
                 Logger::log(
@@ -932,7 +932,7 @@
         }
     };
 
-    // Patch 18: OP_DATAFEED is consensus-owned and is resolved directly from
+    // OP_DATAFEED is consensus-owned and is resolved directly from
     // ctx.chainCtx/ctx.execHeight; no callback can substitute local data.
 
     delegateCheckFunc = [](const std::vector<unsigned char>& sig) {
@@ -953,7 +953,7 @@
         return computeSHA3(in);
     };
 
-    // Patch 18: OP_EXTERNALDATA has no pluggable consensus callback.
+    // OP_EXTERNALDATA has no pluggable consensus callback.
 }
 
 //==================================================================
@@ -1259,7 +1259,7 @@
                         break;
                     }
 
-                    // TRU Smart Contract Patch 11A — OP_0 / OP_FALSE.
+                    // OP_0 / OP_FALSE.
                     // Pushes the canonical empty vector. Under TRU truth semantics,
                     // an empty vector is false. OP_FALSE aliases OP_0 at 0x00.
                     case OP_0:
@@ -1292,7 +1292,7 @@
                         return false;
                     }
 
-                    // TRU Smart Contract Patch 11B — OP_RESERVED is invalid.
+                    // OP_RESERVED is invalid.
                     // Defense-in-depth: execution fails immediately. Patch 11D will also
                     // reject OP_RESERVED during path-independent script preflight, including dead branches.
                     case OP_RESERVED:
@@ -1331,7 +1331,7 @@
                             return false;
                         }
 
-                        // Patch 20 v2: TRU Time Lock V1 is timestamp-only.
+                        // TRU Time Lock V1 is timestamp-only.
                         // Values below 500,000,000 are height-domain values in
                         // Bitcoin-style locktime semantics. TRU does not expose a
                         // height-lock family, so accepting them here would turn a
@@ -1443,7 +1443,7 @@
                         break;
                     }
 
-                    // TRU Smart Contract Patch 04 — OP_ADD uint64 normalization
+                    // OP_ADD uint64 normalization
                     // Unsigned 64-bit little-endian addition with deterministic overflow failure.
                     case OP_ADD:
                     {
@@ -1481,7 +1481,7 @@
                         break;
                     }
 
-                    // TRU Smart Contract Patch 03 — OP_MUL
+                    // OP_MUL
                     // Unsigned 64-bit little-endian multiplication with deterministic overflow failure.
                     case OP_MUL:
                     {
@@ -1519,7 +1519,7 @@
                         break;
                     }
 
-                    // TRU Smart Contract Patch 06 — OP_LESSTHAN
+                    // OP_LESSTHAN
                     // Unsigned numeric comparison using the shared 0..8 byte little-endian uint64 convention.
                     // Stack order is [lhs, rhs] with rhs on top; result is canonical 0x01 / 0x00.
                     case OP_LESSTHAN:
@@ -1553,7 +1553,7 @@
                         break;
                     }
 
-                    // TRU Smart Contract Patch 08 — OP_GREATERTHAN
+                    // OP_GREATERTHAN
                     // Unsigned numeric comparison using the shared 0..8 byte little-endian uint64 convention.
                     // Stack order is [lhs, rhs] with rhs on top; result is canonical 0x01 / 0x00.
                     case OP_GREATERTHAN:
@@ -1587,7 +1587,7 @@
                         break;
                     }
 
-                    // TRU Smart Contract Patch 18 — inclusive numeric comparisons.
+                    // inclusive numeric comparisons.
                     // Same uint64 LE convention and [lhs,rhs] stack order as
                     // OP_LESSTHAN / OP_GREATERTHAN.
                     case OP_LESSTHANOREQUAL:
@@ -1625,7 +1625,7 @@
                         break;
                     }
 
-                    // TRU Smart Contract Patch 02 — OP_VERIFY
+                    // OP_VERIFY
                     // Pops one item and fails the script when it is false.
                     // Truth semantics intentionally match OP_IF/OP_NOTIF.
                     case OP_VERIFY:
@@ -1651,7 +1651,7 @@
                         break;
                     }
 
-                    // TRU Smart Contract Patch 01 — OP_EQUAL
+                    // OP_EQUAL
                     // Pops the top two byte-vectors and pushes a canonical
                     // boolean result: 0x01 when equal, 0x00 when unequal.
                     case OP_EQUAL:
@@ -2007,7 +2007,7 @@
 
                     case OP_EXTERNALDATA:
                     {
-                        // Patch 18: external/off-chain values are not deterministic
+                        // external/off-chain values are not deterministic
                         // consensus inputs. This opcode is reserved but fails closed.
                         Logger::log("[EvaluateScript] ERROR: OP_EXTERNALDATA is not active in deterministic consensus");
                         return false;
@@ -2023,7 +2023,7 @@
                         std::string keyStr(key.begin(), key.end());
                         Logger::log("[EvaluateScript] OP_DATAFEED: Fetching data for key=" + keyStr);
 
-                        // Patch 18: one deterministic consensus feed path.
+                        // one deterministic consensus feed path.
                         // Never consult HTTP, AI providers, local config, wall clock,
                         // process memory, or a replaceable callback here.
                         auto data = oracleFeedDeterministic(keyStr, ctx);
@@ -2129,7 +2129,7 @@
                         break;
                     }
 
-                    // TRU Smart Contract Patch 14C2 — defense-in-depth bridge kill switch.
+                    // defense-in-depth bridge kill switch.
                     // 14C1 removes these bytes from IsSupportedOpcode, so normal script
                     // preflight rejects them path-independently. Keep an explicit execution
                     // failure as a second containment layer: if a future refactor ever
@@ -2228,7 +2228,7 @@
 
 #endif
 
-                    // TRU Smart Contract Patch 11C — reserved upgrade NOP space.
+                    // reserved upgrade NOP space.
                     // 0xbb..0xc2 are consensus-valid no-ops at genesis. Future
                     // upgrades may assign stricter semantics without first making
                     // previously-invalid bytecode valid. These are intentionally
@@ -2307,7 +2307,7 @@
                         break;
                     }
 
-                    // TRU Smart Contract Patch 09 — OP_CAT
+                    // OP_CAT
                     // Concatenates the second stack item with the top item: [A, B] -> [A || B].
                     // Result size is bounded by the consensus stack-item ceiling before allocation.
                     case OP_CAT:
@@ -2347,7 +2347,7 @@
                         break;
                     }
 
-                    // TRU Smart Contract Patch 10 — correct OP_LEFT opcode binding.
+                    // correct OP_LEFT opcode binding.
                     // opcodes.h defines OP_SUBSTR=0x7f and OP_LEFT=0x80; this implementation
                     // performs left-N-bytes semantics, so it belongs to OP_LEFT. OP_SUBSTR
                     // remains unsupported until it receives its own explicit implementation.
@@ -2417,7 +2417,7 @@
                             return false;
                         }
 
-                        // Patch 16 — preflight both duplicated items before any
+                        // preflight both duplicated items before any
                         // stack mutation. This keeps stackBytes/item-count
                         // accounting atomic at the resource boundary.
                         if (stack.size() >
@@ -2458,7 +2458,7 @@
 
                     case OP_MINT_TOKEN:
                     {
-                        // Patch 16 — fail closed. The legacy opcode used
+                        // fail closed. The legacy opcode used
                         // ctx.tx->vout[ctx.outputIndex].amount and a
                         // "tokens:<address>" namespace. Neither is Token Issuer
                         // V1 consensus truth.
@@ -2570,7 +2570,7 @@
         return false;
     }
 
-    // TRU Smart Contract Patch 05 — canonical final truth test.
+    // canonical final truth test.
     // Consensus truth semantics match OP_IF / OP_VERIFY:
     // empty or first byte 0x00 => false; first byte non-zero => true.
     const bool isTrue = !stack.back().empty() && stack.back()[0] != 0;
@@ -2624,7 +2624,7 @@
         {"OP_CHECKSIG", OP_CHECKSIG}, {"OP_CHECKSIGVERIFY", OP_CHECKSIGVERIFY},
         {"OP_CHECKMULTISIG", OP_CHECKMULTISIG},
         {"OP_CHECKMULTISIGVERIFY", OP_CHECKMULTISIGVERIFY},
-        // TRU Smart Contract Patch 12 — compiler/VM parity.
+        // compiler/VM parity.
         // Legacy OP_NOP1 and OP_NOP4..OP_NOP10 text aliases are intentionally
         // not exposed. OP_NOP4..OP_NOP10 alias live bridge opcodes and must
         // never masquerade as harmless no-ops in user-authored scripts.
@@ -2644,7 +2644,7 @@
         {"OP_GAS", OP_GAS},
         {"OP_HALT", OP_HALT},
         {"OP_REVERT", OP_REVERT},
-        // Patch 16: OP_MINT_TOKEN is byte-reserved but not text-compilable.
+        // OP_MINT_TOKEN is byte-reserved but not text-compilable.
         {"OP_TOKEN_BALANCE", OP_TOKEN_BALANCE},
         {"OP_BURN_TOKEN", OP_BURN_TOKEN},
         {"OP_OUTPUTAMOUNT", OP_OUTPUTAMOUNT},
@@ -2733,7 +2733,7 @@
         }
     }
 
-    // TRU Smart Contract Patch 12 — creation-time consensus parity.
+    // creation-time consensus parity.
     // Never return bytecode that consensus preflight would reject. This also
     // applies the same script size/opcount/sigop ceilings before contract
     // creation, preventing users from funding an unusable custom script.
@@ -2743,7 +2743,7 @@
             "[compileTextScript] Compiled script rejected by TRU VM preflight");
     }
 
-    // TRU Smart Contract Patch 14D2A — creation/spend activation parity.
+    // creation/spend activation parity.
     // Patch 14D2 deliberately keeps stateful spends fail-closed. Do not let
     // native/CLI creation fund an output that cannot yet be spent. 14D3 lifts
     // this gate only together with durable state + lineage activation.
--- a/src/script_interpreter.h
+++ b/src/script_interpreter.h
@@ -10,7 +10,7 @@
 
 struct ScriptExecutionContext {
     uint32_t blockTime;                             // Candidate/header or admission time for OP_BLOCKTIME
-    // Patch 20 v2: parent-chain Median-Time-Past (MTP) is a separate
+    // parent-chain Median-Time-Past (MTP) is a separate
     // consensus clock. OP_CHECKLOCKTIMEVERIFY uses this field; it must never
     // substitute the candidate header timestamp or a node-local wall clock.
     uint32_t medianTimePast{0};
@@ -35,13 +35,13 @@
     std::function<bool(const std::vector<unsigned char>& pubkey,
                        const std::vector<unsigned char>& signature,
                        const std::string& message)> signatureCheckFunc;
-    // Patch 18: OP_DATAFEED has no pluggable callback. Consensus feed reads
+    // OP_DATAFEED has no pluggable callback. Consensus feed reads
     // are resolved directly from deterministic execution context.
     std::function<bool(const std::vector<unsigned char>&)> delegateCheckFunc;
     std::function<std::vector<unsigned char>()> chainStateFunc;
     std::function<std::vector<unsigned char>(const std::vector<unsigned char>&)> blake2bFunc;
     std::function<std::vector<unsigned char>(const std::vector<unsigned char>&)> sha3Func;
-    // Patch 18: OP_EXTERNALDATA has no consensus callback and fails closed.
+    // OP_EXTERNALDATA has no consensus callback and fails closed.
     std::vector<bool> execStack; // Tracks execution state for conditional blocks
 
     //ScriptExecutionContext();
@@ -59,7 +59,7 @@
     }
 };
 
-// TRU Security Patch 12A: deterministic script resource accounting.
+// deterministic script resource accounting.
 struct ScriptResourceMetrics {
     uint64_t opCount = 0;
     uint64_t sigOpCost = 0;
--- a/src/smart_contract.cpp
+++ b/src/smart_contract.cpp
@@ -18,7 +18,7 @@
 #include <limits> 
 #include <cctype>
 #include "mempool.h"
-#include "script_context_builder.h"  // Patch 14B: shared execution context
+#include "script_context_builder.h"  // shared execution context
 
 namespace SmartContract {
 
@@ -836,7 +836,7 @@
     }
 }
 
-// Add this function to smart_contract.cpp
+// Smart-contract helper.
 bool spendTimeLockContract(
     Wallet& wallet,
     Blockchain& chain,
@@ -845,7 +845,7 @@
     const std::string& recipientAddress,
     uint64_t fee)
 {
-    // Patch 20: retain the legacy API surface, but remove its independent
+    // retain the legacy API surface, but remove its independent
     // wall-clock/signing/broadcast implementation. Canonical Time Lock spends
     // are now constructed by Wallet::redeemTimeLock(), which is also used by
     // the CLI and RPC path. recipientAddress/fee were never consensus fields;
--- a/src/smart_contract.h
+++ b/src/smart_contract.h
@@ -10,7 +10,7 @@
 
 namespace SmartContract {
 
-// Add this struct definition before the queryContractState function
+// Contract-state query result.
 struct ContractInfo {
     std::string identifier;
     std::string type;
--- a/src/token_evolution.cpp
+++ b/src/token_evolution.cpp
@@ -664,7 +664,7 @@
     evolved["ai_engine"] = providerName;
     evolved["evolution_epoch"] = std::to_string(newEpoch);
 
-    // FIX(patch21): provenance time is set by TRU, not by the AI model.
+    // provenance time is set by TRU, not by the AI model.
     const uint64_t evolutionTimestamp =
         static_cast<uint64_t>(std::time(nullptr));
 
--- a/src/token_evolution_cli.cpp
+++ b/src/token_evolution_cli.cpp
@@ -428,7 +428,7 @@
         if (!issuanceTxid.empty()) {
             std::cout << "Issuance : " << issuanceTxid << "\n";
         }
-        // FIX(patch21): show which state will actually be evolved.
+        // show which state will actually be evolved.
         json latestState = engine.loadLatest(tokenID);
 
         if (!latestState.empty() &&
--- a/src/tokens.cpp
+++ b/src/tokens.cpp
@@ -161,7 +161,7 @@
                 Logger::log("[getUTXOsForAddress] WARNING: malformed UTXO data for " + fullKey + ": " + raw);
                 return;
             }
-            // FIX(patch28): parse the amount defensively per-record. A single
+            // parse the amount defensively per-record. A single
             // malformed record must NOT throw out of the whole scan (which would
             // make the entire address enumerate nothing and show a 0 balance).
             uint64_t amt = 0;
@@ -746,7 +746,7 @@
     return true;
 }
 
-// Add this new function to tokens.cpp
+// Token helper.
 bool parseTRUScriptFromOPReturn(const std::string& scriptPubKeyHex, const std::string& txid,
                                nlohmann::json& truScriptData, const Blockchain* blockchainPtr) {
     Logger logger;
--- a/src/tru-cli.cpp
+++ b/src/tru-cli.cpp
@@ -5,9 +5,9 @@
 //  like the standalone miners do (httplib + nlohmann::json, endpoint "/rpc",
 //  body {jsonrpc, method, params, id}).
 //
-//  BUILD (see CMake snippet delivered alongside this file):
-//      add_executable(tru-cli src/tru-cli.cpp)
-//      target_link_libraries(tru-cli ...)   # same libs your other tools use
+// Build target: tru-cli (configured in CMakeLists.txt).
+//
+//
 //
 //  USAGE:
 //      tru-cli [options] <command> [args...]
@@ -47,7 +47,7 @@
 // ============================================================================
 
 #include "tru_network_params.h"
-#include "rpc_utils.h"  // Patch 05: Bearer/cookie RPC authentication
+#include "rpc_utils.h"  // Bearer/cookie RPC authentication
 #include <iostream>
 #include <fstream>
 #include <sstream>
--- a/src/tru_limits.h
+++ b/src/tru_limits.h
@@ -5,7 +5,7 @@
 #include <cstddef>
 #include <cstdint>
 
-// FIX(patch47): one source of truth for TRU block/network size limits.
+// one source of truth for TRU block/network size limits.
 // Keep the hard consensus limit separate from the miner/template assembly
 // budget so block-level metadata still has headroom before validation.
 namespace tru_limits {
@@ -13,7 +13,7 @@
 inline constexpr std::size_t BLOCK_ASSEMBLY_BYTES = 56ULL * 1024ULL * 1024ULL;  // 56 MiB tx-selection budget
 inline constexpr std::size_t MAX_P2P_MESSAGE_BYTES = 80ULL * 1024ULL * 1024ULL; // 80 MiB absolute framed payload cap
 
-// TRU Security Patch 12C.1: P2P framing/request ceilings (NODE POLICY).
+// P2P framing/request ceilings (NODE POLICY).
 // BLOCK needs the existing 80 MiB envelope because consensus blocks may be
 // 64 MiB and protobuf framing/metadata need headroom. Other message classes
 // are intentionally much smaller so an attacker cannot force 80 MiB protobuf
@@ -29,7 +29,7 @@
 inline constexpr std::size_t MAX_P2P_ADDRESS_STRING_BYTES = 128;
 inline constexpr std::size_t MAX_P2P_INVENTORY_ITEM_BYTES = 66; // "b:"/"t:" + 64 hex chars
 
-// TRU Security Patch 12C.2: peer abuse/rate-limit policy (NODE POLICY).
+// peer abuse/rate-limit policy (NODE POLICY).
 // Connection admission is per source IP and occurs before PeerConnection/thread
 // creation. Runtime token buckets preserve enough burst capacity for one
 // maximum-size block while bounding sustained peer traffic and request abuse.
@@ -58,7 +58,7 @@
     P2P_INBOUND_REQUEST_BURST >= P2P_SYNC_BLOCK_REQUEST_BATCH,
     "P2P request burst must cover one sync batch");
 
-// TRU Security Patch 12C.3a: locally-owned outstanding block-request bounds.
+// locally-owned outstanding block-request bounds.
 inline constexpr std::size_t MAX_P2P_INFLIGHT_BLOCK_REQUESTS_PER_PEER =
     P2P_SYNC_BLOCK_REQUEST_BATCH + 28; // 128
 static_assert(
@@ -67,7 +67,7 @@
 
 inline constexpr std::uint32_t P2P_PEER_REAP_INTERVAL_SECONDS = 5;
 
-// TRU Security Patch 12C.3b: untrusted-source side-fork quotas.
+// untrusted-source side-fork quotas.
 // Global side-pool bounds remain in blockchain.cpp. These source quotas ensure
 // one peer IP (or the RPC/legacy-network bucket) cannot monopolize that pool.
 inline constexpr std::size_t MAX_SIDE_BLOCKS_PER_UNTRUSTED_SOURCE = 64;
@@ -82,12 +82,12 @@
 inline constexpr std::uint32_t P2P_SCORE_DECAY_SECONDS = 10 * 60;
 inline constexpr std::size_t MAX_P2P_ABUSE_TABLE_ENTRIES = 4096;
 
-// TRU Security Patch 10: one source of truth for monetary and mempool policy
+// one source of truth for monetary and mempool policy
 // bounds. MAX_MONEY mirrors the consensus value already enforced by
 // validateBlock(); the mempool/queue limits are NODE POLICY, not consensus.
 inline constexpr std::uint64_t MAX_MONEY = 21000000ULL * 100000000ULL;
 
-// TRU Security Patch 12A: consensus execution/resource ceilings.
+// consensus execution/resource ceilings.
 // These are deliberately shared by block validation and mempool validation so
 // a locally-relayed transaction cannot become consensus-invalid when mined.
 inline constexpr std::size_t MAX_CONSENSUS_TX_BYTES = 4ULL * 1024ULL * 1024ULL; // 4 MiB / tx
@@ -114,7 +114,7 @@
 inline constexpr std::size_t MAX_TX_QUEUE_BYTES = 16ULL * 1024ULL * 1024ULL;     // pre-validation queue
 inline constexpr std::size_t MAX_TX_QUEUE_TXS = 1024;
 
-// TRU Security Patch 12B: NODE POLICY ONLY, not block consensus.
+// NODE POLICY ONLY, not block consensus.
 // One TRU atom per serialized byte blocks free/zero-fee mempool pinning.
 // Every direct/queued/RPC/internal Mempool::addTransaction() call shares the
 // same bounded script-validation concurrency gate.
--- a/src/tru_network_params.h
+++ b/src/tru_network_params.h
@@ -8,18 +8,18 @@
 
 namespace tru_network {
 
-// Patch 15B.1: immutable mainnet wire identity.
+// immutable mainnet wire identity.
 // Byte order on the wire is exactly EC 7C 8F B9.
 inline constexpr std::array<std::uint8_t, 4> MAINNET_P2P_MAGIC = {
     0xec, 0x7c, 0x8f, 0xb9
 };
 inline constexpr std::string_view MAINNET_NETWORK_ID = "tru-mainnet";
 
-// Patch 15B.2: canonical TRU mainnet service defaults.
+// canonical TRU mainnet service defaults.
 inline constexpr std::uint16_t MAINNET_RPC_PORT = 21832;
 inline constexpr std::uint16_t MAINNET_P2P_PORT = 21833;
 
-// Patch 15A: TRU-specific Base58Check P2PKH namespace.
+// TRU-specific Base58Check P2PKH namespace.
 // Version 0x41 yields 34-character mainnet addresses beginning with 'T'.
 inline constexpr std::uint8_t MAINNET_P2PKH_VERSION = 0x41;
 
--- a/src/tx.cpp
+++ b/src/tx.cpp
@@ -1,6 +1,6 @@
 
 #include "tx.h"
-#include "tru_limits.h"  // TRU Security Patch 10: shared MAX_MONEY
+#include "tru_limits.h"  // shared MAX_MONEY
 #include <sstream>
 #include <iomanip>
 #include <stdexcept>
@@ -26,7 +26,7 @@
 }
 
 uint64_t Transaction::computeFee(const Blockchain& chain) const {
-    // TRU Security Patch 10: fee calculation is used by getblocktemplate and
+    // fee calculation is used by getblocktemplate and
     // must mirror block-validation duplicate-input / MAX_MONEY invariants.
     if (isCoinbase) {
         throw std::runtime_error("Coinbase transaction has no mempool fee");
@@ -285,7 +285,7 @@
 
 
 void Transaction::computeTxId() {
-    // FIX(patch51): the transaction ID must NOT depend on scriptSig.
+    // the transaction ID must NOT depend on scriptSig.
     //
     // serializeBinary() writes each input's scriptSig. That is correct for
     // the wire format, but it meant computeTxId() returned one value before
@@ -382,7 +382,7 @@
     const std::vector<unsigned char> scriptPubKey =
         hexDecode(scriptPubKeyHex);
 
-    // TRU Security Patch 11: one sighash implementation only.
+    // one sighash implementation only.
     const std::vector<unsigned char> sighashBytes =
         getSigHash(index, scriptPubKey);
     const std::string sighash(
@@ -424,12 +424,12 @@
     return true;
 }
 // -----------------------------------------------------------------------------
-// TRU Security Patch 11:
+//
 // The old signInputRpc() ASCII "SIGHASH|txid|index" algorithm was removed.
 // Transaction::getSigHash() is the single transaction-signature digest.
 // -----------------------------------------------------------------------------
 // -----------------------------------------------------------------------------
-// TRU Security Patch 12A:
+//
 // Removed unused verifyInput()/verifyInputScript() alternate verification paths.
 // Consensus and mempool validation use VerifyScripts() exclusively.
 // -----------------------------------------------------------------------------
@@ -465,7 +465,7 @@
         oss << "|" << out.amount << "|" << out.scriptPubKey;
     }
     
-    // TRU Smart Contract Patch 20C:
+    //
     // The legacy text/block transaction representation omitted version and
     // lockTime. Keep byte-for-byte legacy output for ordinary v1/lockTime=0
     // transactions, but carry the core fields whenever either is non-default.
@@ -579,7 +579,7 @@
         tx.vout.push_back(out);
     }
     
-    // TRU Smart Contract Patch 20C:
+    //
     // Parse optional tagged transaction-core fields plus the pre-existing
     // metadata trailer. Historical transactions have no TXCORE field and
     // therefore retain the constructor defaults version=1 / lockTime=0.
--- a/src/utils.cpp
+++ b/src/utils.cpp
@@ -1,5 +1,5 @@
 #include "utils.h"
-#include <cstdlib>   // FIX(patch27): std::getenv
+#include <cstdlib>   // std::getenv
 #include <iostream>
 #include <sys/ioctl.h>
 #include <unistd.h>
@@ -182,7 +182,7 @@
     }
 }
 //=====================================================================
-// Add this helper function above startCLI in main.cpp
+// Shared CLI helper.
 //=====================================================================
 void displayOutputProgressive(std::string& outputBuffer, const std::string& message, int& currentRow, int rows, std::mutex& coutMutex, int color, bool addSpacing) {
     std::lock_guard<std::mutex> lock(coutMutex);
@@ -242,7 +242,7 @@
             Logger::log("[loadWalletSeed] Loaded seed of size: " + std::to_string(seed.size()));
         }
     } else {
-        // FIX(patch27): guard against SILENTLY minting a new wallet over an
+        // guard against SILENTLY minting a new wallet over an
         // existing funded chain. If chain data already exists, a missing seed
         // almost always means the real seed was lost/moved — generating a new
         // one here would orphan all prior coinbase rewards (balance reads 0).
--- a/src/utils.h
+++ b/src/utils.h
@@ -122,7 +122,7 @@
 }
 
 //========================================================
-// extractTargetSmart declaration (if you want it global)
+// Shared target extraction helper.
 //========================================================
 std::string extractTargetSmart(const std::string& scriptHex);
 // ------------------------------------------------
--- a/src/utxo.cpp
+++ b/src/utxo.cpp
@@ -21,7 +21,7 @@
 static std::string extractAddressFromScriptTail(const std::string& scriptHex);
 static bool looksLikeMagicLockScript(const std::string& scriptHex);
 
-// REPLACE your entire getUTXOsForAddress(...) with this version
+// Collect UTXOs owned by an address.
 std::vector<UTXO> UTXOSet::getUTXOsForAddress(const std::string& address) const {
     std::vector<UTXO> result;
 
@@ -61,9 +61,9 @@
 
         // Fallbacks if needed
         if (utxoAddress.empty()) {
-            // If you have a helper that decodes address straight from script:
+            // Reserved fallback: direct script address decoding (not implemented here).
             // utxoAddress = extractAddressFromScriptPubKey(utxo.scriptPubKey, txid, vout, blockchainPtr);
-            // Or last-chance fallback if you keep a “tail” heuristic:
+            // Reserved fallback: script-tail address decoding (not implemented here).
             // utxoAddress = extractAddressFromScriptTail(utxo.scriptPubKey);
         }
 
@@ -501,7 +501,7 @@
 
             if (addr.empty())
             {
-                // TRU Smart Contract Patch 19A — canonical addressless contract UTXOs.
+                // canonical addressless contract UTXOs.
                 //
                 // Hash Lock V1 intentionally has no embedded P2PKH address:
                 //     OP_HASH160 <20-byte HASH160> OP_EQUAL
@@ -587,7 +587,7 @@
             uint64_t amount = std::stoull(utxoVal.substr(firstDelim + 1, secondDelim - firstDelim - 1));
             std::string scriptPubKey = utxoVal.substr(secondDelim + 1);
 
-            // FIX(patch50): drop the persisted coinbase flag before address
+            // drop the persisted coinbase flag before address
             // extraction.
             //
             // UTXO records are written (see storeUTXO, ~line 272) as:
@@ -632,7 +632,7 @@
                 addr = extractAddressFromScriptTail(scriptPubKey);
             }
 
-            // FIX(patch50): this function used to fail silently. A UTXO whose
+            // this function used to fail silently. A UTXO whose
             // address could not be resolved was simply never counted, with no
             // trace anywhere, which is exactly why the explorer/CLI balance
             // gap was so hard to localise. Say something.
@@ -714,13 +714,13 @@
     return utxo;
 }
 //-----------------------------------------------------------------------------
-// In production, we need to replace this stub with proper cryptographic verification
+// Unimplemented cryptographic verification; this stub requires replacement before production use.
 // (using OpenSSL, libsodium, etc.).
 //------------------------------------------------------------------------------
 static bool verifyMetadataSignature(const std::string &newMetadataJson,
                                     const std::string &cryptoSignature)
 {
-    // For demonstration purposes we always return true.
+    // Demonstration stub: verification unconditionally succeeds.
     return true;
 }
 
@@ -747,7 +747,7 @@
         }
         parts.push_back(raw.substr(start));
 
-        // FIX(patch23b): fields after parts[2] are persisted metadata
+        // fields after parts[2] are persisted metadata
         // such as cb=1. They must not cause a valid UTXO to be discarded.
         if (parts.size() < 3) {
             Logger::log("[getAllUTXOs] ⚠️  Unexpected format (need at least 3 parts): got " 
@@ -816,7 +816,7 @@
 
 UTXOSet::UTXOSet(const std::string &dbPath, Blockchain* blockchainPtr, bool loadRegistry)
     : dbStorage(dbPath), blockchainPtr(blockchainPtr) {
-    // Patch 08B.3b: validation sandboxes query the copied LevelDB directly and
+    // validation sandboxes query the copied LevelDB directly and
     // never consult seenTokens. Avoid a full-database registry scan/materialize
     // on every sandbox construction.
     if (loadRegistry) loadTokenRegistry();
--- a/src/wallet.cpp
+++ b/src/wallet.cpp
@@ -196,7 +196,7 @@
 
 
 // ============================================================================
-// TRU Security Patch 12B.1: size-aware wallet fee compatibility
+// size-aware wallet fee compatibility
 // ============================================================================
 // Patch 12B admits unconfirmed transactions only when fee >= serialized bytes
 // * MIN_RELAY_FEE_SAT_PER_BYTE. Preserve the historical 10,000-TRU-atom wallet
@@ -280,7 +280,7 @@
     return std::max<uint64_t>(WALLET_MIN_BASE_FEE, policyFee);
 }
 
-// TRU Smart Contract Patch 19 — canonical Hash Lock helpers.
+// canonical Hash Lock helpers.
 static bool parseCanonicalHashLockScriptHex(
     const std::string& scriptHex,
     std::string* expectedHash160Hex = nullptr)
@@ -313,7 +313,7 @@
     return true;
 }
 
-// TRU Smart Contract Patch 20 — canonical Time Lock helper.
+// canonical Time Lock helper.
 // Exact script: 04<4-byte little-endian unix time>b17576a914<20-byte HASH160>88ac
 static bool parseCanonicalTimeLockScriptHex(
     const std::string& scriptHex,
@@ -885,7 +885,7 @@
         throw std::runtime_error("[Wallet] addTransaction => no local chain pointer");
     }
 
-    // TRU Security Patch 10 v2: Blockchain::addTransaction() now performs
+    // Blockchain::addTransaction() now performs
     // bounded queue admission and can reject. Preserve Wallet's existing void
     // API, but make rejection visible to every caller by throwing instead of
     // silently pretending the transaction was accepted.
@@ -3187,7 +3187,7 @@
             Logger::log("[send_transaction] Retrieved " + std::to_string(utxos.size()) + " UTXOs via RPC for " + sender);
         }
 
-        // FIX(patch54-safe-spend): apply consensus spendability rules before
+        // apply consensus spendability rules before
         // ordinary TRU input selection.
         std::vector<UTXO> availableUtxos;
         if (isLocalChain && blockchainPtr) {
@@ -3231,7 +3231,7 @@
                     }
                 }
 
-                // FIX(patch55-standard-tru-utxo): ordinary TRU sends may
+                // ordinary TRU sends may
                 // spend only canonical P2PKH coin outputs owned by `sender`.
                 // Protocol/data/control outputs can carry TRU atoms too, but
                 // must never be treated as generic spendable TRU.
@@ -3284,7 +3284,7 @@
                 continue;
             }
 
-            // FIX(patch55-standard-tru-utxo): defense in depth.
+            // defense in depth.
             const std::string& selectedSpk = u.scriptPubKey;
             const bool selectedIsP2PKH =
                 selectedSpk.size() == 50 &&
@@ -3358,7 +3358,7 @@
         std::string txHex = bytesToHex(tx.serializeBinary());
         Logger::log("[send_transaction] Serialized transaction hex: " + txHex);
 
-        // FIX(patch54-safe-spend): success means accepted by the mempool,
+        // success means accepted by the mempool,
         // never merely queued for later validation.
         if (isLocalChain) {
             if (!blockchainPtr || !blockchainPtr->mempool) {
@@ -5492,7 +5492,7 @@
                        scriptPubKey[9] == 0x14 &&
                        scriptPubKey[30] == OP_EQUALVERIFY &&
                        scriptPubKey[31] == OP_CHECKSIG) {
-                // Patch 20: Time Lock retains a normal P2PKH ownership tail.
+                // Time Lock retains a normal P2PKH ownership tail.
                 expectedPubkeyHash = bytesToHex(
                     std::vector<unsigned char>(scriptPubKey.begin() + 10, scriptPubKey.begin() + 30));
                 Logger::log("Expected Time Lock pubkey hash: " + expectedPubkeyHash);
@@ -6451,7 +6451,7 @@
 }
 
 //==========================================================
-// Add this helper function to calculate satNumber correctly
+// Calculate the atom ordinal.
 //=========================================================
 uint64_t Wallet::calculateCurrentSatNumber() {
     uint64_t totalSats = 0;
@@ -6530,7 +6530,7 @@
     std::string scriptHex = buildTRUScript_OPRETURN(humanData, ownerAddress, 
                                                     inscriptionIndex, satNumber, timestamp);
     
-    // Patch 12B.1: do not construct a script Patch 12A consensus rejects.
+    // do not construct a script Patch 12A consensus rejects.
     if (scriptHex.length() / 2 > tru_limits::MAX_SCRIPT_BYTES) {
         throw std::runtime_error(
             "TRUScript data exceeds consensus script-size limit");
@@ -6807,7 +6807,7 @@
     
     // DON'T do optimistic updates here - let the blockchain handle it when the tx is mined
     
-    // FIX(patch54-safe-spend): require validated mempool acceptance before
+    // require validated mempool acceptance before
     // recording a transfer, so rejection cannot leave false history.
     if (!blockchainPtr->broadcastTransaction(tx)) {
         throw std::runtime_error("TRUScript transfer rejected by mempool/broadcast path");
@@ -7135,10 +7135,10 @@
             throw std::runtime_error("Failed to sign transaction");
         }
 
-        // FIX(patch54-safe-spend): one signature pass is sufficient.
+        // one signature pass is sufficient.
         Logger::log("[transferExtendedToken] Transaction ID: " + tx.txid);
 
-        // FIX(patch54-safe-spend): do NOT mutate confirmed token/UTXO/
+        // do NOT mutate confirmed token/UTXO/
         // ownership indexes before confirmation. applyBlock() is the source of
         // truth once this transaction is actually mined.
         if (blockchainPtr->mempool->addTransaction(tx) != MempoolAddStatus::SUCCESS) {
@@ -7789,7 +7789,7 @@
             continue;
         }
 
-        // FIX(patch54-safe-spend): tokenUTXO is the authoritative direct
+        // tokenUTXO is the authoritative direct
         // association for a token-controlling output.
         const std::string controllingKey = "tokenUTXO:" + txid + ":" + std::to_string(vout);
         if (storage->exists(controllingKey)) {
@@ -7857,7 +7857,7 @@
 
         std::string utxoDataKey = "utxo:" + utxoKey;
         std::string raw;
-        // FIX(patch54-safe-spend): request decoded checksummed payload.
+        // request decoded checksummed payload.
         if (!storage->getWithDataChecksum(utxoDataKey, raw)) {
             Logger::log("[findOneCoinUtxo] UTXO data not found for key: " + utxoDataKey);
             return;
@@ -7878,7 +7878,7 @@
         }
         if (amt == 0) return;
 
-        // FIX(patch54-safe-spend): token transfers use this helper for their
+        // token transfers use this helper for their
         // TRU fee input, so it must enforce coinbase maturity too.
         bool isCoinbase = false;
         uint32_t createdAtHeight = 0;
@@ -7949,7 +7949,7 @@
             return;
         }
 
-        // FIX(patch54-safe-spend): use the direct token UTXO association.
+        // use the direct token UTXO association.
         const std::string controllingKey = "tokenUTXO:" + uv[0] + ":" + uv[1];
         if (storage->exists(controllingKey)) {
             Logger::log("[findOneCoinUtxo] Token-controlling UTXO skipped: " + uv[0] + ":" + uv[1]);
@@ -8644,7 +8644,7 @@
         tx.version = 1;
         tx.lockTime = lockTime;
         tx.vin.emplace_back(txid, vout);
-        // Patch 20: final sequence disables locktime. Use non-final sequence.
+        // final sequence disables locktime. Use non-final sequence.
         tx.vin[0].sequence = 0xfffffffeU;
 
         uint64_t fee = WALLET_MIN_BASE_FEE;
@@ -9839,7 +9839,7 @@
     const std::string& contractAddress,
     int voteOption)
 {
-    // TRU Smart Contract Patch 17 — fail closed on the pre-V1 wallet builder.
+    // fail closed on the pre-V1 wallet builder.
     //
     // The legacy implementation emitted OP_RETURN "VOTE:n" plus a small
     // payment to the contract script. That shape is not canonical Voting V1:
--- a/src/wallet.h
+++ b/src/wallet.h
@@ -640,7 +640,7 @@
         const std::string& targetPrefix,
         const std::string& privateKeyPEM);
     
-    // Add this helper if you don't have it already
+    // Wallet helper declaration.
     std::string extractTargetFromMagicLockScript(const std::string& scriptHex) const;
 };
 
--- a/src/wallet_cli.cpp
+++ b/src/wallet_cli.cpp
@@ -178,7 +178,7 @@
             txid = result.substr(pos + 6);
         }
 
-        // 4) Show your full‐screen dialog
+        // 4) Show the full-screen dialog.
         displaySuccessMessageTX(
             txid,
             wallet.getCurrentAddress(),
@@ -189,7 +189,7 @@
             coutMutex
         );
 
-        // returning empty = “all good, we already displayed it”
+        // An empty result indicates that the dialog already displayed the output.
         return "";
     }
     catch (const std::exception& e) {
--- a/src/wallet_styles.h
+++ b/src/wallet_styles.h
@@ -1,4 +1,4 @@
-// Add this new file: wallet_styles.h
+// Wallet presentation styles.
 #ifndef WALLET_STYLES_H
 #define WALLET_STYLES_H
 
